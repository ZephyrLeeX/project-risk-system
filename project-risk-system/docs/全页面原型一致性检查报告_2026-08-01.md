# 全页面原型一致性检查报告

- 检查日期：2026-08-01
- 原型基线：`/Users/mianmiannuan/Documents/workspace/项目风险管理平台/ui-prototype`
- 正式工程：`/Users/mianmiannuan/Documents/workspace/项目风险管理平台/project-risk-system`
- 检查范围：11 个原型页面、正式前端路由、主要页面结构、核心交互、弹框/抽屉、桌面端与 390×844 手机端、自动化检查

## 1. 总体结论

**当前正式系统尚未与全部原型保持一致，不能判定为全站验收通过。**

| 指标 | 结果 |
|---|---:|
| 原型页面 | 11 页 |
| 已有正式对应路由 | 5 页 |
| 尚未接入正式路由 | 6 页 |
| 基本一致 | 1 页（01） |
| 部分一致 | 4 页（02、04、05、06） |
| 完全缺失 | 6 页（03、07、08、09、10、11） |

当前的主要问题不是字体或基础响应式，而是以下功能一致性缺口：

1. 后台管理首页、API Key 管理、系统配置、审计日志、个人邮箱配置、邮箱同步结果尚未开发为正式页面。
2. Web 风险看板中的 Agent 智能对话被禁用，个人邮箱配置和邮箱同步结果入口缺失，本周周报邮件汇总区块缺失。
3. 用户管理缺少原型中的勾选与批量启用/停用。
4. 角色权限缺少角色复制、启停、权限重置、角色用户入口、权限对照、最近权限变更/审计入口。
5. 项目数据导入把“新建导入抽屉”简化成直接打开文件选择器，并缺少原型的发布确认弹框、历史状态筛选、分页和完整批次详情弹框等流程。

## 2. 页面逐项检查

| 序号 | 原型页面 | 正式路由 | 结论 | 主要证据与差异 |
|---|---|---|---|---|
| 01 | 登录 | `/login` | 基本一致 | 页面标题、账号、密码、密码显隐、记住账号、忘记密码、登录按钮、安全提示和整体双栏布局均已实现；正式页改为统一错误提示，未保留原型的账号/密码逐字段错误区和手机端能力提示文案，因此不是严格一比一。当前浏览器已有登录态，未通过退出登录破坏会话，页面结构由源码对照确认。 |
| 02 | Web 风险看板 | `/` | 部分一致 | 5 个指标卡、本周重点、6 个业务页签、风险筛选、风险/回款/待办/时间线等详情弹框已实现；但 Agent 按钮为禁用状态，顶部个人菜单及个人邮箱配置/同步结果/修改密码入口缺失，本周周报邮件汇总整个区块缺失。正式页还增加了查询按钮和分页，交互结构与原型不完全相同。 |
| 03 | 后台管理首页 | 无 | 缺失 | 路由表无管理概览路由；侧栏“管理概览”被标记为禁用，并错误指向 `/admin/users`。原型中的用户、角色、导入、API 服务统计、健康状态、待关注事项、最近导入和审计记录均未接入。 |
| 04 | 用户管理 | `/admin/users` | 部分一致 | 统计卡、筛选、用户表、新增/编辑抽屉、角色分配、数据范围、启停、解锁、重置密码和操作记录已实现；缺少原型表格勾选列、全选及批量启用/停用。后台侧栏中的 03、07、08、09 入口仍为禁用。 |
| 05 | 角色权限 | `/admin/roles` | 部分一致 | 角色目录、基础信息、默认范围、权限搜索/分组选择、保存、新增和删除自定义角色已实现；缺少原型的角色统计卡、复制角色快捷操作、角色启停、重置权限、查看角色用户、角色权限对照弹框、最近权限变更表和审计日志入口。 |
| 06 | 项目数据导入 | `/admin/imports` | 部分一致 | 当前批次、Sheet 识别、预览、匹配、确认入库、历史记录和回滚已接真实数据；“新建导入”已验证可打开文件选择器，但没有先打开原型的“新建项目数据导入”抽屉；发布采用页面内确认入库，没有原型发布确认弹框；历史状态筛选、分页、错误下载和完整批次详情弹框不完整。 |
| 07 | API Key 管理 | 无 | 缺失 | 路由和正式视图均不存在；侧栏入口为禁用。服务商、模型、密钥、测试、轮换、统计和调用日志均尚未接入。 |
| 08 | 系统配置 | 无 | 缺失 | 路由和正式视图均不存在；侧栏入口为禁用。风险类别/等级、邮箱规则、项目别名、安全与通知策略等均尚未接入。 |
| 09 | 审计日志 | 无 | 缺失 | 路由和正式视图均不存在；侧栏入口为禁用。筛选、详情、Trace ID、复制和导出均尚未接入。 |
| 10 | 风险管理员个人邮箱配置 | 无 | 缺失 | 无正式路由和业务端入口。邮箱连接参数、同步规则、连接测试、保存、立即同步和停用等均尚未接入。 |
| 11 | 邮箱同步结果 | 无 | 缺失 | 无正式路由和业务端入口。同步指标、邮件结果、批次、详情、重试和关联风险等均尚未接入。 |

## 3. 运行与视觉检查结果

### 3.1 桌面端

对 `/`、`/admin/users`、`/admin/roles`、`/admin/imports` 进行真实浏览器检查：

- 页面级横向溢出：0 页。
- 可见正文最小字号：12px。
- 浏览器控制台错误/警告：0 条。
- 风险看板 6 个业务页签中，部门回款、管理者待办、应收与回款、风险时间线、已解除风险均可切换并加载真实内容；项目风险清单为默认页签。
- 新增用户抽屉可打开，包含账号信息、角色分配、项目数据范围和账号安全四部分。
- 新增角色弹框可打开，包含名称、编码、说明、复制来源、默认数据范围和启用状态。
- 新建导入按钮可打开文件选择器，但交互形态与原型抽屉不一致。

### 3.2 手机端（390×844）

对 02、04、05、06 页进行响应式抽查：

- 页面级横向溢出：0 页。
- 可见正文最小字号：12px。
- 风险看板指标卡正常单列重排，风险表格在手机端使用卡片化布局规则。
- 风险看板横向页签条有部分按钮位于可滚动区域，这是原型既有的横向导航方式，不属于页面级溢出。
- 后台侧栏在关闭状态移出视口，通过移动端菜单按钮打开；后台数据表放在独立横向滚动容器中，与后台原型的处理方式一致。

### 3.3 工程检查

执行 `pnpm check` 通过：

- TypeScript/Vue 类型检查通过。
- contracts：2 个测试通过。
- API：33 个测试通过。
- Web：3 个测试通过。
- API 与 Web 生产构建通过。

自动化测试通过只能证明当前代码可构建和既有测试通过，不能替代 11 页原型一致性验收；当前 Web 测试仅覆盖工程基线页，页面级回归覆盖不足。

## 4. 修复优先级

### P0：补齐缺失页面与入口

1. 03 管理概览。
2. 07 API Key 管理。
3. 08 系统配置。
4. 09 审计日志。
5. 10 风险管理员个人邮箱配置。
6. 11 邮箱同步结果。
7. 恢复 02 页个人菜单和 10、11 页入口。

### P1：恢复已实现页面中被删减的原型功能

1. 02：Agent 智能对话、本周周报邮件汇总及详情链路。
2. 04：用户选择、全选、批量启用和批量停用。
3. 05：复制、启停、权限重置、角色用户、权限对照、权限变更记录和审计入口。
4. 06：新建导入抽屉、发布确认弹框、批次详情、历史筛选/分页和错误下载。

### P2：逐页精确视觉收口与自动化

1. 按原型逐区块核对间距、按钮主次、卡片顺序和弹框尺寸。
2. 为 11 页建立桌面端和 390×844 手机端截图基线。
3. 为所有主要按钮、筛选、弹框、抽屉和路由建立页面级端到端测试。
4. 每页完成后单独验收，不在实现下一页时顺带重排已验收页面。

## 5. 最终判定

当前版本的工程质量检查通过，已实现页面的字号和基础响应式合格，但**页面覆盖率只有 5/11，且 02、04、05、06 均存在原型功能删减或交互替换**。

因此首次“所有页面与原型一致”验收结果为：**不通过**。

## 6. 修复后复验（2026-08-01）

以上 P0、P1 修复已全部实施：11 个原型页面均已有正式路由或受权限保护的正式页面组件；02、04、05、06 页被删减或替换的原型功能已经恢复。正式系统仍使用真实接口数据，未改变原型的信息架构和功能模块。

复验结果：

- 正式路由覆盖：11/11。
- 当前系统管理员可访问的 8 个路由均成功加载，页面级横向溢出为 0，可见文字最小字号为 12px。
- 10、11 页按原型由风险管理员使用，分别受 `mailbox.manage_self`、`mailbox.sync_self` 权限保护；系统管理员不能越权访问。
- 新建导入抽屉、Agent 对话、周报详情、权限矩阵、系统配置目录切换、审计导出弹框均已验证可打开。
- `pnpm check` 通过：contracts 2 项、API 33 项、Web 3 项，共 38 项测试；类型检查、API 和 Web 生产构建全部成功。

修复后“所有页面与原型一致”阶段验收结果为：**通过**。后续仅允许在不改变原型模块、字段、顺序和交互链路的前提下接入更多真实后端能力与自动化回归。
