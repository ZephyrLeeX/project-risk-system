# 项目风险管理平台第二阶段验收报告

## 1. 验收结论

- 验收日期：2026-07-31
- 验收结果：通过
- 阶段范围：项目清单 Excel 导入、补充回款与法务事项匹配、真实风险看板、
  部门回款、管理者待办、应收与回款、风险时间线、风险解除与重新打开
- 验收样本：`/Users/mianmiannuan/Desktop/项目清单.xlsx`
- 数据处理：全链路验收采用可回滚批次，完成后业务数据和临时账号已全部清理

第二阶段已经形成从 Excel 数据进入系统、生成项目与风险、形成处理待办、展示回款
与风险进展、解除或重新打开风险、记录时间线和审计、最终按批次回滚的完整闭环。
所有业务查询同时执行权限和项目数据范围校验，可以进入后台剩余模块真实化阶段。

## 2. 已验收业务能力

### 2.1 项目清单 Excel 导入

- 上传 `.xlsx` 文件并执行预检；
- 解析“数据回款”“涵谷回款”“发函-诉讼清单”三个业务工作表；
- 忽略仅用于展示的“汇总”工作表；
- 展示准备、警告和错误行，存在警告时必须人工确认；
- 事务性确认导入并保留批次、原始行和回滚快照；
- 批次历史、逐行明细和完整回滚；
- 中文文件名规范化和20MB文件大小限制；
- 原始文件按批次保存到受控存储目录。

真实样本结果：

| 检查项 | 结果 |
| --- | ---: |
| 主项目行 | 131 |
| 可直接导入 | 1 |
| 警告行 | 130 |
| 错误行 | 0 |
| 涵谷补充回款 | 16 |
| 自动匹配补充回款 | 0 |
| 待人工匹配补充回款 | 16 |
| 法务事项 | 2 |
| 已匹配法务事项 | 2 |

补充回款未匹配时不会创建重复项目或计入部门金额。人工关联后可作为项目金额的
受控回退来源，解除关联后立即恢复为待匹配状态。法务事项只建立项目关联并生成
独立风险，不重复创建主项目。

### 2.2 真实 Web 风险看板

- 项目总数、风险总数、高风险、待回款和已回款5项指标；
- 本周重点关注列表；
- 风险清单关键词、等级、类别和负责人筛选；
- 风险分页、完整详情、证据来源和建议措施；
- 项目数据范围过滤；
- Excel 金额为空时显示“数据待补充”，不制造模拟金额。

真实样本生成131个项目、133条风险，其中项目清单风险131条、法务事项风险2条。
项目风险等级分布为高27、中24、低80；独立风险记录中高风险共28条。

### 2.3 部门回款汇总

- 按部门汇总项目数、计划回款、已回款、待回款和完成率；
- 展示金额完整与缺失项目数量；
- 查看部门下项目明细及金额来源；
- 仅采用完整项目清单金额或管理员确认关联的补充回款；
- 未匹配补充回款不计入任何部门。

验收中关联1条补充回款后，待匹配数量由16降为15，目标项目金额来源变为
`SUPPLEMENTAL`，应收金额为315,000.00元；解除关联后恢复原状态。

### 2.4 管理者待办

- 每条有效风险自动生成且只生成1条关联待办；
- 展示待办总数、待处理、处理中、已完成和紧急数量；
- 自动形成本周日程建议；
- 按负责人和状态筛选；
- 查看关联风险、证据和建议；
- 具备权限的用户可修改负责人、状态、截止日期和处理说明；
- 待办变更写入审计并追加时间线事件。

真实样本生成133条待办，其中28条紧急待办。状态由待处理更新为处理中后，
审计和时间线均生成对应记录。

### 2.5 应收与回款

- 只统计当前存在有效风险的项目；
- 按项目、风险等级和负责人筛选；
- 展示计划金额、已回款、待回款、下一笔回款和完成率；
- 详情保留12个月回款数据和全部有效风险；
- 金额为空时保持为空，不按0计算；
- 下一笔回款只根据真实月度数据或原始回款进展推导。

真实样本包含131个风险项目。原项目清单金额不完整时，131个项目均标记待补充；
关联1条补充回款后，金额完整项目变为1，取消关联后恢复。

### 2.6 风险时间线

- 风险首次识别、风险信息更新和等级变化；
- 待办生成、负责人或截止日期调整、状态推进和完成；
- 风险解除和重新打开；
- 事件类型、风险等级、项目和关键词筛选；
- 分页列表和完整事件详情；
- 事件使用追加式历史，不覆盖既有记录。

真实样本导入后生成266条初始事件，包括133条风险识别和133条待办生成。
推进1条待办后增加到267条；解除风险后增加到269条；重新打开后增加到271条。

### 2.7 已解除风险

- 风险管理员填写5至2000字原因解除风险；
- 风险状态、解除人、解除时间和原因在同一事务中更新；
- 未完成的关联待办同步完成；
- 已解除风险列表支持关键词、原等级、原类别和负责人筛选；
- 保留完整解除信息和风险详情；
- 具备权限的用户填写原因后可重新打开风险；
- 重复解除或重复打开会被拒绝。

验收中解除后已解除风险列表新增1条，关联待办变为已完成；重新打开后列表恢复为0，
待办恢复待处理。解除和重新打开均生成时间线及审计记录。

## 3. 权限与数据范围

| 能力 | 查看权限 | 变更权限 |
| --- | --- | --- |
| Web风险看板及业务页签 | `dashboard.view` | — |
| 项目清单与补充回款导入 | `admin.import.manage` | `admin.import.manage` |
| 管理者待办 | `dashboard.view` | `risk.resolve` |
| 风险解除与重新打开 | `dashboard.view` | `risk.resolve` |

所有风险、待办、回款和时间线查询均执行项目数据范围过滤。验收确认：

- 风险管理员在全部项目范围内可以推进待办、解除和重新打开风险；
- 查看/审计员可以读取授权范围内数据；
- 查看/审计员尝试解除风险返回403；
- 状态、待办、时间线和审计使用同一事务写入，失败时不会出现部分提交。

## 4. 自动化与构建

执行命令：

```bash
pnpm check
```

结果：

| 检查项 | 结果 |
| --- | ---: |
| 共享契约测试 | 2项通过 |
| API测试 | 32项通过 |
| Web测试 | 3项通过 |
| 自动化测试合计 | 37项通过 |
| TypeScript类型检查 | 通过 |
| API生产构建 | 通过 |
| Web生产构建 | 通过 |

真实Excel全链路脚本额外验证了健康检查、预检、警告确认、正式导入、风险与待办生成、
回款关联、全部业务查询、待办推进、风险解除与重开、只读权限、审计和批次回滚。

## 5. 响应式界面验收

### 5.1 桌面端

- 6个业务页签均能加载真实数据；
- 项目风险清单20行首屏数据、部门6行、待办133条、风险项目131条、
  时间线266条、已解除风险空状态均正确；
- 页面宽度与视口一致，无页面级横向溢出；
- 全部可见文字最小字号为12px。

### 5.2 手机端

验收视口为390×844。浏览器内容区宽度375px，业务内容宽度353px：

- 风险、部门回款、待办和应收回款表格均转为单列卡片/网格；
- 卡片宽度325px，没有被压缩成逐字竖排；
- 风险时间线转为单列网格；
- 6个业务页签均无页面级横向溢出；
- 全部可见文字最小字号为12px；
- 风险详情、部门回款详情、待办详情、回款详情和时间线详情5类弹框宽度359px，
  均无内部横向溢出且最小字号为12px。

## 6. 数据库与迁移

已应用8个迁移：

1. `20260727000100_init`
2. `20260727000200_user_project_scopes`
3. `20260728000100_project_import_foundation`
4. `20260730000100_supplemental_collection_import`
5. `20260730000200_legal_matter_import`
6. `20260730000300_risk_dashboard_foundation`
7. `20260731000100_manager_action_items`
8. `20260731000200_risk_timeline_events`

Prisma确认数据库结构为最新，PostgreSQL容器健康状态为`healthy`。

验收回滚和清理后的数据库基线：

| 检查项 | 结果 |
| --- | ---: |
| 项目 | 0 |
| 风险 | 0 |
| 管理者待办 | 0 |
| 风险时间线事件 | 0 |
| 导入批次 | 0 |
| 主表导入行 | 0 |
| 补充回款记录 | 0 |
| 法务事项记录 | 0 |
| 临时导入部门 | 0 |
| 基础风险分类 | 8 |
| 临时浏览器用户 | 0 |
| 有效会话 | 0 |
| 浏览器验收审计残留 | 0 |

## 7. 审计与安全

第二阶段关键动作已纳入审计：

- 项目导入预检、确认和回滚；
- 补充回款人工关联和解除关联；
- 管理者待办更新；
- 风险解除和重新打开。

导入、待办、风险状态和时间线写入均采用事务。接口继续使用HttpOnly会话、
CSRF来源校验、权限守卫和数据范围服务。验收过程未输出或记录管理员密码、
会话令牌、数据库密码等敏感值。

## 8. 验收收尾

- 真实Excel验收批次已经回滚；
- 浏览器验收批次、临时风险管理员、会话、审计和存储文件已经清理；
- API和Web临时开发服务已经停止；
- PostgreSQL容器继续运行，供下一阶段开发使用；
- 本轮未发现需要修改的第二阶段业务代码。

## 9. 下一阶段建议

下一阶段进入后台剩余模块真实化，建议按以下顺序实施：

1. API Key管理：密钥创建、加密保存、掩码展示、启停、轮换、连通性测试和审计；
2. 系统配置：风险规则、同步周期、通知策略和参数校验；
3. 审计日志：查询、筛选、详情、导出和权限控制；
4. 风险管理员个人邮箱配置：加密凭据、连接测试和配置更新；
5. 邮箱同步结果：同步任务、邮件去重、项目匹配、风险提取、失败重试和人工复核；
6. Agent智能对话：在权限和项目数据范围内使用已导入项目、回款、风险和邮件数据。
