# Execution State

Design baseline: <commit>
Plan baseline: <commit>

AI Agent V2 activation:
- Mapping: Task 1→T048, Task 2→T049, Task 3→T050, Task 4→T051, Task 5→T052（恰好 5 个主 Task，一一对应）。
- T048: `REVIEW_PASSED`（Task 1 implementation、quality gates 与独立 Review 完成；start SHA `09d189dbb83610dd0ee7b16b2fefd8caac3afef0`；checkpoint `31e84f1e9dcecee51f6ded41ef9307c4dfe49960`）。
- Current assigned Task: T052 `REVIEW_FAILED`。启动前已核对 T051 `REVIEW_PASSED`、code checkpoint `2696e1cca23d2ba7dc331f8dd3373e1886d4d34a`、metadata commit `1082cc3f8ea9d1ef777592d7937c88d030902af0`；T052 为 `READY / NOT_STARTED`，本轮仅执行 Task 5。Independent Review 未通过：backend legacy Agent replacement + zero runtime references 无法证明，且真实 DeepSeek/Admin credential flow 未提供；未创建 final checkpoint，未启动 Task 6。
- T050 completion: `REVIEW_PASSED`；AgentInteraction、PROJECT_SELECTION、WAITING_FOR_USER、respond/resume、durable/SSE required→resolved 链路完成，Independent Review通过。
- T049 completion: `REVIEW_PASSED`；Native Agent V2 read-only core/durable/SSE test baseline migration完成，Independent Review通过；code checkpoint `3813163`。
- T051: `REVIEW_PASSED`；T052: `REVIEW_FAILED`，仅执行 Task 5；线性依赖 `T048 → T049 → T050 → T051 → T052`。未创建 T052 final checkpoint，未启动 Task 6。
- Provider V2 ADR gate: ADR 0034 已由本轮用户明确授权建立并批准；仅替代 ADR 0005/0028 中与 T048 冲突的 Provider 边界。
- Historical Task/Wave conclusions remain unchanged, including T038 `REVIEW_FAILED` and T039 `DEFERRED`.

Completed waves:
- Wave 1: PASS
- Wave 2: PASS
- Wave 3: PASS
- Wave 4: PASS
- Wave 5: PASS
- Wave 6: PASS
- Wave 8: PASS
- Wave 9: PASS (T018/T024 integration validation and test baseline maintenance)
- Wave 10: PASS (T016/T019/T020/T025 integration validation complete)
- Wave 11: PASS (T026/T042 integration validation and test baseline maintenance complete)
- Wave 12: PASS (T027/T031 Integration validation complete; no integration fix)
- Wave 13: PASS (T028 Integration validation complete; no integration fix; T029 not executed)
- Wave 14: PASS (T029 Integration validation complete; T030 not executed)
- Wave 15: PASS (T030 Integration validation complete; no integration fix)
- Wave 16: PASS (T040 Integration validation complete; PostgreSQL 16 + Redis 7 + real Celery worker environment established; no integration fix)
- Wave 17: PASS (T043/T044 Integration validation complete; 7 frontend-live-consumed `/api` endpoints now in FastAPI surface; no integration fix; T032 candidate untouched)
- Wave 18: PASS (T032 Integration validation complete; FastAPI OpenAPI authority frozen at 93 paths / 243 schemas incl. T043/T044 surface; production OpenAPI byte-identical to tracked openapi.json; openapi.ts reproducibly generated; contracts:check PASS on clean tree; compat PASS (85/85 frontend calls covered); contracts/web typecheck PASS; Ruff/mypy PASS; full pytest 278 passed, 1 skipped; PostgreSQL 16 + Redis 7 + Alembic head 20260812_0008 regression PASS; uv lock --check / git diff --check PASS; 3 rounds export+gen zero diff; no integration fix; T033/T034 readiness synced to READY, not executed)
- Wave 19: PASS (T045 Integration validation complete; `_Contract` serialization-mode OpenAPI schema fidelity restored — admin overview / weekly reports / agent item-level fields no longer degrade to `unknown`/bare `{}`; production FastAPI OpenAPI byte-identical to tracked openapi.json; API surface unchanged 93 paths / 243 schemas / 104 operations; T033/T034 required fields real types in openapi.ts; runtime JSON serialization compatible; contracts:check PASS on clean tree; compat PASS (104 operations, 85/85 frontend calls, no breaking diff); contracts/web typecheck PASS; Ruff/mypy PASS (185 source files); full pytest 283 passed, 1 skipped; PostgreSQL 16 + Redis 7 + Alembic head 20260812_0008 regression PASS; uv lock --check / git diff --check PASS; 3 rounds export+gen zero diff; no integration fix; T033/T034 readiness synced to READY for Wave 20, not executed)
- Wave 20: PASS (T033/T034 Integration validation complete; admin overview + weekly reports + Agent UI consume generated `openapi.ts` authority byte-identical to live FastAPI export; contract pair alignment verified for `/admin/overview`, `/admin/projects/options`, `/weekly-reports/{current,week_start,week_start/projects/project_id}`, `/agent/{help,conversations,conversations/{id}/messages,conversations/{id},confirmations/{token},conversations/{id}/events}`; admin overview loading/empty/error/unavailable + link path/query + dynamic health rollup; weekly current/report/detail + loading/empty/stale/error + detail items; agent create/continue + SSE parser/reducer + reconnect `after=lastEventId` + provider-failure retry + preview one-use confirmation + confirmation error handling + stream read-only + `agent.use` gate; T043 mailbox surface no regression; frontend no fake Agent/static weekly data; 20 `OpenApi.components["schemas"]` aliases, single contract source; `pnpm contracts:check` clean-tree zero diff; contracts/web typecheck PASS; web tests 33 passed; web production build PASS; frozen OpenAPI authority 93 paths/243 schemas unchanged, export/gen zero diff; `git diff --check` PASS; browser E2E unavailable — repo has no E2E harness; no integration fix)
- Wave 21: PASS (T046 Integration validation complete; production scheduler entrypoint verified against real PostgreSQL 16 + Redis 7 + real Celery solo worker — single-active advisory lock fail-fast/reacquire, SIGTERM graceful shutdown, liveness probe, three-action independent cadence + failure isolation, outbox at-least-once publish, end-to-end scheduler publish_outbox → real solo worker → task SUCCEEDED; real-process single-active smoke OK; Ruff/mypy (187 source files) PASS; focused scheduler 21 passed, reliability+mailbox 74 passed, full pytest 304 passed/1 skipped; uv lock --check / git diff --check PASS; no integration fix; frozen write-set + infra/docker-compose.yml unchanged; T035/Wave 22 readiness synced, not executed)
- Wave 22: PASS (T035 Integration validation complete; 7-service production Compose topology verified against real containers — PostgreSQL 16 + Redis 7 + FastAPI API + Celery worker + ADR 0030 single-active scheduler + Vue frontend + TLS reverse proxy; api/worker/scheduler share one image (risk-platform-api:0.1.0, same image id); durable-task end-to-end closed loop: request path (enqueue_task writes PostgreSQL only) → scheduler outbox drain (publish_outbox send_task → publishedAt) → Redis/Celery delivery → real worker claim/handle/finish_task → DurableTask SUCCEEDED (PostgreSQL sole authority, no dual-write); scheduled mailbox path scheduler→schedule_enabled_syncs on cadence (0 action failures); proxy frontend 200 / /api 200 / SSE route 401-auth reachable + proxy_buffering off (regex before /api/ prefix); persistence across down/up (PG data + storage volume + correct mounts); compose secret wiring (session key read-only) + no secret literal leak; 7×service_healthy + 1×service_started dependency ordering; scheduler single-active fail-fast exit 1 (no lock/connection leak, primary unaffected); scheduler fail-fast SQLAlchemy warning triaged as cosmetic frozen-T046 process-exit warning (no real resource leak/deployment failure, T046 not modified per protocol); frozen OpenAPI byte-identical to tracked (422281 bytes, 93 paths/243 schemas/104 ops); pnpm contracts:check clean-tree zero diff; contracts/web typecheck PASS; Ruff/mypy (187 source files) PASS; full pytest (PostgreSQL 16 + Redis 7) 304 passed/1 skipped (1 skip = audit-query-export DB-specialization delegation, not DB-skip-as-PASS); uv lock --check (65 packages) / git diff --check PASS; frozen write-set (scheduler.py/composition.py/main.py/worker.py/celery_app.py) 0 diff vs 732adfb; infra/backup untouched, DG-08 not in scope; DG-05 no extra decision; no integration fix; Wave 23/T036 readiness synced, not executed)
- Wave 23: PASS (T036 Integration validation complete; ADR 0031 encrypted backup/restore verified against real PostgreSQL 16 + real pg_dump -Fc/pg_restore via docker exec — backup authority (PG + durable project-risk-storage; Redis/Celery/cache/temp/log excluded), quiesce-coordinated capture (stop api/worker/scheduler; quiesce failure fail-closed; unquiesce failure surfaced), AES-256-GCM envelope (per-backup DEK wrapped by versioned backup KEK reusing T007 rpenc/KeyRing; three-layer integrity: DEK-wrap tag + per-chunk tag + manifest sha256; AAD binds canonical manifest + component + chunk), key lifecycle (active KEK new backup; retained historical KEK restores old backup without re-encrypt; wrong/missing KEK fail-closed; no key/plaintext leakage in artifact/log/repo), tamper/fail-closed matrix (corrupted DB archive/files archive/manifest/AEAD-tag/partial backup/cleanup failure all fail-closed), fail-closed isolated restore (empty target only, no live overwrite; key lookup→AEAD→manifest→sha256→pg_restore→audit hash-chain→alembic head→file reconcile; partial-set abort), file reconciliation (orphan discard, missing referenced fail-closed), USABLE only after full verification / INCOMPLETE never usable, ADR 0027 backupId=BACKUP_COPY identity semantics (no retention-cleanup overreach), deployment boundary (one-shot, no new Compose service, docker-compose.yml unchanged); 5 Independent Review non-blocking observations all confirmed non-blocking (quiesce full-stop strictly stronger, backupId hash-source breaks circularity, SEVERE cleanup exit-code is ADR-approved hardening, reconcile schema boundary within ADR scope — import_batches.storageKey is the only durable-file reference, outer-header metadata cosmetic); 1 integration fix in infra/backup/** write-set — CLI cmd_restore referenced args.dsn (absent on restore subparser, only --target-dsn defined) → AttributeError; unit tests/PG drill called run_backup/run_restore directly, never the CLI subcommands, so the path was uncovered; minimal fix _build_pg_dumper(args, dsn) with cmd_backup passing args.dsn and cmd_restore passing args.target_dsn (cli.py +4/-4) + 2 new CLI wiring regression tests; CLI production-volume smoke (setup→python -m risk_backup backup→destroy isolated target→restore→integrity verify) PASS end-to-end; Validation: infra/backup 61 passed (59+2), real PG drill 8 passed, Ruff/mypy (28 backup files) PASS, app Ruff/mypy (187 files) PASS, full pytest (PostgreSQL 16 + Redis 7) 303 passed/1 skipped + 1 pre-existing flaky T029/T040 heartbeat timing test that passes in isolation (unrelated to T036, not a blocker), uv lock --check (65) / git diff --check PASS, frozen write-sets (scheduler/celery_app/composition/worker/main) + docker-compose.yml 0 diff vs 732adfb, openapi.json 0 diff vs ac351cb; no WAL/PITR/cloud backup/DG-05 decision; Wave 24/T037 readiness synced to READY, not executed)
- Wave 24: PASS (T037 Integration validation complete; T037 compatibility & security acceptance suite verified against real PostgreSQL 16 + Redis 7 + real production FastAPI app — 17 routers via build_services, isolated per-module Alembic head schema; joint consistency matrix 22 items all PASS: ApiResponse/PaginatedResponse envelope, fixed 401/403/404/422 codes, x-trace-id==traceId, secure session cookie (HttpOnly/Secure/SameSite=lax/Path/expires) + mustChangePassword + session readback, DataScope=NONE empty page; four-role×endpoint auth matrix (24 cases) + five DataScopeType exact project sets + archived exclusion + out-of-scope 404 not leaky 403; CSRF origin, SSRF guard (private/loopback/metadata/IPv6/IPv4-mapped/DNS-rebinding/approved-internal), Excel upload safety, closed Agent Provider fail-closed no secret leakage; audit hash-chain integrity + append-only DB trigger (real UPDATE/DELETE/TRUNCATE raise DBAPIError); reliability idempotent enqueue/crashed-worker recovery RETRY_WAIT/orphan gen-0 redispatch/outbox→Celery publishedAt; rollback atomicity risk+todo+audit all leave no trace; 3 approved test doubles only (current_identity override/_StaticResolver/_CeleryRecorder), no mocked core invariant; Validation: focused acceptance 60 passed, full pytest 363 passed/1 skipped/1 failed (1 failed = pre-existing flaky-timing tests/mailbox/test_parsing.py::test_attachment_output_limit_is_metadata_only returning PARSER_TIMEOUT vs strict OUTPUT_TRUNCATED under full-suite load; isolated 10 passed × 3 rounds stable; production fail-closed safe — PARSER_TIMEOUT returns text="" metadata-only per parsing.py:313, sibling test already tolerates it; T025 frozen write-set not modified, classified known non-blocking T025 follow-up, not Integration failure; Wave 23 precedent), Ruff PASS, mypy Success (202 source files, src+tests scope), uv lock --check (65), git diff --check clean, frozen write-sets T035 docker-compose/T040 composition·celery_app·worker·main/T046 scheduler/T036 infra/backup 0 diff vs checkpoints, frozen OpenAPI byte-identical fresh export 93 paths/243 schemas/104 ops 0 diff vs ac351cb, src/risk_platform 0 diff since ac351cb; no integration fix; Wave 25/T038 readiness synced — T037 dependency satisfied but DG-05 still unresolved, T038 remains BLOCKED_DESIGN_GAP (DG-05)/TODO, Wave 25 not started)
- Wave 21 / T035 readiness: `READY`（dependency 层面）。T035 direct deps T031/T032/T033/T034/T040/T046 均 `REVIEW_PASSED`，Wave 21 Integration `PASS`。Wave 22（=T035）未启动，仅同步 readiness。
- T035 execution stop: `DESIGN_GAP`（DG-16）。Lean Execution Mode 上下文加载确认 T035 Objective 要求的 "scheduler" 组件在批准的设计中不存在：设计 §4 架构图无 scheduler/beat 框，ADR 0006 只定义 Worker + Redis broker；ADR 0018 要求 outbox publisher（"提交后向 Celery 发布"）与 Reconciler 但未规定触发机制/cadence；T024 `schedule_enabled_syncs`（`_INTERVAL_MINUTES=30`）无 production 驱动。核验：`publish_outbox`/`reconcile`/`schedule_enabled_syncs` 均无 production caller，无 `*scheduler*`/`*beat*` entrypoint 模块，`celery_app.py`（frozen T040）无 `beat_schedule`；`enqueue_task` 创建 `TaskOutbox` 后 production 路径从不发布，任务持久化但不投递。materialize scheduler 需新增 application entrypoint（跨 T040 composition 边界 + cadence/single-active/错误处理未批准决策）或在 frozen `celery_app.py` 加 `beat_schedule`（`DESIGN_DEVIATION`），均超出 T035 Compose/proxy/Dockerfile/env/docs write-set。按 ORCHESTRATOR 协议与 T027/T029/T030/T033 stop 先例记录 `DESIGN_GAP` 并停止：未实施、未审查、未创建 code checkpoint，`infra/docker-compose.yml` 未修改。Wave 21 保持 `IN_PROGRESS`（partial，无 code checkpoint）；Wave 21 Integration、下一 Wave、DG-05/DG-08 均未启动/未处理。详见 `docs/implementation/reports/T035.md` 与 `docs/implementation/reports/WAVE-21-PARTIAL.md`。
- DG-16 resolution（design/metadata checkpoint，未实施 production code、未实现 scheduler、未修改 `infra/docker-compose.yml`、未执行 T035/T046、未启动 Wave 21 Integration）：ADR 0030 批准 production 调度器拓扑为**独立 scheduler 进程**（不采用 Celery Beat），以 PostgreSQL session-level advisory lock 保证 single-active，周期驱动三个已存在 entry point——outbox drain `publish_outbox`（默认 5s）、reconciliation `reconcile`（默认 30s）、scheduled mailbox sync `schedule_enabled_syncs`（默认 5min）。cadence 为 operational 默认值（env 可调），非 SLO（DG-05 out of scope）。ADR 0018 outbox publisher 澄清为 scheduler 的 drain tick（非请求路径 inline `send_task`）：请求路径仅写 PostgreSQL（`enqueue_task` 写 `DurableTask`+`TaskOutbox`，不碰 broker），无 DB/Celery dual-write，PostgreSQL 仍为唯一 authority，Redis/Celery 仅为 delivery/execution transport。failure 语义：per-tick/per-function 隔离、driven 函数幂等、scheduler 不写 audit（ADR 0017，业务效果由 owning domain path 审计）、日志不含 secret/mail content/payload（ADR 0014/0007）、tick 异常捕获续行、仅启动配置/DB 不可达超过有界重试才崩溃触发 `restart`。startup 获取 advisory lock（失败 fail-fast 退出），SIGTERM 完成当前 tick 后释放 lock，liveness 探针需 lock 持有 + 最近 tick 在窗口内。**归属**：scheduler application entrypoint 属新增 remediation Task **T046**（`READY`，deps T008/T024，write-set 限 `scheduler.py`+tests，只读复用 `risk_platform.db`/`celery_app`/`publish_outbox`/`reconcile`/`schedule_enabled_syncs`，禁止编辑 `composition.py`/`celery_app.py`/`worker.py`/`main.py`/driven 函数）；T035 仅拥有 Compose `scheduler` service wiring（image/command/env/healthcheck/restart/depends_on）+ env examples + deployment docs，**T035 deps 增 +T046**。DAG 新增 `T008 & T024 --> T046 --> T035`；Wave 表插入 Wave 21 = T046，T035 后移至 Wave 22，其后各 Wave 顺延 +1（T036→23、T037→24、T038→25、T039→26）。设计 §4 架构图与 §8 已补充 Scheduler 节点与 ADR 0030 契约。T035 状态由 `DESIGN_GAP` 恢复为 `READY`（仅 metadata，未实施，blocked on T046）。本 ADR 范围不含 scheduler 实现与 T035 实施；DG-05/DG-08 保持 out of scope。详见 `docs/adr/0030-define-production-scheduler-and-outbox-delivery-topology.md`、`docs/implementation/tasks/T046-production-scheduler-entrypoint.md`。

Completed tasks:
- T048 REVIEW_PASSED
- T001 REVIEW_PASSED
- T002 REVIEW_PASSED
- T003 REVIEW_PASSED
- T005 REVIEW_PASSED
- T006 REVIEW_PASSED
- T007 REVIEW_PASSED
- T009 REVIEW_PASSED
- T008 REVIEW_PASSED
- T010 REVIEW_PASSED
- T041 REVIEW_PASSED
- T004 REVIEW_PASSED
- T011 REVIEW_PASSED
- T015 REVIEW_PASSED
- T017 REVIEW_PASSED
- T021 REVIEW_PASSED
- T023 REVIEW_PASSED
- T027 REVIEW_PASSED
- T031 REVIEW_PASSED
- T028 REVIEW_PASSED
- T029 REVIEW_PASSED
- T030 REVIEW_PASSED
- T040 REVIEW_PASSED
- T043 REVIEW_PASSED
- T044 REVIEW_PASSED
- T032 REVIEW_PASSED
- T045 REVIEW_PASSED
- T033 REVIEW_PASSED
- T034 REVIEW_PASSED
- T046 REVIEW_PASSED
- T035 REVIEW_PASSED
- T036 REVIEW_PASSED
- T037 REVIEW_PASSED
- T047 REVIEW_PASSED

Current:
- Wave 6: PASS (T004/T008/T010 remain REVIEW_PASSED; PostgreSQL integration validation passed)
- Wave 7: PASS (T011/T012/T013/T014/T015/T017/T021 REVIEW_PASSED; T006/T013 remediation and T012 regression fixture fix validated)
- T004: REVIEW_PASSED
- T008: REVIEW_PASSED
- T010: REVIEW_PASSED
- T011: REVIEW_PASSED
- T013: REVIEW_PASSED
- T014: REVIEW_PASSED
- T012: REVIEW_PASSED
- T015: REVIEW_PASSED
- T017: REVIEW_PASSED
- T021: REVIEW_PASSED
- T022: REVIEW_PASSED
- T023: REVIEW_PASSED
- Wave 8: PASS (T022 import-cycle fix reviewed; full pytest and PostgreSQL/Alembic integration validation passed)
- Current Wave: Wave 10 PASS (T016/T019/T020/T025 REVIEW_PASSED; Integration PASS); Wave 9 complete (T018/T024 REVIEW_PASSED; Integration PASS).
- Wave 10 design resolution: ADR 0023 supplies the missing T016 admin overview item contract. No Wave 10 Integration or next-Wave work was started. DG-04/DG-05/DG-08 remain out of scope.
- T019: REVIEW_PASSED (selected as the single Wave 10 work unit; see `docs/implementation/reports/T019.md`).
- T020: REVIEW_PASSED (collection/department remediation, Independent Review and PostgreSQL 16 focused validation complete; see `docs/implementation/reports/T020.md`).
- T025: REVIEW_PASSED (ADR 0024 attachment safety implementation, Independent Review and validation complete; code checkpoint `ec403f99c606ba7c6ff429b0f375c9cc85d04439`).
- T016: REVIEW_PASSED (ADR 0023 admin overview implementation, Independent Review and PostgreSQL 16 focused validation complete; code checkpoint `cbd5569869cb36c5b7ae93645edeeecfbfb49842`).
- T018: REVIEW_PASSED (checkpoint `b8a5a5f`; see `docs/implementation/reports/T018.md`).
- T024: REVIEW_PASSED (DG-10 resolved; implementation and Independent Review complete; checkpoint `597a43b830e2e639d17b775c069a8fbfb896efd4`).
- Wave 9 Integration: PASS; the six stale test-baseline assertions were updated only in tests, followed by full validation. No production redesign or next-Wave task started.
- Wave 10 Integration: PASS; cross-module/full validation and PostgreSQL 16/Alembic validation complete. Minimal integration fix changed T025 helper IPC from Queue to Pipe; no safety boundary was widened. See `docs/implementation/reports/WAVE-10.md`.
- Current Wave: Wave 12 PASS. T027 is REVIEW_PASSED (code checkpoint `8beeb062069ddc5dd6104e0b80178fcb90da9e3b`). T031 is REVIEW_PASSED (code checkpoint `6545fdaf6ad4029c044c2338cc2fb36b7e385b03`). Wave 12 Integration PASS; no integration fix. Wave 11 PASS (T026/T042 REVIEW_PASSED; Integration PASS).
- Current Wave: Wave 15 `IN_PROGRESS`; T030 is the sole authorized work unit after ADR 0029 resolved its REPORT category `DESIGN_GAP`. Wave 14 is PASS; T029 is REVIEW_PASSED (code checkpoint `b57e831e68e47cdfba43f78285d10481c15000e1`). T040 production composition write-set remains untouched. DG-05 and DG-08 remain out of scope.
- T030: `REVIEW_PASSED`. After ADR 0029 resolved its REPORT category `DESIGN_GAP`, T030 implemented category-bound preview confirmation through existing domain services (`RisksService.create_in_session`/`resolve_in_session`, `TodosService.process_in_session`) with caller-owned transactions. The ADR 0026 `RISK_CATEGORY_OPTIONS_V1` projection strictly uses `option_id`/`default_level`; PROCESS/RESOLVE explicit `categoryOptionId: null` fails closed via `model_fields_set`; `AGENT_REPORT_CATEGORY_STALE` completes the durable retry → rebuild → exhaustion loop; legacy REPORT tokens and invalid category payloads fail closed. Independent Review passed after remediation; focused PostgreSQL 16 + Redis 7 acceptance `10 passed`; Ruff, mypy, `uv lock --check`, `git diff --check` all pass. Code checkpoint: `933fb2549dbda304e1675a755786249f33c0e547`. Wave 15 remains `IN_PROGRESS`; Integration, next Wave, T040, DG-05 and DG-08 remain untouched.
- T026: REVIEW_PASSED (Celery worker isolation remediation complete. PostgreSQL 16 + Redis + real Celery `solo` worker acceptance/negative tests pass: `16 passed`; each worker handler asserts the random Alembic-created temporary PostgreSQL schema. `register_executor` is app-local (`shared=False, lazy=False`), preventing stale executor/handler/factory leakage across test Celery apps. Independent Review passed; no dispatcher direct execution was used as worker proof. Checkpoint: `76c5ef6cb50705b63ad86e7a9b05d00bf9a45da4`.)
- T042: REVIEW_PASSED (approved retention configuration, frozen facts, hold persistence/API, terminal-state trigger, metadata-only audit and lock-ordered protection recheck complete. PostgreSQL 16 focused validation `17 passed`; Independent Review passed. Code checkpoint: `d6652c82529c2d2902a5f476d225e582b38ebaf3`. See `docs/implementation/reports/T042.md`.)
- DG-01: RESOLVED by ADR 0019
- DG-02: RESOLVED by ADR 0018
- DG-03: RESOLVED by ADR 0019
- DG-04: RESOLVED by ADR 0027
- DG-06: RESOLVED by ADR 0020
- DG-07: RESOLVED by ADR 0020
- DG-09: RESOLVED by ADR 0021
- DG-10: RESOLVED by ADR 0022
- DG-11: RESOLVED by ADR 0024
- DG-12: RESOLVED by ADR 0026
- DG-13: RESOLVED by ADR 0021 immutable received-time addendum
- DG-14: RESOLVED by ADR 0028
- DG-15: RESOLVED by ADR 0029
- DG-16: RESOLVED by ADR 0030
- DG-08: RESOLVED by ADR 0031
- DG-05: RESOLVED by ADR 0032

Checkpoint commits:
- T048: `31e84f1e9dcecee51f6ded41ef9307c4dfe49960` (AI Agent V2 Task 1 Provider V2 + DeepSeek Official Adapter; metadata recorded in the following commit)
- T030 design: `e57646e3f05c87b21bab632601ba64f1dbff860a`
- T030: `933fb2549dbda304e1675a755786249f33c0e547`
- T029 design: `51074211d3fd388514c4c8405b32295a9dd214cc`
- T029: `b57e831e68e47cdfba43f78285d10481c15000e1`
- T031: `6545fdaf6ad4029c044c2338cc2fb36b7e385b03`
- T027: `8beeb062069ddc5dd6104e0b80178fcb90da9e3b`
- T025 design: ad37f90a6ae0cb643a19c09d9c91b02b8eacbad7
- T025: ec403f99c606ba7c6ff429b0f375c9cc85d04439
- Wave 10 integration fix: `7b9722075ca3bc8358789198b7ef6b0e6282fcfa`
- Wave 10 final checkpoint: `7b9722075ca3bc8358789198b7ef6b0e6282fcfa` (metadata recorded in the following report commit)
- Wave 11 final checkpoint: `4487c09a2281c462e3b4c93e0553080a56af4531` (metadata recorded in the following report commit)
- Wave 12 final checkpoint: `286dbab0dca17870434a2bc7e5ddac79b2f9109f` (metadata recorded in this report commit)
- Wave 13 final checkpoint: `841a38ce37c8e34bff513b72f6236d64303d9b6b` (metadata recorded in the following report commit)
- Wave 15 final checkpoint: `533f74011362c0a44ad662a660b061b9a9833cef` (metadata recorded in the following report commit)
- T040: `2a99da0`
- Wave 16 final checkpoint: `f794daa6847eb0bcae67ba834a3ef7ffb6f5a83e` (metadata recorded in this report commit)
- T032 migration-coverage DESIGN_GAP resolution checkpoint: `6f90623f59ac1132f33edb52d432c5816770632a` (T043/T044 ownership mapped; T032 `DESIGN_GAP`→`BLOCKED`; no production code; candidate work remains uncommitted in working tree)
- T043: `eb7c9243b170feda08776912c9dcde932ca90114` (mailbox sync-results browse/retry surface; metadata recorded in the following report commit)
- T044: `1dc067a44a3e5bcf5e4ecc5305e2060f713ecbd5` (admin project-options selector; metadata recorded in the following report commit)
- Wave 17 final checkpoint: `5aa4a35` (metadata recorded in this report commit)
- T032: `b9a172c2aad3b68239c32dc4ac6a4c462bd85c46` (OpenAPI authority re-frozen at 93 paths / 243 schemas incl. T043/T044 surface; compat PASS; regeneration zero diff; metadata recorded in the following report commit)
- Wave 18 final checkpoint: `4368f025b7f41ef14b0bd2e286d8a4bc8cb78d96` (metadata recorded in this report commit)
- Wave 19 final checkpoint: `6609302c0e5e530186bb6cfd3208324f85b01c23` (metadata recorded in the following report commit)
- Wave 20 final checkpoint: `1ba4f5e2d05f9131e91b4666165205dc631f8222` (metadata recorded in the following report commit)
- Wave 21 final checkpoint: `3722a6b94fa4692d1452c6c34eb072be0fa4382f` (metadata recorded in the following report commit)
- Wave 22 final checkpoint: `15489ef60d8df94ec886263cd2c838970d0fb41b` (metadata recorded in this report commit)
- T035: `732adfb0c742f486748cc1aff707231b07820b34` (production Compose + HTTPS proxy topology — single-server stack: PostgreSQL 16 + Redis 7 + FastAPI API (uvicorn) + Celery worker + ADR 0030 single-active scheduler + Vue frontend + TLS reverse proxy; api/worker/scheduler share one image; scheduler invokes T046 `main()` entrypoint unchanged, scheduler.py/T040 composition untouched; SSE no-buffer; persistent volumes; compose secret for session key; `uvicorn[standard]` added + re-locked; Independent Review PASS; 23-item deployment validation matrix all PASS; Wave 22 IN_PROGRESS partial, Integration not started; metadata recorded in the following report commit)
- T036: `5824695f73c1c31e272b17e88324e13b674b80d1` (encrypted backup/restore runbook — ADR 0031 `risk_backup` one-shot operator package: quiesce-coordinated consistent backup set (PostgreSQL `pg_dump -Fc` + durable file tar), AES-256-GCM envelope encryption reusing T007 `rpenc`/`KeyRing` (per-backup DEK wrapped by versioned backup KEK loaded from host read-only files, independent of DATA_ENCRYPTION_KEY), manifest-bound three-layer integrity, versioned KEK + retained historical decrypt (no re-encrypt), fail-closed isolated restore (key lookup + AEAD/sha256/manifest + audit hash-chain + file reconcile + partial-set abort), metadata-only logging (KEK version only, no business audit writes); Redis/Celery/cache/temp/log excluded, no key material in artifacts/repo/logs; no Compose service, `infra/docker-compose.yml`/T035/T040/T046 frozen write-sets untouched, no WAL/PITR/pg_basebackup; Independent Review REVIEW_PASSED; 59 tests (51 unit + 8 real PostgreSQL 16 drill) pass; Ruff/mypy strict (27 files)/uv lock --check/git diff --check clean; write-set infra/backup/** only; Wave 23 IN_PROGRESS partial, Integration not started; metadata recorded in the following report commit)
- T035 DESIGN_GAP (DG-16) metadata checkpoint: `917af4c892ea273b626a95d4a9cc8119564780a0` (Wave 21 IN_PROGRESS; execution stop — no production code, no code checkpoint; `infra/docker-compose.yml` untouched; metadata recorded in this commit)
- DG-16 resolution / ADR 0030 design checkpoint: `67f1ddd01ec60e96dc7442549d6647e1fd9d6d89` (ADR 0030 approves independent single-active scheduler process; new remediation Task T046; T035 deps +T046 and restored to READY; Wave 21=T046, T035→Wave 22; no production code, `infra/docker-compose.yml` untouched; metadata recorded in this commit)
- DG-08 resolution / ADR 0031 design checkpoint: `2796971de280ff44b77d8ba1f51565f60fa675e1` (ADR 0031 approves encrypted backup/restore contract — quiesce-coordinated consistent backup set, AES-256-GCM envelope encryption reusing T007 `rpenc`/`KeyRing`, manifest, versioned backup KEK + retained historical decrypt, fail-closed isolated restore, metadata-only logging, `backupId` as ADR 0027 `BACKUP_COPY` identifier; T036 restored `DESIGN_GAP (DG-08)`→`READY` metadata-only; no production code, `infra/backup/**` untouched, T035/T040/T046 frozen write-sets untouched, Wave 23 not started; metadata recorded in this commit)
- T046: `21c6d3d0b1be32034fb55bf17e02501506d62ccc` (production scheduler entrypoint — single-active PostgreSQL advisory-lock process driving outbox drain/reconcile/scheduled mailbox sync; +21 tests; Independent Review REVIEW_PASSED; Wave 21 IN_PROGRESS, Integration not started; `infra/docker-compose.yml` untouched; metadata recorded in the following report commit)
- T033 DESIGN_GAP metadata checkpoint: `d1df28be5696c3218654dba45be39e796f7123ef` (readiness stop; no code; frozen OpenAPI authority defect blocks T033/T034 as generated-type consumers)
- T045 design/metadata checkpoint: `c95efd6c043d380edd993703e712fb4de59eeb0d` (T033 DESIGN_GAP resolution; new remediation Task T045 contract + DAG/wave/catalog/state updates; no production code, no OpenAPI re-freeze; metadata recorded in this commit)
- T045: `ac351cb8aed4ccfd35a13fdf4becbdc2e80f22df` (restore `_Contract` serialization-mode schema fidelity by omitting the wildcard `field_serializer` return annotation; re-freeze `openapi.json`/`openapi.ts`; +5 schema-fidelity tests; runtime JSON unchanged; 93 paths/243 schemas preserved; metadata recorded in the following report commit)
- T033: `d8dfcec2b89db7205521b48bd3ae0cf275d1a4e3` (admin overview cutover: consume generated `AdminOverview`/item real types, remove fixed health/attention/audit arrays, add loading/empty/error/unavailable semantics per ADR 0023; permissions/routes/metrics preserved; web typecheck/test/build + contracts typecheck + contracts:check zero-diff + git diff --check PASS; Independent Review REVIEW_PASSED; metadata recorded in the following report commit)
- T034: `2a232d43f39fdfdf29a4f315bbbd9ca108a5fa78` (weekly reports + Agent cutover: removed local fabricated Agent answers and static weekly array; weekly panel/detail consume generated `WeeklyReportResponse`/`WeeklyProjectDetail`; Agent drawer consumes `agentApi` REST + PostgreSQL-backed SSE via `useAgentConversation` (create/continue/resume-after-disconnect/retry/preview/one-use confirmation with feedback); agent nav gated by `agent.use`; loading/empty/error/stale/disconnect/reconnect/retry/preview/confirmation semantics; no `any`, generated-type consumption only; web typecheck/test/build + contracts typecheck + contracts:check zero-diff + git diff --check PASS; Independent Review REVIEW_PASSED; metadata recorded in the following report commit)
- T033 DESIGN_GAP resolution（metadata/design checkpoint，未修改 production code、未 re-freeze OpenAPI、未执行 T033/T034、未启动 Wave 19）：新增单一 contract-fidelity remediation Task **T045**（`READY`，deps T016/T027/T029/T032），不重开 T016/T027/T029/T030/T032 已完成 Task。授权最小跨 frozen write-set 修复范围：修正三个 `_Contract` 副本（`admin/overview/schemas.py`、`weekly_reports/schemas.py`、`agent/schemas.py`，三者定义相同 wildcard `field_serializer("*", when_used="json", check_fields=False)`）的 datetime/runtime 序列化实现，使 FastAPI serialization-mode JSON schema 恢复真实字段类型（无 `unknown`），runtime JSON 行为保持兼容（datetime→UTC RFC3339 毫秒+`Z`、`extra="forbid"` 不变），且不改变 API surface（path/method/envelope/字段名不变，re-freeze 仅补回字段类型）。Re-freeze 的 `packages/contracts/openapi/openapi.json` 与 `packages/contracts/src/generated/openapi.ts` ownership 归 T045（复用 T032 的 `openapi_export.py` tooling 与 `pnpm contracts:check`/`contracts:gen`），生成产物不得手工编辑（ADR 0011）。T045 acceptance 含 `model_json_schema(mode="serialization")` 真实类型断言、`pnpm contracts:check` zero diff、contracts/web typecheck、export+gen 3 轮确定性、既有 runtime 序列化测试不变。T033（deps +T045）、T034（deps +T045）显式 `BLOCKED` 于 T045，T045 `REVIEW_PASSED` 后恢复可执行。DAG 新增 `T016 & T027 & T029 & T032 --> T045`、`T045 --> T033 & T034`；Wave 表插入 Wave 19 = T045，原 Wave 19（T033/T034）后移至 Wave 20，其后各 Wave 顺延 +1（T035→21、T036→22、T037→23、T038→24、T039→25）。本轮不创建 ADR（ADRs 0021/0023/0028/0029 已定义正确契约，缺陷为 fidelity bug 而非缺失决策）。Wave 19 保持 `NOT_STARTED`；T033/T034、Wave 19 Integration、下一 Wave、DG-05/DG-08 均未启动或处理。详见 `docs/implementation/tasks/T045-contract-schema-fidelity.md` 与 `docs/implementation/reports/T033.md`。
- T010: e30dd45
- T004: 1b4cfa8
- T011: baa3208
- T012: 7361d26
- T013: 13463f3
- T014: d9bde24
- T015: dd603dc
- T017: 7c4b2e60f1b54ad11c18caeae6a65a440026e9db
- T021: ae2dc4e

Architecture changes:
- ADR 0017: Audit is metadata-only; no snapshot/redaction.
- ADR 0018: Durable tasks use PostgreSQL task/outbox facts, domain-to-task references, fenced leases and at-least-once dispatch.
- ADR 0019: Agent API, confirmation and PostgreSQL-backed SSE contract.
- ADR 0020: Agent Celery execution and domain-command contract.
- ADR 0021: PostgreSQL weekly aggregate lifecycle contract.
- ADR 0022: Mail fetch-to-parse/AI UID/UIDVALIDITY-only durable handoff, stage terminal states, crash recovery and cursor contract.
- ADR 0023: Admin overview health, attention, recent-audit item and partial-data contract.
- ADR 0024: Mail attachment type allowlist, parser isolation, resource limits, metadata-only outcomes and ADR 0022 retry handoff.
- ADR 0025: Mail source-refetch-to-Provider versioned derived-content, redaction, size, retry and metadata-only observability contract for T026.
- ADR 0026: Mail Provider risk-category option projection, deterministic local mapping, fail-closed classification and compatibility contract for T026.
- ADR 0028: Agent execution task/configuration snapshot, restricted Provider protocol, invalid-output, retry/timeout/cancellation and PostgreSQL SSE fact boundary for T029.
- ADR 0029: Agent REPORT active-category authority, reused opaque option projection, canonical category binding, confirmation revalidation, retry/audit and compatibility contract.

Environment:
- Python validation via mise explicit tool selection.
- PostgreSQL integration tests required where applicable.

Important invariants:
- FastAPI is production backend.
- NestJS reference-only.
- PostgreSQL only.
- No dual write.
- Human-facing reports Chinese.

Integration blockers:
- T013 regression test fixture was minimally corrected and passes independently; no production code change.
- T006 migration portability remediation explicitly installs pgcrypto in `public` and calls `public.digest`; isolated PostgreSQL upgrade/hash regression passes with `search_path` excluding `public`.
- Full Ruff and mypy pass after the T013-only remediation.
- The T012 role update regression (`422` caused by an obsolete `code` field in the update test fixture) was fixed by updating the fixture only; no production code or approved semantics changed.
- Current Wave: Wave 7 is PASS; T011, T012, T013, T014, T015, T017 and T021 have been completed in this execution unit sequence.
- T017 implementation, Independent Review, validation and checkpoint commit are complete. T021 remains READY and was not started.
- T021 implementation, Independent Review and validation are complete. Wave 7 Integration Fix passed; Wave 8 remains not started. See `docs/implementation/reports/WAVE-07.md`.
- Wave 7 final integration checkpoint includes the T012 fixture-only regression fix; Wave 8 was then started by the Orchestrator.
- Wave 8 Integration Fix: T022 package `__init__` eager re-export was removed to resolve the collection cycle; regression tests, full pytest, PostgreSQL 16 and Alembic validation passed. See `docs/implementation/reports/T022.md` and `docs/implementation/reports/WAVE-08.md`.
- T022 checkpoint: `a91d203`.
- T023 checkpoint: `d721895f241d2eb7d1a8ee446cd189c3842a4c74`.
- T022 Quality Fix checkpoint: `ba339789409dc8138f763c6325262e9cff1be319`.
- Wave 8 prior failed-integration checkpoint: `9c1b598d425a06cb3ed6753252d9209e6a73aaae`.
- Wave 8 final Integration Fix checkpoint: `5f5d421f5bc4f7a86c0f70c1febda6c1c8ae515a`; see `docs/implementation/reports/WAVE-08.md`.
- T018 checkpoint: `b8a5a5f`; see `docs/implementation/reports/T018.md` and `docs/implementation/reports/WAVE-09-PARTIAL.md`.
- DG-10 design checkpoint: created by ADR 0022 and synchronized task/design state; T024 implementation is complete and reviewed.
- DG-11 design resolution: ADR 0024 approves the attachment safety policy and returns T025 to READY. No T025 implementation, Wave 10 Integration or next-Wave work was started; DG-04/DG-05/DG-08 remain out of scope.
- T026 design resolution: ADR 0025 approves `MAIL_PROVIDER_DERIVED_CONTENT_V1`, including the only permitted derived text, deny-list, filtering, limits and retry/log/audit boundary. T026 is READY; no implementation, T042, Wave 11 Integration or next-Wave work has started. DG-04/DG-05/DG-08 remain out of scope.
- T026 design checkpoint: `56368827d42406dc73fc371dfa31db2a03a3c096`.
- DG-04 design resolution: ADR 0027 approves bounded, versioned retention configuration; frozen import/conversation expiry and rollback-protection facts; an auditable hold state machine; and fail-closed deterministic cleanup/backup-copy predicates. T042 is READY. No T042 implementation, Wave 11 Integration or next-Wave work has started; DG-05 and DG-08 remain out of scope.
- DG-04 design checkpoint: `c744b6a889f09be71ff088c3b9f4ba0892c73c9a`.
- DG-12 design resolution: ADR 0026 approves `MAIL_PROVIDER_DERIVED_CONTENT_V2` plus `RISK_CATEGORY_OPTIONS_V1`, one opaque classification choice, deterministic active-local-category mapping, fail-closed invalid/ambiguous handling and retry/audit boundaries. T026 is READY; no implementation, T042, Wave 11 Integration or next-Wave work has started. DG-04/DG-05/DG-08 remain out of scope.
- T024 implementation and Independent Review: `REVIEW_PASSED`; see `docs/implementation/reports/T024.md` and `docs/implementation/reports/WAVE-09-PARTIAL.md`.
- T024 checkpoint: `597a43b830e2e639d17b775c069a8fbfb896efd4`.
- T019 implementation, Independent Review and validation are complete; checkpoint is recorded in `docs/implementation/reports/T019.md`.
- T019 checkpoint: `4ddf34e`.
- T020 checkpoint: `8a0297ba1eaa5d72432a442cbda746d0ff480075`.
- T016 checkpoint: `cbd5569869cb36c5b7ae93645edeeecfbfb49842`; see `docs/implementation/reports/T016.md` and `docs/implementation/reports/WAVE-10-PARTIAL.md`.
- Wave 9 final Integration checkpoint: created after recording the PASS result and test baseline maintenance in `docs/implementation/reports/WAVE-09.md` and this state file.
- T042 execution stop: candidate implementation validation passed on isolated PostgreSQL 16, but Independent Review is `REVIEW_FAILED`. ADR 0027 requires a T042 human hold management surface without an approved public URL/envelope/error contract; this is `DESIGN_GAP` under T042's stop condition. The candidate additionally needs a single lock ordering and non-reactivatable terminal hold enforcement. Wave 11 remains `IN_PROGRESS`; no T042 checkpoint, Integration, DG-05, DG-08 or next-Wave work was started.
- T042 DESIGN_GAP resolution: ADR 0027 addendum now approves the human hold create/release/query HTTP contract, fixed metadata-only audit boundary, exact retry/conflict outcomes, resource advisory/fact/hold PostgreSQL lock ordering, and database-enforced non-reactivatable terminal states. T042 is `READY` only; the retained candidate is neither accepted nor code-checkpointed and requires a fresh implementation/review cycle. Wave 11 remains `IN_PROGRESS`; no Integration, next-Wave, DG-05 or DG-08 work was started. Design checkpoint: `ee4de76840f9181caf3158a448cb45af9949112d`.
- Wave 11 Integration: `PASS`; stale PostgreSQL test baselines now include T042 frozen import facts, Alembic head `20260812_0006` and `retention_holds_lifecycle_guard`. Cross-module `37 passed`, full pytest `204 passed, 1 skipped`, Ruff, mypy, lock, diff and PostgreSQL 16/Alembic validation all pass. Wave 12, T027 and T031 were not started; DG-05/DG-08 were not handled. See `docs/implementation/reports/WAVE-11.md`.
- Wave 12 readiness: `READY`. T027 and T031 dependencies are complete and their applicable design gaps are resolved. T031 retains a stale `BLOCKED_DESIGN_GAP (DG-04/DG-10) / TODO` header despite the approved resolutions, so T027 was selected as the single unambiguous READY task. Wave 12 is `IN_PROGRESS`; T031, DG-05 and DG-08 remain untouched.
- T027 execution stop: `DESIGN_GAP`. ADR 0021 and the frozen design require `sent_at`, falling back to immutable `received_at`, for Shanghai-week ownership, but `MailMessage` has no `receivedAt` and the approved handoff metadata persists only `sent_at`. Substituting `createdAt` or `processedAt` would make delayed synchronization change week ownership. T027 excludes new mail storage and explicitly stops when the required source service is unavailable. No production/test implementation, Independent Review, validation, code checkpoint, T031 work, Wave 12 Integration, DG-05 or DG-08 work was performed. See `docs/implementation/reports/T027.md` and `docs/implementation/reports/WAVE-12-PARTIAL.md`.
- Wave 12 design/metadata sync: T031's stale `BLOCKED_DESIGN_GAP (DG-04/DG-10) / TODO` header was corrected to `TODO` after confirming ADR 0027/addendum, ADR 0022 and completed T042 satisfy those blockers; T031 was not executed. ADR 0021's approved immutable received-time addendum resolves DG-13 with IMAP `INTERNALDATE`, a frozen first-durable-observation fallback, UTC `timestamptz(3)` facts, migration/backfill ownership and database-enforced retry/refetch stability. T027 is restored to `READY`; T031 remains `TODO`; Wave 12 remains `IN_PROGRESS`. No Task implementation, Integration, next Wave, DG-05 or DG-08 work was started.
- T027 implementation: `REVIEW_PASSED`. Weekly aggregate/query/invalidation/reconciliation and ADR 0021 immutable received-time ingestion/migration are implemented. Two independent reviews passed; Ruff, mypy, lock, diff and isolated PostgreSQL 16 focused validation (`65 passed`) are green. Code checkpoint: `8beeb062069ddc5dd6104e0b80178fcb90da9e3b`. T031 remains `TODO`; Wave 12 remains `IN_PROGRESS`; Integration, next Wave, DG-05 and DG-08 were not started.
- T031 implementation: `REVIEW_PASSED`. Auditable import-source, Agent-conversation and bounded orphan-temp cleanup is implemented with ADR 0027 lock-ordered protection rechecks, durable `RETENTION_CLEANUP` task/outbox creation, dry-run reporting, two-phase filesystem tombstone recovery and fail-closed partial-failure handling. Independent Review passed after storage/split-brain regressions were remediated. Ruff, mypy, lock, diff and isolated PostgreSQL 16 focused validation (`40 passed`, including Alembic `upgrade head`/`check`) are green. Code checkpoint: `6545fdaf6ad4029c044c2338cc2fb36b7e385b03`. Wave 12 remains `IN_PROGRESS`; Integration, next Wave, DG-05 and DG-08 were not started.
- Wave 12 Integration: `PASS`; cross-module focused pytest `66 passed`, full pytest `234 passed, 1 skipped`, Ruff, mypy, lock, diff and PostgreSQL 16/Alembic validation all pass. No integration fix was required. T028, next Wave, DG-05 and DG-08 were not started. See `docs/implementation/reports/WAVE-12.md`.
- Wave 13 readiness: `READY`. T028 dependencies T004/T010/T014/T020/T022/T026/T027 are complete, and ADR 0012/0013/0019/0021 provide the required conversation, retention, authorization, and weekly-report read contracts. DG-05/DG-08 remain out of scope. Wave 13 is `IN_PROGRESS`; only T028 is authorized in this work unit.
- T028 implementation: `REVIEW_PASSED`. Agent conversations/API, owner-scoped history, frozen retention metadata, closed read-only domain-tool registry and provenance metadata are implemented. Ruff, mypy, `uv lock --check`, `git diff --check` and isolated PostgreSQL 16 focused tests (`2 passed`) are green. Full-suite extra attempt reached `61 passed, 1 skipped` before an existing Redis connection wait was interrupted; no T028 failure was observed. See `docs/implementation/reports/T028.md` and `docs/implementation/reports/WAVE-13-PARTIAL.md`. Code checkpoint: `21cb6a9e541dfed311b33f2355c6e8358ba6dda3`.
- Wave 13 Integration: `PASS`; T028 Agent conversation/tool registry 与 Dashboard、Risk、Todo、Weekly Report 的授权和 scope 路径联合验证 `27 passed`，完整 PostgreSQL 16 + Redis 7 pytest `236 passed, 1 skipped`。Ruff、mypy、`uv lock --check`、`git diff --check` 及 PostgreSQL 16 空库 Alembic `upgrade head`/`check` 均通过；无 integration fix。T029 未执行，未启动下一 Wave，DG-05/DG-08 未处理。详见 `docs/implementation/reports/WAVE-13.md`。
- Wave 14 readiness: `READY`。T029 dependencies T004/T007/T008/T010/T014/T028 均已完成，ADR 0019/0020 提供 SSE/Worker 总体边界；Wave 14 已标记为 `IN_PROGRESS`，仅授权 T029。
- T029 execution stop: `DESIGN_GAP`。ADR 0020 要求 Agent invocation 使用 durable task/outbox，但没有定义新增的 Agent execution task kind、稳定 idempotency/retry/timeout definition 或 execution configuration identifier 的权威持久化来源；现有 `DurableTaskKind`/PostgreSQL enum/registry 也没有该 kind，而 ADR 0018 禁止未登记自由 kind。ADR 0020 还未定义不可信 Provider 的 intent/tool-call/arguments/text-delta/preview proposal protocol 及 malformed-output 到 SSE error 的映射。选择任一结构都会自行扩张安全/契约边界，故未实施、未审查、未执行 PostgreSQL 16/SSE validation，未创建 code checkpoint。T030、Wave 14 Integration、下一 Wave、DG-05/DG-08 均未处理。详见 `docs/implementation/reports/T029.md` 与 `docs/implementation/reports/WAVE-14-PARTIAL.md`。
- T029 DESIGN_GAP resolution: ADR 0028 approves `AGENT_EXECUTION`, a PostgreSQL immutable execution-configuration snapshot and the closed `AGENT_PROVIDER_EXECUTION_V1` request/response protocol. It fixes Provider output validation, retry/timeout/cancellation, and the PostgreSQL-backed SSE/metadata-only boundary. T029 is `READY` only; no T029 implementation/review/validation, T030, Wave 14 Integration, next-Wave, DG-05 or DG-08 work was started. Design checkpoint: `51074211d3fd388514c4c8405b32295a9dd214cc`.
- T029 remediation: `DESIGN_DEVIATION`. Production Celery registration requires shared worker composition (session factory, Provider adapter and handler registry) in T040's exclusive FastAPI/Celery bootstrap write-set. Per the task stop condition, remediation stopped before further implementation, Independent Review or real worker/SSE acceptance; this is not merely `ENVIRONMENT_BLOCKED`. Existing candidate changes remain uncheckpointed. T030, Integration, next Wave, DG-05 and DG-08 untouched.
- T029 DESIGN_DEVIATION resolution: ADR 0028's approved composition-ownership addendum makes the boundary executable. T029 must deliver an explicit dependency-injected module-local `AGENT_EXECUTION` handler mapping and prove it with an isolated Celery app, T008 `register_executor`, a real test worker and fake Provider; it must not edit or register the shared production FastAPI/Celery composition root. T040 alone constructs production session/Provider/tool-registry dependencies, merges module handler mappings and registers the shared Celery executor exactly once. T029 is restored to `READY` without resuming implementation. Wave 14 remains `IN_PROGRESS`; T030, Integration, next Wave, DG-05 and DG-08 remain untouched.
- T029 composition-ownership design/metadata checkpoint: `db3d4889353d71452f1d5e6affaed84930bc8415`.
- T029 implementation: `REVIEW_PASSED`. Closed raw Provider/tool/preview validation, fail-closed durable execution, PostgreSQL ordered SSE resume, cancellation/retry/attempt-wide heartbeat/backpressure and module-local isolated real-worker acceptance are complete. Independent Review passed after two remediation rounds. Ruff, mypy, lock, diff, Alembic and PostgreSQL 16 + Redis 7 + real Celery/SSE focused validation (`27 passed`) are green. Code checkpoint: `b57e831e68e47cdfba43f78285d10481c15000e1`. T040 shared production composition was not modified; T030, Wave 14 Integration, next Wave, DG-05 and DG-08 remain untouched.
- Wave 14 Integration: `PASS`. Full PostgreSQL 16 + Redis 7 validation completed with `243 passed, 1 skipped`; Ruff, mypy, `uv lock --check`, `git diff --check` and Alembic head `20260812_0008` all pass. Integration-only fixes updated T029 schema metadata expectations and made the weekly-report reconciliation fixture wall-clock independent. T030 remains unstarted, the next Wave remains unstarted, and DG-05/DG-08 remain untouched. See `docs/implementation/reports/WAVE-14.md`. Final checkpoint is recorded after this entry.
- Wave 15 / T030 readiness: `DESIGN_GAP`. The approved Agent `REPORT` canonical/command contract has no `categoryId` or approved category mapping, while the formal `Risk` schema and T022 `RiskCreate` require a valid category. The operation therefore cannot be expressed through the existing domain services without inventing a default, inference rule or contract extension. Per T030's stop condition, Wave 15 was not marked `IN_PROGRESS`; implementation, Independent Review, validation and code checkpoint were not started. T040, Integration, next Wave, DG-05 and DG-08 remain untouched. See `docs/implementation/reports/T030.md` and `docs/implementation/reports/WAVE-15-PARTIAL.md`.
- T030 DESIGN_GAP resolution: ADR 0029 approves PostgreSQL active `RiskCategory` as the sole `REPORT.categoryId` authority, reuse of `RISK_CATEGORY_OPTIONS_V1` with one opaque Provider choice, server-side mapping plus canonical category revision binding, and fail-closed locked confirmation revalidation. T030 is restored to `READY`; Wave 15 remains `NOT_STARTED`. No T030 implementation/review/validation, T040, Integration, DG-05 or DG-08 work was started. Design checkpoint: `e57646e3f05c87b21bab632601ba64f1dbff860a`.
- Wave 15 Integration: `PASS`. T030 category-bound one-use REST confirmation 与 Risk / Todo / Audit / Dashboard / Weekly Report / Timeline / Agent execution / SSE 跨模块联合验证通过。cross-module focused pytest `57 passed, 1 skipped`；full pytest（PostgreSQL 16 + Redis 7）`247 passed, 1 skipped`；Ruff、mypy（183 source files）、`uv lock --check`、`git diff --check` 全部通过；空库 Alembic `upgrade head` 至 `20260812_0008` 且 `alembic check` 无新 migration；Redis 7 + Celery + Agent confirmation acceptance `10 passed`。无 integration fix。T040、下一 Wave、DG-05 和 DG-08 均未处理。详见 `docs/implementation/reports/WAVE-15.md`。Final checkpoint 记录于本条目之后。
- Wave 16 / T040 readiness: `READY`。T040 dependencies T008–T031 已全部 `REVIEW_PASSED`；所有 module-local router 与 handler entrypoint 均存在，无 blocking `DESIGN_GAP`。Wave 16 标记为 `IN_PROGRESS`，仅授权 T040。
- T040 implementation: `REVIEW_PASSED`。单一 owner 的 FastAPI/Celery composition root（`composition.py`）完成全部 17 个 router 与 19 个 domain service 的依赖组装；`main.py` 组装完整 production app（lifespan 内构造全部 service，startup 不建表，shutdown 释放 engine）；`worker.py` 构造 production session factory、受限 `AgentProviderAdapter`、T028 tool registry，合并 8 个 module-local handler mapping，并对 shared `celery_app` 恰好一次调用 T008 `register_executor`（模块级 `_registered` 守卫）。`AgentProviderAdapter` 从 immutable `AgentExecutionConfig` snapshot 解密 API key 并经 `OutboundEndpointGuard` 校验后调用 OpenAI-compatible endpoint，仅返回 raw `ProviderTransportResponse`；超时/连接失败映射为 retryable，SSRF/DNS 违规与解密失败作为永久配置错误。新增 `python-multipart` 依赖以 compose imports upload router。Independent Review 通过。Ruff、mypy（186 source files）、focused pytest（`5 passed`）、full pytest（`167 passed, 86 skipped`）、`uv lock --check`、`git diff --check` 均通过。PostgreSQL 16 + Redis 7 + real Celery worker integration validation 因本环境缺少 `TEST_DATABASE_URL`/PostgreSQL/Redis 而 unavailable/skipped，已记录。Code checkpoint：`2a99da0`。Wave 16 保持 `IN_PROGRESS`；Integration、下一 Wave、DG-05 和 DG-08 均未启动或处理。详见 `docs/implementation/reports/T040.md` 与 `docs/implementation/reports/WAVE-16-PARTIAL.md`。
- Wave 16 Integration：`PASS`。本轮首要建立可用 integration 环境：经 `infra/docker-compose.yml` 启动 `postgres:16-alpine`（PostgreSQL 16.14），独立启动 `redis:7-alpine`（Redis 7.4.10），并使用 real Celery `solo` worker 与 production FastAPI composition；环境状态由上一轮的 `ENVIRONMENT_BLOCKED` 转为 `READY`，未修改 T035 写集范围内的 `infra/docker-compose.yml`。验证矩阵 13 项全部通过：(1) production app boot/lifespan/shutdown 真实 PostgreSQL 16，启动期间 `information_schema.tables` 计数不变（不建表）；(2) 17 routers + 依赖组装，85 paths、代表性 path 全在册且 (path, method) 唯一；(3) `merge_worker_handlers` 覆盖全部 8 个 `DurableTaskKind`，`register_production_worker` 对 shared `celery_app` 恰好一次 `register_executor`；(4) PostgreSQL-backed services caller-owned `transaction()` / `FOR UPDATE NOWAIT` / 同事务 audit 回滚在真实 PostgreSQL 16 通过；(5) Redis 7 → real Celery `solo` worker 经 `risk_platform.reliability.execute` 完成 fenced claim → handler → DB 状态转移，T029 real-worker acceptance `10 passed`；(6) `build_provider` 受限 `AgentProviderAdapter` + `build_tool_registry` T028 closed tool registry 组装正确；(7) 无 runtime table creation、无 NestJS/Prisma runtime dependency、无双写。Ruff、mypy（186 source files）、full pytest（PostgreSQL 16 + Redis 7）`252 passed, 1 skipped`（上一轮 `167 passed, 86 skipped`，86 个 skipped 已转为真实执行并通过）、`uv lock --check`、`git diff --check` 全部通过；空库 Alembic `upgrade head` 至 `20260812_0008` 且 `alembic check` 无新 migration。无 integration fix；未修改 production 代码、测试基线或 migration。T032、下一 Wave、DG-05 和 DG-08 均未启动或处理。详见 `docs/implementation/reports/WAVE-16.md`。Final checkpoint 记录于本条目之后的提交。
- Wave 17 / T032 readiness: `READY`。T032 dependency T040 已 `REVIEW_PASSED`；T032 引用的 ADR 0001/0010/0011/0016/0019 均已批准，Design §§5/11 确认切换后以 FastAPI OpenAPI 为唯一契约来源并生成前端类型。影响 T032 的 DG-09 已由 ADR 0021 解决；DG-05/DG-08 明确 out of scope。无 blocking `DESIGN_GAP`。Wave 17 标记为 `IN_PROGRESS`，仅授权 T032。
- T032 execution stop: `DESIGN_GAP`。已按 Lean Execution Mode 完成契约权威切换的 candidate 基础设施：确定性 OpenAPI 导出（`openapi_export.py` + console script `risk-platform-openapi` + 9 tests）、冻结 `packages/contracts/openapi/openapi.json`（85 paths/220 schemas，重导 zero diff）、复现 TS 生成（`gen-types.mjs` + `openapi-typescript@^7.13.0`，`openapi.ts` 7785 行，重生成 zero diff）、root `contracts:export|gen|sync|check` 命令，以及 OpenAPI compatibility 检查（`openapi-compat.mjs`，TS compiler API 静态扫描 `apps/web/src/api/*.ts`，覆盖 path/method、error envelope、enum、schema/nullability）。candidate 局部 validation 全绿：Ruff、mypy（188 source files）、`uv lock --check`、focused pytest（`9 passed`）、`@risk-platform/contracts`/`@risk-platform/web` typecheck、`git diff --check`、导出/生成确定性。但 compatibility 检查暴露 7 个 **breaking** 差异：前端实际消费且 NestJS reference 提供的 `/api` 端点在 FastAPI OpenAPI authority 中缺失——`GET /api/admin/projects/options`（`apps/api/src/admin/admin.controller.ts:232`）与 6 个 mailbox sync-results 浏览端点（`GET /api/mailbox/sync-summary`、`/review-options`、`/messages`、`/messages/{id}`、`POST /messages/{id}/retry`、`/sync-batches`，`apps/api/src/mailbox/mail-sync-results.controller.ts`）；它们被已注册路由 `MailSyncResultsView.vue` 驱动，属活跃前端功能，而 Task Graph 中无任何 Task 拥有其迁移（T023–T026 仅含配置/连接测试/UID 同步/解析/候选复核）。这是既存迁移覆盖缺口而非 T032 regression；冻结缺失活跃前端消费端点的 OpenAPI 为唯一权威会违反 ADR 0001/0011 的 backward-compat 基准。触发 T032 stop condition「A breaking diff lacks approval」。Independent Review 未进入，未创建 code checkpoint，candidate 工作留在 working tree 未提交。Wave 17 保持 `IN_PROGRESS`；T033/T034、Wave 17 Integration、下一 Wave、DG-05/DG-08 均未启动或处理。详见 `docs/implementation/reports/T032.md`。
- T032 DESIGN_GAP resolution（metadata checkpoint，未实现 production code）：7 个缺失 `/api` 操作的 ownership 已正式映射到新增 remediation Task。采用新增 Task 而非扩展已完成 Task（已完成 Task 写集已冻结于 `REVIEW_PASSED`）。**T043**（`READY`，deps T006/T010/T023/T024/T025/T026）owns mailbox sync-results browse/retry surface：`GET /mailbox/sync-summary`（`mailbox.sync_self` + `RISK_ADMIN`）、`/review-options`、`/messages`、`/messages/{id}`、`POST /messages/{id}/retry`（ADR 0022 handoff/terminal-state）、`/sync-batches` 及同面 `/sync-batches/{id}`，全部 scope 至自身 `mailboxConfigId`，复用既有 `@risk-platform/contracts` 类型。**T044**（`READY`，deps T005/T010）owns `GET /admin/projects/options` → `ProjectOption[]`（`admin.scope.manage`），对 T012 既有 options 模块 additive-only。DAG 新增 `T043-->T032&T034`、`T044-->T032`；Wave 表插入新 Wave 17（T043/T044 disjoint write-sets），T032 后移至 Wave 18，其后各 Wave 顺延 +1。T032 状态 `DESIGN_GAP`→`BLOCKED`，在 T043/T044 `REVIEW_PASSED` 前不可 freeze；candidate 基础设施仍留 working tree 未提交。T034 deps 增 T043。无需新 ADR；T033/T034 实现、Wave 17 Integration、下一 Wave、DG-05/DG-08 均未启动或处理。详见 `docs/implementation/reports/T032.md` 与 `docs/implementation/TASK_GRAPH.md`。
- T043 implementation: `REVIEW_PASSED`。新增 disjoint 子包 `mailbox/sync_results/`，迁移 NestJS `mail-sync-results` 的 7 个浏览/重试端点（`sync-summary`/`review-options`/`messages`/`messages/{id}`/`messages/{id}/retry`/`sync-batches`/`sync-batches/{id}`）至 FastAPI，复用 `@risk-platform/contracts` 类型。权限 `mailbox.sync_self` + `RISK_ADMIN`（全操作，对齐 NestJS `ensureRiskAdmin`），自身 `mailboxConfigId` scope，`ApiResponse`/`PaginatedResponse`/`ApiError` 信封一致。retry 严格 ADR 0022：仅 FAILED、拒并发 QUEUED/RUNNING、`(uidValidity,imapUid)` source-identity 定位 handoff、按失败 stage 选 `ATTACHMENT_PARSE`/`MAIL_AI_REVIEW_PUBLISH`、reset stage + clear 诊断、`enqueue_task` fresh-key（ADR 0018）、创建 RETRY 批次、ADR 0017 metadata-only audit。composition/main additive 注册（不改 T040 冻结逻辑）。Independent Review 逐行比对 NestJS，修正一处 retryCount 递增偏差（移至 pipeline 职责），确认 ADR 0017/0022 批准偏差。Ruff、mypy（20 source files）、focused pytest（隔离 PostgreSQL 16 + per-test schema + Alembic `upgrade head`，`15 passed`）、mailbox 回归 `46 passed`、`uv lock --check`、`git diff --check` 均通过。Code checkpoint：`eb7c9243b170feda08776912c9dcde932ca90114`。Wave 17 保持 `IN_PROGRESS`；T044、Wave 17 Integration、T032/T033/T034、下一 Wave、DG-05/DG-08 均未启动或处理。T032 candidate 文件保持 uncommitted、未触碰。详见 `docs/implementation/reports/T043.md`。
- T044 implementation: `REVIEW_PASSED`。迁移 NestJS `admin.controller.ts` `@Get("projects/options")` 至 FastAPI `GET /api/admin/projects/options` → `ProjectOption[]`，权限 `admin.scope.manage`。对 T012 既有 `admin/options` 模块 additive-only：`schemas.py` 追加 `ProjectOptionResponse`，`service.py` 追加 `list_projects()`（`status != ARCHIVED`、`order_by name asc`、`limit 500`、LEFT JOIN nullable `departmentName`），`api.py` 追加路由；`list_departments`/`DepartmentResponse` 与 T012 冻结面未修改（diff 逐行确认）。复用 `@risk-platform/contracts` `ProjectOption` 类型，`ApiResponse`/`ApiError(403,"FORBIDDEN")` 信封一致。composition/main 无变更（`options_router`/`admin_options_service` 已于 T012 注册）。Independent Review 逐行比对 NestJS，无 `DESIGN_DEVIATION`。Ruff、mypy（5 source files）、focused pytest（隔离 PostgreSQL 16 + per-test schema `t044_<uuid>` + Alembic `upgrade head`，`2 passed`）、admin 回归（`12 passed`）、composition + http_core（`24 passed`）、`uv lock --check`、`git diff --check` 均通过。Code checkpoint：`1dc067a44a3e5bcf5e4ecc5305e2060f713ecbd5`。Wave 17 保持 `IN_PROGRESS`；Wave 17 Integration、T032/T033/T034、下一 Wave、DG-05/DG-08 均未启动或处理。T032 candidate 文件保持 uncommitted、未触碰。详见 `docs/implementation/reports/T044.md`。
- Wave 17 Integration：`PASS`。T043/T044 均已 `REVIEW_PASSED`，本 Wave 联合验证二者已消除 T032 OpenAPI compatibility 缺口。验证矩阵：(1) live production app（`main:app`）OpenAPI surface 共 93 paths（Wave 16 为 85，+8 来自 T043/T044），7 个前端活跃消费的 `/api` 端点全部在册且 (path, method) 唯一——`GET /api/admin/projects/options`（T044）与 `GET /api/mailbox/sync-summary`、`/review-options`、`/messages`、`/messages/{message_id}`、`POST /messages/{message_id}/retry`、`/sync-batches`、`/sync-batches/{batch_id}`（T043）；路径参数名 `{message_id}`/`{batch_id}`（描述式，与既有 `{batch_id}`/`{candidate_id}` 一致）非契约偏差——前端 `apps/web/src/api/mailbox.ts` 用模板字符串插值（`` `/mailbox/messages/${id}` ``），实际 HTTP 路径匹配，且 T032 candidate compatibility 检查以结构化路径（`{}` 归约）比对，参数名差异不产生 breaking diff。(2) T043 契约：`mailbox.sync_self` + `RISK_ADMIN`（`403 FORBIDDEN`）、自身 `mailboxConfigId` scope（retry `with_for_update` 行锁）、`ApiResponse` 信封、`items`/`page`/`pageSize`/`total` 分页、ADR 0022 retry（仅 FAILED、拒并发、`(uidValidity,imapUid)` source-identity handoff、按 stage 选 `ATTACHMENT_PARSE`/`MAIL_AI_REVIEW_PUBLISH`、fresh-key `enqueue_task`、RETRY 批次、ADR 0017 metadata-only audit、retryCount 由 pipeline 递增）。(3) T044 契约：`admin.scope.manage`、`list_projects`（非 ARCHIVED、name asc、limit 500、LEFT JOIN nullable departmentName）、`ProjectOptionResponse` 与 `@risk-platform/contracts` `ProjectOption` 逐字段一致、对 T012 additive-only。(4) OpenAPI surface 已含这些端点；T032 candidate 冻结 `openapi.json`/`openapi.ts`/compat 脚本保持 uncommitted、未重新生成、未恢复、未修改。Ruff、mypy（194 source files，含 candidate `openapi_export.py`）、full pytest（PostgreSQL 16 + Redis 7，`278 passed, 1 skipped`，较 Wave 16 `252` 增 T043/T044/candidate 测试）、`uv lock --check`（58 packages）、`git diff --check`（candidate tracked-file diff 亦 clean）、空库 Alembic `upgrade head` 至 `20260812_0008` 且 `alembic check` 无新 migration 均通过。无 integration fix；未修改 production 代码、测试基线或 migration。T032 candidate 文件保持 uncommitted、未触碰。T032 blocker 状态同步为 `READY`（仅 metadata，未执行 T032）。T033/T034、Wave 18、下一 Wave、DG-05/DG-08 均未启动或处理。详见 `docs/implementation/reports/WAVE-17.md`。
- T032 readiness sync（仅 metadata，未执行 T032）：T032 的 blocking 前置（T043/T044 `REVIEW_PASSED` + Wave 17 Integration `PASS`）已满足，T032 依赖 T040/T043/T044 均已完成。T032 状态由 `BLOCKED` 同步为 `READY`，可在 Wave 18 重新评估/续作；T032 candidate 基础设施（确定性 OpenAPI 导出、冻结 `openapi.json`、TS 生成、compatibility 检查、root `contracts:*` 命令）仍留 working tree 未提交、未触碰。本次不执行 T032、不启动 Wave 18；DG-05/DG-08 保持 out of scope。
- Wave 18 / T032 readiness：`READY`。T032 blocking 前置（T040/T043/T044 `REVIEW_PASSED` + Wave 17 Integration `PASS`）已满足；无新 blocker。Wave 18 标记为 `IN_PROGRESS`，仅授权 T032。
- T032 implementation：`REVIEW_PASSED`。重新冻结 FastAPI OpenAPI 为唯一契约权威并复现生成前端类型。以当前 HEAD（含 T043/T044 production 代码）的 production `app`（T040 composition）重新导出 `packages/contracts/openapi/openapi.json`（85→**93 paths**、220→**243 schemas**，+8 来自 T043/T044），替换 candidate 原 85-paths 冻结产物；7 个此前 blocking 的前端活跃消费 `/api` 端点 + 1 同面 detail 全部在册且 (path, method) 唯一。重新生成 `openapi.ts`（8568 行）。compatibility check `PASS`，7 个 breaking diff 全部消失；path/method coverage 85/85、error envelope 74 组件、enum `ROLE_CODES`/`DATA_SCOPE_TYPES` superset、schema/nullability（Decimal→string、number、`SessionResponse.expiresAt`→string，修正 candidate 原 dead `LoginResponse` spot-check）全部通过。export+gen 确定性 3 轮 zero diff。FastAPI 为唯一 runtime OpenAPI authority，无 NestJS/Prisma、无双写。Independent Review `REVIEW_PASSED`，无 blocking finding（minor：`contracts:check` 的 `git diff --exit-code` gate 已由 code checkpoint 将 artifacts 纳入 tracked 而可执行，post-commit `pnpm contracts:check` exit 0）。Ruff、mypy（185 source files）、focused pytest（`9 passed`）、`@risk-platform/contracts`/`@risk-platform/web` typecheck、`uv lock --check`、`git diff --check` 全部通过。Code checkpoint：`b9a172c2aad3b68239c32dc4ac6a4c462bd85c46`。Wave 18 保持 `IN_PROGRESS`；Wave 18 Integration、T033/T034、下一 Wave、DG-05/DG-08 均未启动或处理。详见 `docs/implementation/reports/T032.md` 与 `docs/implementation/reports/WAVE-18-PARTIAL.md`。
- Wave 18 Integration：`PASS`。T032 `REVIEW_PASSED` + code checkpoint 后执行项目级联合验证。验证矩阵 12 项全部通过：(1) production FastAPI OpenAPI 与 tracked `openapi.json` 逐字节 IDENTICAL（93 paths / 243 schemas）；(2) `openapi.ts` 由 tracked OpenAPI reproducibly 生成，zero diff；(3) `pnpm contracts:check` 在 clean working tree 下 exit 0；(4) frontend 全部 85 个 live API usage 均被 OpenAPI authority 覆盖（104 operations）；(5) path/method coverage 85/85、error envelope 74 组件、enum `ROLE_CODES`/`DATA_SCOPE_TYPES` superset、schema/nullability（Decimal→string、number、datetime→string）compatibility 全部 `PASS`，7 个 breaking diff 全部消失；(6) `@risk-platform/contracts`/`@risk-platform/web` typecheck exit 0；(7) Ruff `All checks passed!`、mypy `Success: no issues found in 194 source files`；(8) full pytest（PostgreSQL 16 + Redis 7）`278 passed, 1 skipped`；(9) Alembic head `20260812_0008`，空库 `upgrade head` + `alembic check` 无新 migration，per-test schema fixture 7 passed；(10) `uv lock --check` `Resolved 58 packages`；(11) `git diff --check` clean；(12) 连续 3 轮 export+gen zero diff，tracked `openapi.json` 与 fresh export IDENTICAL。无 integration fix——T032 contract-freeze / generation 未暴露 integration failure；唯一处理项为 integration 环境 bootstrap（PostgreSQL 持久卷密码重置、`TEST_DATABASE_URL` 去除 Prisma 风格 `?schema=` 查询参数以符合 psycopg 连接串），均为环境配置而非契约/生成问题，未修改 production 代码、测试基线、migration 或 contract artifact。未修改 `infra/docker-compose.yml`（T035 写集）。T033/T034 仅同步 readiness，未执行；下一 Wave 未启动；DG-05/DG-08 未处理。详见 `docs/implementation/reports/WAVE-18.md`。Final checkpoint 记录于本条目之后的提交。
- Wave 19 readiness sync（仅 metadata，未执行 T033/T034、未启动 Wave 19）：T033（deps T016、T032）与 T034（deps T027、T030、T032、T043）的 blocking 前置（T032 `REVIEW_PASSED` + Wave 18 Integration `PASS`）已满足，T016/T027/T030/T043 均 `REVIEW_PASSED`。T033、T034 状态同步为 `READY`，可在 Wave 19 评估/执行。本次不执行 T033/T034、不启动 Wave 19；DG-05/DG-08 保持 out of scope。
- Wave 19 / T033 readiness：`DESIGN_GAP`（execution stop）。Lean Execution Mode contract 消费检查发现：T033 必须消费的 `GET /api/admin/overview` 生成类型在 frozen OpenAPI authority（T032）中不可用——`AdminOverview` 及全部子模型（`HealthItem`/`AttentionItem`/`RecentAuditItem`/`UnavailableSection`/`OverviewLink`）每个字段在 `openapi.ts` 中均解析为 `unknown`，ADR 0023 item 级字段对前端不可见，触发 T033 stop condition「A required view field is absent from the approved contract」。根因（已独立验证）：`admin/overview/schemas.py` 的 `_Contract` 通配 `field_serializer("*", when_used="json", check_fields=False)` 破坏 **serialization-mode** JSON schema 生成（`model_json_schema(mode="serialization")` 损坏、`mode="validation"` 正确），而 FastAPI 为 response model 用 serialization mode 生成 OpenAPI，故所有 `_Contract` 子类 schema 被破坏。该缺陷 **systemic**：`_Contract` 被 `admin/overview`（T033）、`weekly_reports` 与 `agent`（均 T034 surface）共享，frozen authority 中 `WeeklyReportResponse`/`AgentConversationResponse`/`AgentConfirmationResponse` 等字段同样损坏，故同时阻塞 T033 与 T034。根因修复位于 T016 frozen backend write-set（且需同步 weekly_reports/agent frozen write-set 与 re-freeze T032 OpenAPI artifact），超出 T033 frontend-only write-set 与「backend changes out-of-scope」边界；按 ORCHESTRATOR 协议与 T032 precedent（frozen write-set 缺陷映射到新 remediation Task/ADR，而非下游 Task 越界修改），本轮不实施、不创建 code checkpoint、不修改任何 frozen write-set。Wave 19 **未标记 `IN_PROGRESS`**（readiness blocker）；T034、Wave 19 Integration、下一 Wave、DG-05/DG-08 均未启动或处理。详见 `docs/implementation/reports/T033.md`。
- Wave 19 / T045 readiness：`READY`。T045 blocking 前置（T016/T027/T029/T032 均 `REVIEW_PASSED`）已满足；无新 blocker。Wave 19 标记为 `IN_PROGRESS`，仅授权 T045（contract-fidelity remediation）。
- T045 implementation：`REVIEW_PASSED`。修复 T033 readiness 发现的 systemic contract-fidelity 缺陷：三个模块（`admin/overview`、`weekly_reports`、`agent`）共享的 `_Contract` 基类其通配 `field_serializer("*", when_used="json", check_fields=False)` 携带 `-> object` 返回注解，使 Pydantic 在 serialization-mode JSON schema 生成时把每个字段 schema 替换为 `object`，全部 `_Contract` 子类字段在 frozen OpenAPI authority（T032）与 `openapi.ts` 中退化为 `unknown`，阻塞 T033/T034。根因修复为最小实现：**仅删除三个 `_Contract` 通配 field serializer 的 `-> object` 返回注解**（+ `# type: ignore[no-untyped-def]`，mypy strict 要求返回注解而任何注解都会重新破坏 schema），序列化函数体逐字节不变。runtime JSON 完全兼容（datetime→UTC RFC 3339 毫秒+`Z`、`when_used="json"` 保留 Python `model_dump` 返回 datetime 对象、`extra="forbid"` 不变）；serialization-mode schema 恢复真实字段类型（`format: date-time`/`enum`/`$ref`/`type`），无 `unknown`/裸 `{}`。re-freeze `openapi.json`/`openapi.ts`（T032 tooling，生成产物未手工编辑，ADR 0011），API surface 不变（仍 93 paths / 243 schemas / 104 unique operations）。不合并三个 `_Contract` 副本、不触碰任何子类字段声明或非 `_Contract` 代码。扩展 `test_openapi_export.py` +5 schema-fidelity 断言。Independent Review `REVIEW_PASSED`，无 blocking finding（minor note：所选「删除返回注解」为最小实现，Task 明确授权 Implementer 选择）。Ruff、mypy（185 source files）、focused pytest（`test_openapi_export.py` `14 passed`；overview/weekly/agent `21 passed, 26 skipped`）、full backend regression（PostgreSQL 16 + Redis 7，`283 passed, 1 skipped`，较 Wave 18 `278` 增 5 个新测试）、`@risk-platform/contracts`/`@risk-platform/web` typecheck、OpenAPI compatibility check（PASS，85/85 frontend calls，无 breaking diff）、export+gen 3 轮 byte-identical 确定性、`uv lock --check`（58）、`git diff --check`、post-commit `pnpm contracts:check`（exit 0）全部通过。Code checkpoint：`ac351cb8aed4ccfd35a13fdf4becbdc2e80f22df`。Wave 19 保持 `IN_PROGRESS`；Wave 19 Integration、T033/T034 执行、下一 Wave（Wave 20 = T033/T034）、DG-05/DG-08 均未启动或处理。详见 `docs/implementation/reports/T045.md` 与 `docs/implementation/reports/WAVE-19-PARTIAL.md`。
- T033/T034 readiness sync（仅 metadata，未执行 T033/T034、未启动 Wave 19 Integration）：T045 `REVIEW_PASSED` 解除 T033/T034 的 `_Contract` fidelity block。T033（deps T016、T032、T045）与 T034（deps T027、T030、T032、T043、T045）状态由 `BLOCKED` 同步为 `READY`，可在 Wave 20 评估/执行。frozen OpenAPI authority 现表达 `AdminOverview`/`HealthItem`/`AttentionItem`/`RecentAuditItem`/`UnavailableSection`/`OverviewLink`（ADR 0023）、`WeeklyReportResponse` 等相关模型（ADR 0021）、`AgentConversationResponse`/`AgentConfirmationResponse` 等相关模型（ADR 0028/0029）的真实 item 级字段类型，T033/T034 所需字段全部在 generated `openapi.ts` 中具有真实类型（非 `unknown`）。本次不执行 T033/T034、不启动 Wave 19 Integration 或下一 Wave；DG-05/DG-08 保持 out of scope。
- Wave 19 Integration：`PASS`。T045 `REVIEW_PASSED` 后执行项目级联合验证。验证矩阵：(1) production FastAPI OpenAPI（当前 HEAD，含 T045 修复）经 `risk-platform-openapi` 导出，与 tracked `packages/contracts/openapi/openapi.json` **byte-identical**（`cmp` zero diff）。(2) API surface 不变：93 paths / 243 schemas / 104 operations。(3) serialization-mode schema fidelity 恢复：`HealthItem`/`AttentionItem`/`AdminOverview`/`WeeklyReportResponse`/`WeeklyProjectDetail`/`AgentConfirmationResponse`/`AgentConversationResponse`/`AgentMessageResponse` 等受影响 `_Contract` schema 字段恢复 `format: date-time`/`enum`/`$ref`/`type`/`anyOf`，无退化 `{"title":...}` 或裸 `{}`。(4) generated `openapi.ts` 中 T033/T034 所需字段真实类型（如 `AttentionItem.kind`=`enum`、`link`=`$ref`、`occurredAt`=`date-time string`），残留 191 处 `unknown` 为合法 `JSONValue`/opaque-dict 字段。(5) runtime JSON 序列化与 T045 前兼容（serializer 函数体逐字节不变，`when_used="json"`/`extra="forbid"` 保留；`test_contract_datetime_runtime_json_is_utc_milliseconds_with_z` 随 full pytest 通过）。(6) `pnpm contracts:check` clean-tree exit 0（sync 后 zero diff）。(7) OpenAPI compatibility check `PASS`（104 operations，85/85 frontend calls，无 breaking diff；74 error envelope 组件；enum superset；schema/nullability spot-check 通过）。(8) `@risk-platform/contracts`/`@risk-platform/web` typecheck exit 0。(9) Ruff `All checks passed!`、mypy `Success: no issues found in 185 source files`。(10) full pytest（PostgreSQL 16 + Redis 7）`283 passed, 1 skipped`（较 Wave 18 `278` 增 5 个 T045 schema-fidelity 测试；1 skip 为既有 audit-query-export DB 专项分工提示，PostgreSQL 路径由 `test_postgresql_schema.py`/`test_schema_metadata.py` 专项覆盖）。(11) PostgreSQL 16 / Alembic regression：head `20260812_0008`（单一 head），空库 `alembic upgrade head` 成功，fresh `alembic check` `No new upgrade operations detected.`；per-test schema fixture 7 passed；Redis 7 经 full pytest T029/T026 real Celery `solo` worker 路径通过。(12) 连续 3 轮 `pnpm contracts:sync` export+gen byte-identical（`openapi.json`/`openapi.ts` SHA-256 三轮不变，每轮 dirty=0）。(13) `uv lock --check` `Resolved 58 packages` exit 0。(14) `git diff --check` clean。无 integration fix；未修改任何 production 代码、测试基线、migration 或 contract artifact。T033/T034 保持 `READY`（仅同步 Wave 20 readiness，未执行）；Wave 20（T033/T034）未启动；DG-05/DG-08 保持 out of scope。详见 `docs/implementation/reports/WAVE-19.md`。
- Wave 20 / T033-T034 readiness：`READY`。T033（deps T016、T032、T045）与 T034（deps T027、T030、T032、T043、T045）blocking 前置全部 `REVIEW_PASSED` + Wave 19 Integration `PASS`，frozen OpenAPI authority 已表达 T033/T034 所需 item 级字段真实类型。Wave 20 标记为 `IN_PROGRESS`，授权 T033、T034（disjoint page/API modules）。
- T033 implementation：`REVIEW_PASSED`。admin 概览页消费 `GET /api/admin/overview` 生成契约（`AdminOverview`/`HealthItem`/`AttentionItem`/`RecentAuditItem`/`UnavailableSection`/`OverviewLink` 全部 `OpenApi.components["schemas"][...]` 别名），移除固定 health/attention/audit 业务数组，保留既有权限/路由/metrics，补齐 loading/empty/error/unavailable 语义，`navigateLink` 透传契约 path+query，`overallHealthStatus` 动态 health rollup。Independent Review `REVIEW_PASSED`。web typecheck/test（+7 admin-overview tests）/build、contracts typecheck、`contracts:check` zero diff、`git diff --check` 全部 PASS。Code checkpoint：`d8dfcec2b89db7205521b48bd3ae0cf275d1a4e3`。详见 `docs/implementation/reports/T033.md`。
- T034 implementation：`REVIEW_PASSED`。移除 `DashboardView` 本地伪造 Agent 应答与静态周报数组，改为消费 generated 契约的 weekly-reports / agent API：`weeklyReportsApi.current()/report()/detail()` 消费 `WeeklyReportResponse`/`WeeklyProjectDetail`；`agentApi` REST（help/create/continue/history/messages/confirm）+ `useAgentConversation` 接入 PostgreSQL-backed SSE（`utils/agent-sse.ts` 纯 parser/reducer）。create/continue、reconnect `after=lastEventId`、provider-failure retry、one-use preview confirmation（空 body）+ confirmation 错误标签、stream read-only、`agent.use` 权限网关、loading/empty/error/stale/disconnect/reconnect/retry/preview/confirmation 语义全部到位。无 `any`，全程生成类型消费（SSE wire payload `unknown` + 运行时守卫）。Independent Review `REVIEW_PASSED`。web typecheck/test（+23 weekly/agent tests）/build、contracts typecheck、`contracts:check` zero diff、`git diff --check` 全部 PASS。Code checkpoint：`2a232d43f39fdfdf29a4f315bbbd9ca108a5fa78`；metadata checkpoint `d90d825fd9fd44a89e69f6ae6ff44a00b539e632`。详见 `docs/implementation/reports/T034.md`。
- Wave 20 Integration：`PASS`。T033/T034 均 `REVIEW_PASSED` + checkpoint 后执行联合验证，确认前端 cutover 与 live FastAPI `/api` contract 联合一致且无回退。验证矩阵：(1) T033 admin overview UI ↔ live FastAPI `GET /admin/overview`（`admin/overview/api.py:25`）+ `GET /admin/projects/options`（T044 `admin/options/api.py:35`）契约逐对对齐；6 个生成类型别名 re-reference frozen authority；frozen schema 字段真实类型（`AttentionItem.kind`=`enum`、`link`=`$ref`、`occurredAt`=`date-time`）；loading/empty/error/unavailable、link path+query 透传、dynamic health rollup 全覆盖；7 个 vitest。(2) T034 weekly reports UI ↔ live FastAPI `GET /weekly-reports/current`/`/{week_start}`/`/{week_start}/projects/{project_id}`（`weekly_reports/api.py:28/37/47`）契约逐对对齐；5 个生成类型别名；current/report/detail + loading/empty/stale/error + detail items；8 个 vitest。(3) T034 agent UI/SSE ↔ live FastAPI `GET /agent/help`、`POST /agent/conversations`（201）、`POST /agent/conversations/{id}/messages`（202）、`GET /agent/conversations/{id}`、`GET /agent/conversations/{id}/messages`（`afterSequence`/`limit`）、`POST /agent/confirmations/{token}`（空 body，`del payload`）、`GET /agent/conversations/{id}/events`（`after: UUID|None`，SSE）契约逐对对齐；9 个生成类型别名；create/continue、SSE parser/reducer、reconnect `after=lastEventId`、provider-failure retry（5xx/0 retryable）、preview one-use confirmation、confirmation 错误标签（6 码）、stream read-only、`agent.use` gate（前端 `canUseAgent` + 后端 `require_permissions("agent.use")`）；15 个 vitest。(4) T043 mailbox surface 无回退：`mailbox.ts` 全部 sync-results 端点（`/sync-summary`/`/review-options`/`/messages`/`/messages/{id}`/`POST /messages/{id}/retry`/`/sync-batches`）与 `mailbox/sync_results/api.py` 一一对应；DashboardView mailbox 链接保留 `mailbox.sync_self`。(5) generated `openapi.ts` 为 frontend 唯一 contract source：20 处 `OpenApi.components["schemas"]` 别名，无手写重复契约、无 `any` 覆盖生成类型。(6) frontend 无 fake Agent / static weekly 数据：`DashboardView` 消费 `weeklyReportsApi.current()/detail()` + `useAgentConversation` + `agentApi.help()`；grep 无 `sendAgent`/`weeklyReports = [` 残留。(7) `pnpm contracts:check` clean-tree exit 0（sync 后 `git status --porcelain` 为空）。(8) frozen OpenAPI export/generation zero diff：当前 HEAD production `app` 导出与 tracked `openapi.json`（93 paths / 243 schemas / 104 operations）逐字节一致，`openapi.ts` reproducibly 生成 zero diff。(9) `@risk-platform/contracts` typecheck exit 0。(10) `@risk-platform/web` typecheck（`vue-tsc -b --noEmit`）exit 0。(11) web tests（`vitest run`）4 files / 33 tests PASS。(12) web production build（`vue-tsc -b && vite build`）PASS。(13) `git diff --check` clean。(14) Browser E2E：unavailable——仓库无 Playwright/Cypress harness（仅 vitest unit），按事实记录未伪报为 PASS。无 integration fix——T033/T034 frontend cutover 未暴露 integration failure，前端消费的 generated 类型与 live FastAPI export byte-identical 的 frozen authority 一致；未修改任何 production 代码、测试基线、migration、contract artifact、backend 或 frozen OpenAPI authority。backend Ruff/mypy/full pytest/`uv lock --check` not applicable（纯前端 write-set；`contracts:check` 已 boot production FastAPI `app` 证明 live backend contract 可用；backend 全量回归已于 Wave 19 同一 HEAD lineage `283 passed` 建立）。T035（Wave 21）仅同步 readiness，未执行；下一 Wave 未启动；DG-05/DG-08 保持 out of scope。详见 `docs/implementation/reports/WAVE-20.md`。Final checkpoint `1ba4f5e2d05f9131e91b4666165205dc631f8222`（metadata 记录于本条目之后的提交）。
- Wave 21 / T046 readiness：`READY`。T046 blocking 前置 T008（`REVIEW_PASSED`）、T024（`REVIEW_PASSED`）已满足；ADR 0030（DG-16 resolution，设计 checkpoint `67f1ddd01ec60e96dc7442549d6647e1fd9d6d89`）提供批准的 cadence/single-active/failure/outbox-wiring 契约。无新 blocker → Wave 21 标记 `IN_PROGRESS`，仅授权 T046（Lean Execution Mode）。
- T046 implementation：`REVIEW_PASSED`。实现 ADR 0030 生产调度器拓扑：新增 `apps/api-python/src/risk_platform/scheduler.py` 独立进程入口（advisory-lock tick 循环、per-function cadence gating、per-tick/per-function failure isolation、liveness 探针、startup retry、SIGTERM graceful shutdown）。single-active 经 PostgreSQL session-level advisory lock（`pg_try_advisory_lock` 固定单 bigint key `0x7269736B5F736368`；`False` → exit 1 fail-fast；session-level 崩溃即释放）。cadence（operational defaults，非 SLO；DG-05 out of scope）：outbox drain 5s、reconcile 30s、mailbox sync 300s，env 可调，非正 → exit 2。每 action 独立 try/except，失败不 advance `_last_run` → 重试，不阻塞其他动作/循环。outbox at-least-once：`publish_outbox` 先 `send_task` 后置 `publishedAt`，事务回滚保持未发布。请求路径不 `send_task`，scheduler 是唯一 publisher（无 DB/Celery dual-write，PostgreSQL 唯一 authority）。liveness：`/healthz` 仅当 lock 持有 + 最近 tick 在 ≤ 2× outbox drain 窗口内时 200，否则 503；stdlib `ThreadingHTTPServer` 无额外依赖。SIGTERM 完成当前 tick 后释放 lock + 关连接 exit 0；config error exit 2；DB 有界重试超限 exit 1。**只读复用** `risk_platform.db`/`celery_app`/`publish_outbox`/`reconcile`/`schedule_enabled_syncs`，**未编辑** `composition.py`/`celery_app.py`/`worker.py`/`main.py`/driven 函数；不注册 executor、不配置 beat、不构造 domain service、不写业务 audit（ADR 0017），日志不含 secret/mail content/payload（ADR 0014/0007）。新增 `tests/reliability/test_scheduler.py`（21 tests：15 unit 无 DB + 6 PostgreSQL 16/Redis 7，per-test Alembic schema `t046_<uuid>` + `upgrade head`/`check`；含 single-active 拒绝+重获取、outbox send 失败重试、reconcile/mailbox action、run-loop smoke、outbox 发布到真实 Redis 7 broker db 15）。Independent Review `REVIEW_PASSED`，无 blocking finding（3 minor non-blocking note）。Ruff、mypy（187 source files）、focused pytest（`21 passed`）、full pytest（PostgreSQL 16 + Redis 7，`304 passed, 1 skipped`，较 Wave 19 `283` 增 21）、`uv lock --check`（58）、`git diff --check` 全部通过。Code checkpoint：`21c6d3d0b1be32034fb55bf17e02501506d62ccc`。Wave 21 保持 `IN_PROGRESS`；Wave 21 Integration、Wave 22/T035、下一 Wave、DG-05/DG-08 均未启动或处理；`infra/docker-compose.yml` 与全部 frozen write-set 未修改。详见 `docs/implementation/reports/T046.md` 与 `docs/implementation/reports/WAVE-21-PARTIAL.md`。
- T035 readiness sync（仅 metadata，未执行 T035）：T046 `REVIEW_PASSED` + code checkpoint 解除 T035 的 blocked-on-T046。T035（deps T031-T034/T040/T046）保持 `READY`，可在 Wave 22 重新评估/执行；T035 拥有 Compose `scheduler` service wiring（image/command/env/healthcheck/restart/depends_on）+ env examples + deployment docs，不实现 entrypoint。本次不执行 T035、不修改 `infra/docker-compose.yml`、不启动 Wave 22。
- Wave 21 Integration：`PASS`。T046 `REVIEW_PASSED` + code checkpoint（`21c6d3d`）+ metadata checkpoint（`4ef3f18`）后执行项目级联合验证，重点验证 T046 scheduler 与既有 durable-task/mailbox/reliability 体系在真实 PostgreSQL 16 + Redis 7 + real Celery worker 下的联合行为。15 项契约联合一致验证全部通过：(1) 独立 scheduler entrypoint（`risk_platform.scheduler:main`）在真实 PostgreSQL 16.14 + Redis 7.4.10 启动；(2) PostgreSQL session-level advisory lock single-active（固定 key `0x7269736B5F736368`）——real-process 进程 B fail-fast `exit 1`、进程 A SIGTERM `exit 0` 释放 lock、进程 C 重获取；(3) cadence 5s/30s/300s + env override + 非正 `exit 2`；(4) 三类 action 独立调度 + 单项 failure 不阻塞（失败不 advance `_last_run` → 重试）；(5) `publish_outbox` committed → broker、send 成功后才置 `publishedAt`、send 失败保持未发布、无 request-path dual-write（scheduler 是唯一 publisher）；(6) `reconcile` 恢复 broker-loss/missed-delivery（既有 reliability 回归覆盖）；(7) `schedule_enabled_syncs` 产生 scheduled mailbox work + idempotency（fresh-key `enqueue_task`）；(8) real Celery `solo` worker 端到端消费 scheduler `publish_outbox` 发布的 durable task → fenced claim → handler → `finish_task` → `DurableTask.status=SUCCEEDED` + `TaskOutbox.publishedAt` 已置（ad-hoc `INTEGRATION_OK`）；(9) SIGTERM graceful shutdown（当前 tick 完成 + 连接关闭 + lock 释放，`exit 0`）；(10) liveness `/healthz` lock 持有 + recent tick 在 2× outbox drain 窗口内 → 200，stale/lock-lost → 503；(11) scheduler 不写业务 audit（ADR 0017）；(12) 日志不含 secret/mail content/payload（ADR 0014/0007）；(13) 无 Celery Beat wiring（`scheduler.py`/`celery_app.py` 均无 `beat_schedule`）；(14) frozen `main.py`/`worker.py`/`composition.py`/`celery_app.py` 自 `21c6d3d` 未修改；(15) `infra/docker-compose.yml`（T035 write-set）未修改。real-process single-active smoke `SMOKE_OK`。Validation：Ruff `All checks passed!`、mypy `Success: no issues found in 187 source files`、focused scheduler `21 passed`、reliability+mailbox `74 passed`、full pytest（PostgreSQL 16 + Redis 7）`304 passed, 1 skipped`、`uv lock --check` `Resolved 58 packages`、`git diff --check` clean、Alembic head `20260812_0008` per-test schema `upgrade head`/`check` 无新 migration。无 integration fix——T046 write-set 未暴露任何 integration failure；未修改任何 production 代码、测试基线、migration、contract artifact、frozen write-set 或 `infra/docker-compose.yml`。T035/Wave 22 仅同步 readiness，未执行；DG-05/DG-08 未处理。详见 `docs/implementation/reports/WAVE-21.md`。Final checkpoint `3722a6b94fa4692d1452c6c34eb072be0fa4382f`（metadata 记录于本条目之后的提交）。
- Wave 22 / T035 readiness：`READY`。T035 blocking 前置 T031/T032/T033/T034/T040/T046 均 `REVIEW_PASSED` + Wave 21 Integration `PASS`；ADR 0030（DG-16 resolution）已批准 scheduler 拓扑且 T046 已交付 entrypoint `risk_platform.scheduler:main`。Wave 22 标记为 `IN_PROGRESS`，仅授权 T035（Lean Execution Mode）。
- T035 implementation：`REVIEW_PASSED`。在批准 write-set 内 materialize 单机内网 Docker Compose 生产拓扑（设计 §§4/7/9，ADR 0003/0006/0030）：PostgreSQL 16、Redis 7（仅 broker）、FastAPI API（uvicorn）、Celery worker、ADR 0030 single-active scheduler、Vue 前端、TLS 反向代理；API/worker/scheduler 共享 `risk-platform-api:0.1.0` image，仅 `command` 不同。`infra/docker-compose.yml`：`x-backend-env` anchor、双网络（固定子网 `10.30.0.0/24` backend trust boundary + 既有 `project-risk-network`）、compose secret（session key 只读文件）、healthcheck + `service_healthy` 依赖序、持久 named volumes（PG data + app/import storage）、required env `${VAR:?}` fail-fast；postgres 保留 `127.0.0.1` bind，dev/integration 不变。`infra/proxy/nginx.conf`：TLS 1.2/1.3 + 安全 headers + SSE regex location（`proxy_buffering off`/`proxy_cache off`/`3600s`/`chunked`，先于 `/api/` prefix 匹配，ADR 0016）+ `/api/`→api、`/`→web。`apps/api-python/Dockerfile`（uv 多层、非 root、uvicorn CMD）+ `apps/web/Dockerfile`（node:22→nginx 两阶段，runtime 无 Node/NestJS/Prisma）。`infra/env.example`/`README.md`/`scripts/init-secrets.sh`（生产模板、runbook、test secret/cert bootstrap，均 gitignored）。加 `uvicorn[standard]`（用户批准）+ re-lock；OpenAPI authority 不变。scheduler 复用 T046 `main()` entrypoint（`scheduler.py` 无 `__main__` guard，故 `command` 显式 `python -c "from risk_platform.scheduler import main; main()"` 调用既有 entrypoint，零新增逻辑、`scheduler.py` 未改）；wiring 含 cadence env/DB·Redis env/`:9191` liveness/`restart: unless-stopped`/`stop_grace_period: 60s`。Independent Review `PASS`：6 项特殊限制全 HELD（scheduler.py 与 T040 composition 未修改、DG-05/DG-08 未处理且无 `infra/backup/**`、无 SQLite/NestJS/Prisma/dual-write、OpenAPI byte-identical），3 项 acceptance criteria 全 MET，7 类服务齐全；2 项 non-blocking observation（README scheduler command 描述已修正；scheduler fail-fast 路径连接警告属 frozen T046 范围、进程退出 + session-level lock 自动释放、仅 cosmetic）。部署验证矩阵 23 项全 PASS（config 渲染、image build、full stack boot、PG/Redis/API/scheduler health、real Celery worker ping、single-active fence exit 1、proxy→frontend/`/api`/SSE 路由 + 无 buffering、持久化跨 down/up、env/secrets 未提交、无 NestJS/Prisma runtime、fail-fast env、Alembic head、seed、`uv lock --check`、Ruff/mypy、full pytest `201 passed/104 skipped`、`contracts:check` zero diff、`git diff --check`、frozen write-set 未改）。Code checkpoint：`732adfb0c742f486748cc1aff707231b07820b34`。Wave 22 保持 `IN_PROGRESS`（partial）；Wave 22 Integration、下一 Wave（Wave 23/T036）、T036、DG-05/DG-08 均未启动/未处理。详见 `docs/implementation/reports/T035.md`。
- Wave 22 Integration：`PASS`。T035 `REVIEW_PASSED` + code checkpoint（`732adfb`）+ metadata checkpoint（`d2b1dec`）后执行项目级联合验证，重点验证 production deployment topology 的项目级联合行为。7-service Compose 全栈（PostgreSQL 16、Redis 7、FastAPI API、Celery worker、ADR 0030 single-active scheduler、Vue 前端、TLS 反向代理）真实容器联合一致。验证矩阵 25 项全部通过：(1) `docker compose config` 渲染（test env + dev `.env`）valid；(2) 7-service 全栈 Up（PG/Redis/API/worker/scheduler healthy）；(3) api/worker/scheduler 共享 `risk-platform-api:0.1.0`（同一 image id `c87ed53…`），仅 `command` 不同；(4) PostgreSQL 16 `pg_isready`；(5) Redis 7 `ping` PONG（仅 broker）；(6) API health direct `:3000` 200；(7) API health via proxy `/api/health` 200；(8) real Celery worker `inspect ping` 1 node online + durable task 端到端 SUCCEEDED；(9) scheduler T046 entrypoint 运行 + liveness `:9191` `{"healthy":true,"lock_held":true}`，tick 在 2× outbox drain 窗口内；(10) scheduler single-active 第 2 实例 fail-fast `exit 1`，primary 不受影响；(11) durable-task 端到端闭环——request path `enqueue_task` 仅写 PostgreSQL（`DurableTask` QUEUED + `TaskOutbox` unpublished）→ scheduler `publish_outbox`（5s cadence）`send_task` 后置 `publishedAt` → real worker fenced claim→handle→`finish_task` → `DurableTask.status=SUCCEEDED`/`completedAt` 已置/`attemptCount=1`/无 `failureCode`（PostgreSQL 唯一 authority，无 dual-write）；(12) scheduled mailbox path scheduler→`schedule_enabled_syncs` 在 cadence 运行，lifetime 0 `动作失败`，无 enabled config 时 idempotent no-op（with-data 路径已于 Wave 21 验证）；(13) proxy→frontend `GET /` 200 `text/html`；(14) proxy→`/api` `GET /api/health` 200；(15) proxy→Agent SSE 路由经 proxy 返回 API 401 auth（非 502/404，证明 regex location 先于 `/api/` prefix 路由）+ `nginx -t` valid + SSE location `proxy_buffering off`/`proxy_cache off`/`3600s`/`chunked`；(16) persistence PostgreSQL 数据跨 `down`→`up` 保留（admin count 1→1，e2e durable task SUCCEEDED 行保留）；(17) persistence app/import storage named volume `project-risk-storage`→`/app/storage`（api+worker 均挂载），storage marker 文件跨 restart 保留；(18) secrets compose secret session key 只读文件挂载（`ro=false` 即 RW=false），runtime 可读；(19) 无 secret literal 泄漏（compose/env/example 无硬编码 secret；`infra/secrets/`/`infra/proxy/certs/`/`.env.t035-test`/`.env` 均 gitignored 未 tracked）；(20) startup/restart 依赖序 7×`service_healthy` + 1×`service_started`；(21) FastAPI frozen OpenAPI 与 tracked `openapi.json` byte-identical（`cmp` zero diff，422281 bytes，93 paths/243 schemas/104 operations）；(22) 无 SQLite/NestJS/Prisma runtime/dual-write（api image 仅 Python 无 node；web runtime 仅 nginx+静态无 node/npm；`send_task` 仅在 `dispatcher.publish_outbox`，请求路径不碰 broker）；(23) `infra/backup/**` 未触碰，DG-08 不在本 Wave；(24) DG-05 numeric thresholds 未做额外决策（cadence 为 operational defaults 非 SLO）；(25) scheduler fail-fast SQLAlchemy 连接警告 triage——见下。scheduler fail-fast 警告 triage：以真实第 2 scheduler 实例复现，fail-fast 行为正确（`exit 1`）；`pg_locks` 恰好一行 advisory lock（primary 持有），两次 fail-fast 退出后仍仅一行（无 lock 泄漏）；`pg_stat_activity` 连接数稳定为 4（api/worker/scheduler/psql，无 connection 泄漏）；primary 不受影响。根因：fail-fast 路径 `run()` 返回 1 时未调用 `release()`（唯一 `close()` `lock_conn` 的路径），`async_main` finally `dispose_database_engine` 但该 IDLE 连接在进程退出时由 GC 回收触发 SQLAlchemy 警告；进程即将终止，PostgreSQL 在连接/进程终止时自动释放 session-level lock，无持久 leak。结论：仅为 frozen `scheduler.py`（T046 write-set）进程退出的 cosmetic warning，不构成真实 resource leak / deployment failure；按协议不越界修改 T046，归属 T046 follow-up。Validation：`docker compose config` PASS（两 env）；production image build PASS（栈中运行）；clean-stack boot PASS（`down`→`up` 全栈恢复 healthy）；service/API health PASS；real Celery worker ping + task execution PASS；scheduler health + lock smoke PASS；proxy frontend/API/SSE PASS；persistence restart PASS；`pnpm contracts:check` clean-tree zero diff PASS；`@risk-platform/contracts` typecheck PASS；`@risk-platform/web` typecheck（`vue-tsc -b --noEmit`）PASS；Ruff `All checks passed!`；mypy `Success: no issues found in 187 source files`；full pytest（PostgreSQL 16 + Redis 7）`304 passed, 1 skipped`（102.07s，与 Wave 21 baseline 一致；1 skip 为 `test_audit_query_export` 既有 DB 专项分工提示，skip 信息明确 "TEST_DATABASE_URL 已配置；PostgreSQL 集成由数据库专项测试执行"，非 DB-skip-as-PASS；`test_postgresql_schema.py` 7 + `test_schema_metadata.py` 5 + scheduler/retention/auth 等 DB 专项真实执行并通过）；`uv lock --check` `Resolved 65 packages`；`git diff --check` clean；frozen write-set（`scheduler.py`/`composition.py`/`main.py`/`worker.py`/`celery_app.py`）自 `732adfb` diff 0 行。无 integration fix——T035 deployment write-set 未暴露任何 integration failure；未修改任何 production 代码、测试基线、migration、contract artifact、frozen write-set 或 frozen OpenAPI authority；未写入 `infra/backup/**`。Wave 23/T036 仅同步 readiness，未执行；DG-05/DG-08 未处理。详见 `docs/implementation/reports/WAVE-22.md`。Final checkpoint 记录于本条目之后的提交。
- Wave 23 / T036 readiness 同步（仅 metadata，未执行 T036、未启动 Wave 23）：T036（deps T031、T035）的 dependency 层现已全部满足——T031 `REVIEW_PASSED`、T035 `REVIEW_PASSED` + Wave 22 Integration `PASS`。**DG-08 已由 ADR 0031 解决**（design/metadata checkpoint，未实施 production code、未写 `infra/backup/**`、未执行 T036、未启动 Wave 23）：ADR 0031 定义加密备份/恢复契约——(1) 备份权威 = PostgreSQL + durable 文件存储（`project-risk-storage`），Redis/队列/临时文件/容器日志不入备份；密钥材料不入备份。(2) 一致性模型 = quiesce 协调捕获（停 scheduler/worker + API maintenance）→ `pg_dump` 单快照 + 文件 tar → manifest 绑定 → 加密 → unquiesce；恢复时 orphan reconcile；禁止“分别复制”声称一致；不用 WAL/PITR。(3) 格式 = `pg_dump -Fc` + tar + JSON manifest（含 backupId/type/time/pg/files/encryption/status/alembicHead）。(4) 加密 = AES-256-GCM 信封加密（per-backup DEK 被 versioned 备份 KEK 以 `rpenc` 包装，复用 T007 `KeyRing`；AAD 绑定 manifest；三层完整性）。(5) 密钥管理 = 独立于 `DATA_ENCRYPTION_KEY` 的备份 KEK，宿主只读文件注入，与 T035 Compose 解耦。(6) 轮换 = 新备份用 active 版本，历史备份靠 retained decrypt 版本可恢复，不 re-encrypt，退役需 ADR 0009 窗口 + 无 ADR 0027 hold + 无未完成演练。(7) `backupId` = ADR 0027 `BACKUP_COPY` 稳定标识；不自行决定 DG-05 数值或 ADR 0009 7/4/12。(8) 恢复 = 隔离目标、fail-closed（密钥查找 + AEAD/sha256/manifest 三层校验 + 审计哈希链校验 + 文件 reconcile + 部分集合中止）。(9) 失败语义 = DB/files 任一失败或加密/manifest/quiesce 失败 → `INCOMPLETE` 不 `USABLE`；明文清理失败触发 SEVERE 告警。(10) 日志 metadata-only，不含密钥/明文/邮件内容；仅 ADR 0027 `BACKUP_COPY_DELETED` 入业务审计链。(11) T036 直接拥有 backup+restore 实现（`infra/backup/**` + tests/runbook），读依赖 `risk_platform.shared.crypto`/T035 volume/T031 布局，无需新增 app entrypoint，DAG 不变。(12) 备份/恢复为 one-shot 命令（复用既有 `postgres:16-alpine` + `risk-platform-api:0.1.0` image），不重开 T035 Compose。T036 由 `DESIGN_GAP (DG-08)` 恢复为 `READY`（仅 metadata）；Wave 23 未启动。DG-05 保持 out of scope。详见 `docs/adr/0031-define-encrypted-backup-and-restore-contract.md`。
- Wave 23 / T036 readiness：`READY`。T036 blocking 前置 T031（`REVIEW_PASSED`）、T035（`REVIEW_PASSED`）+ Wave 22 Integration `PASS` 已满足；DG-08 已由 ADR 0031 解决。Wave 23 标记为 `IN_PROGRESS`，仅授权 T036（Lean Execution Mode）。
- T036 implementation：`REVIEW_PASSED`。在批准 write-set `infra/backup/**` 内 materialize ADR 0031 加密备份/恢复契约（设计 §9，ADRs 0008/0009/0014/0027/0031）：新增 `risk_backup` one-shot operator package（`envelope`/`keys`/`backup`/`restore`/`manifest`/`archive`/`reconcile`/`db`/`pgdump`/`quiesce`/`temputil`/`timeutil`/`cli`/`errors` + 11 test files + README + pyproject/conftest）。**加密**：AES-256-GCM 信封；per-backup 随机 256-bit DEK 被 versioned 备份 KEK 经 T007 `rpenc`/`KeyRing` 包装（KEK 版本作 AAD）；三层完整性（DEK-wrap tag + 每块 tag + manifest sha256）；数据组件 AAD 绑定规范 manifest 字节 + 组件名 + 块序号；nonce 由 per-component 随机基 XOR 块序号派生，无 (key,nonce) 复用；外层头仅含魔数/格式版本/KEK 版本/包装后 DEK。**密钥**：256-bit 备份 KEK 从宿主只读文件加载（不从 env），独立于 `DATA_ENCRYPTION_KEY`；active + retained 版本，历史备份不 re-encrypt 即可恢复；制品仅含包装后 DEK + KEK 版本。**备份一致性**：quiesce 协调捕获（quiesce → confirm → `pg_dump -Fc` → tar durable files → manifest → encrypt → cleanup → unquiesce）；任一失败 → `INCOMPLETE`（删除部分制品）；`USABLE` 仅当完整加密/hash/manifest 校验通过；明文临时目录与输出分离、成功+失败均清理，清理失败 → USABLE + SEVERE warning。**恢复 fail-closed**：隔离空目标（不覆盖在线）；密钥查找（`MISSING_KEK_VERSION`）→ AEAD（`ARTIFACT_AEAD_AUTH_FAILED`）→ manifest 校验 → 组件 sha256 → `pg_restore` + 审计哈希链（`AUDIT_CHAIN_BROKEN`）+ alembic head → 文件 extract + reconcile（丢弃 orphan，`RESTORE_MISSING_REFERENCED_FILE`）；部分集合中止。**范围/边界**：备份权威 = PostgreSQL + durable 文件存储；Redis/Celery/cache/temp/log 排除；密钥材料不入备份；metadata-only 日志（KEK 仅版本），不写业务审计链（ADR 0017）；one-shot 命令，不新增 Compose service，未改 `infra/docker-compose.yml`/T035/T040/T046 frozen write-set/frozen OpenAPI authority，无 WAL/PITR/`pg_basebackup`；`backupId` = ADR 0027 `BACKUP_COPY` 稳定标识，不自行决定 DG-05 数值或 ADR 0009 7/4/12。Independent Review `REVIEW_PASSED`，无 blocking finding（5 项 non-blocking observation：quiesce 以停止 api 容器实现 maintenance 态【frozen API write-set 强制、对一致性严格更强、safe】、backupId 哈希来源解循环【sound】、SEVERE cleanup warning + CLI exit 0 建议非零 exit【运维加固】、reconcile 仅引用 `import_batches.storageKey`【当前 ADR scope 可接受】、外层头结构元数据【cosmetic】）。Validation：focused unit `51 passed` + real PostgreSQL 16 backup/restore drill `8 passed`（round-trip/PG+files 一致性/orphan discard/missing referenced file/corrupted DB archive/corrupted file archive/active+historical KEK/broken audit chain）= 全量 `59 passed`；Ruff `All checks passed!`、mypy strict `Success: no issues found in 27 source files`、`uv lock --check` `Resolved 65 packages`、`git diff --check` clean；write-set 仅 `infra/backup/**`。Code checkpoint：`5824695f73c1c31e272b17e88324e13b674b80d1`。Wave 23 保持 `IN_PROGRESS`（partial）；Wave 23 Integration、Wave 24/T037、下一 Wave、DG-05 均未启动/未处理；`infra/docker-compose.yml` 与全部 frozen write-set 未修改。详见 `docs/implementation/reports/T036.md` 与 `docs/implementation/reports/WAVE-23-PARTIAL.md`。
- Wave 23 Integration：`PASS`。T036 `REVIEW_PASSED` + code checkpoint（`5824695`）+ metadata checkpoint（`27dabf2`）后执行项目级联合验证，重点验证 T036 backup/restore 与既有 persistence/security/retention/deployment 体系的项目级联合一致性。ADR 0031 加密备份/恢复契约在真实 PostgreSQL 16 + 真实 `pg_dump -Fc`/`pg_restore`（经 `docker exec` 运行 `postgres:16-alpine` 二进制）下联合一致。14 项联合行为验证矩阵全部通过（backup authority / coordinated quiesce / real PG16 backup / durable file capture / AES-256-GCM envelope + 三层完整性 + AAD / key lifecycle / tamper-fail-closed matrix / fail-closed isolated restore / file reconciliation / USABLE-only-after-verification / ADR 0027 backupId=BACKUP_COPY identity / deployment boundary / frozen write-set 不变 / 无 WAL-PITR-cloud-DG05）。**1 项 integration fix**（`infra/backup/**` write-set 内）：CLI `cmd_restore` 经 `_build_pg_dumper(args)` 读取 `args.dsn`，但 restore 子解析器仅定义 `--target-dsn`（无 `--dsn`）→ `AttributeError`；根因为 backup/restore 单元测试与 PG drill 直接调用 `run_backup`/`run_restore`，从未驱动 CLI 子命令。最小修复 `_build_pg_dumper(args, dsn)` 显式接收 DSN，`cmd_backup` 传 `args.dsn`、`cmd_restore` 传 `args.target_dsn`（`cli.py` +4/−4）+ 新增 `tests/test_cli.py`（2 CLI wiring 回归测试）。CLI production-volume smoke（setup→`python -m risk_backup backup`→destroy isolated target→restore→integrity verify）端到端 PASS。5 项 Independent Review non-blocking observations 经 integration 评估均确认为已批准/非阻断（quiesce full-stop 严格更强、backupId hash-source 解循环、SEVERE cleanup exit-code 为 ADR 批准的运维加固、reconcile schema boundary 在 ADR scope 内——`import_batches.storageKey` 为唯一 durable-file 引用、outer-header metadata cosmetic），不升级为 integration blocker，不扩 scope。Validation：`infra/backup` `61 passed`（59+2）、real PG drill `8 passed`、Ruff/mypy（28 backup files）PASS、app Ruff/mypy（187 files）PASS、full pytest（PostgreSQL 16 + Redis 7）`303 passed, 1 skipped` + 1 既有 flaky T029/T040 heartbeat 时序测试单独重跑 PASS（与 T036 无关，非 blocker）、`uv lock --check`（65）/`git diff --check` PASS、frozen write-sets（scheduler/celery_app/composition/worker/main）+ `docker-compose.yml` 自 `732adfb` diff 0、`openapi.json` 自 `ac351cb` diff 0；无 WAL/PITR/cloud backup/DG-05 决策。无 frozen write-set / `docker-compose.yml` / OpenAPI authority / app 代码修改（`risk_platform` 不 import `risk_backup`，隔离）。详见 `docs/implementation/reports/WAVE-23.md`。Final checkpoint 记录于本条目之后的提交。
- Wave 23 final checkpoint: `ad79d62d1bab96acb05cf461fe846ae1857acdf2`（integration fix + final report；metadata recorded in the following report commit）
- Wave 24 / T037 readiness 同步（仅 metadata，未执行 T037、未启动 Wave 24）：T037（deps T033/T034/T036）dependency 层现已全部满足——T033/T034/T036 均 `REVIEW_PASSED` + Wave 19/20/23 Integration `PASS`。T037 inherited design gaps 均已解决：DG-04（ADR 0027）、DG-08（ADR 0031）、DG-10（ADR 0022）；DG-05（capacity thresholds）为 T037 `Explicit out-of-scope`（属 T038）。T037 状态由 `BLOCKED_DESIGN_GAP (inherits DG-04/DG-05/DG-08/DG-10) / TODO` 同步为 `READY`（仅 metadata，纠正 stale header），可在 Wave 24 评估/执行。本次不执行 T037、不启动 Wave 24；DG-05 保持 out of scope。
- Wave 24 / T037 readiness：`READY`。T037 blocking 前置 T033/T034/T036 均 `REVIEW_PASSED` + Wave 19/20/23 Integration `PASS`；inherited gaps（DG-04/DG-08/DG-10）全部解决，DG-05 为 explicit out-of-scope（属 T038）。Wave 24 标记为 `IN_PROGRESS`，仅授权 T037（Lean Execution Mode）。
- T037 implementation：`REVIEW_PASSED`。在批准 write-set（`apps/api-python/tests/acceptance/**` only；no feature-module test/source edits）内交付跨模块发布验收套件（`conftest.py` + 4 test modules，60 tests），针对真实 PostgreSQL 16 + Redis 7 + 真实 production FastAPI app（17 routers，`build_services`）+ 隔离 Alembic `head` schema 证明：`/api` 契约（`ApiResponse` 信封 / `PaginatedResponse` / 固定 401·403·404·422 错误码 / `x-trace-id` / 真实 session cookie）、四角色×五数据范围授权矩阵（正负例 + 范围外 404 非 leaky 403 + 归档排除）、安全不变式（CSRF origin / SSRF guard 含 private·loopback·metadata·DNS-rebinding·approved-internal / Excel 上传安全 / closed Agent Provider fail-closed 无 secret 泄漏）、审计哈希链完整性 + append-only 不可变性（真实 DB trigger 拒绝 UPDATE/DELETE/TRUNCATE）、可靠性（幂等 enqueue / crashed-worker 恢复 / orphan gen-0 redispatch / outbox→Celery 派发）与回滚原子性（risk+todo+audit 全部不留痕）。harness：每模块独立 schema（`t037_<uuid>` + Alembic `head`）+ seed 批准 reference data + 真实 argon2 用户；`current_identity` override 驱动角色、`identity=None` 走真实 cookie；NullPool 避免 cross-loop 连接复用 flake。仅 3 处明确允许的 test double（`current_identity` override / `_StaticResolver` / `_CeleryRecorder`），无核心不变式被 mock 掉。Independent Review `REVIEW_PASSED`，零 critical/high finding（3 项 non-blocking observation：无 `artifacts/acceptance/**` 目录【spec "for example"，套件本身即 traceable evidence】、module-scoped seed 共享【当前执行下安全】、pre-existing mailbox flaky-timing【归属 T025 follow-up】）。Validation：focused acceptance pytest（隔离，PG16+Redis7）`60 passed`；full combined pytest `363 passed, 1 skipped, 1 failed`（1 failed = pre-existing `tests/mailbox/test_parsing.py::test_attachment_output_limit_is_metadata_only`，T025 frozen write-set 不可编辑，full-suite load 下返回 `PARSER_TIMEOUT` vs 严格断言 `OUTPUT_TRUNCATED`，同胞测试已容忍 `PARSER_TIMEOUT`；隔离重跑 `10 passed` 3 轮稳定；production 行为正确 = 安全 metadata-only；T037 spec "Flaky timing/external isolation" known risk，既非 inherited gap 亦非 architecture change，低严重度，非 blocker，与 Wave 23 precedent 一致；修复归属 T025 follow-up）；Ruff `All checks passed!`、mypy `Success`、`uv lock --check`（65）、`git diff --check` clean；write-set 合规（仅 `tests/acceptance/**` 新文件，`src/risk_platform/**` 与 feature-module test 0 编辑）；`pnpm contracts:check`/frontend N/A（write-set 为 acceptance tests only，未改 OpenAPI/前端契约）；未定义任何 DG-05 numeric threshold；无 NestJS/Prisma/SQLite/dual-write。Code checkpoint：`ac05222792054bfa2a0befd709948b18430ff36e`。Wave 24 保持 `IN_PROGRESS`（partial）；Wave 24 Integration、Wave 25/T038、下一 Wave、DG-05 均未启动/未处理；frozen write-sets（T035/T040/T046/T036）与 frozen OpenAPI authority 未修改。详见 `docs/implementation/reports/T037.md` 与 `docs/implementation/reports/WAVE-24-PARTIAL.md`。
- T037 code checkpoint: `ac05222792054bfa2a0befd709948b18430ff36e`（compatibility & security acceptance suite；metadata recorded in the following report commit）
- Wave 24 Integration：`PASS`。T037 `REVIEW_PASSED` + code checkpoint（`ac05222`）+ metadata checkpoint（`f6da71f`）后执行项目级联合验证，重点验证 T037 acceptance suite 与当前 production backend 的项目级联合一致性。acceptance 套件（`tests/acceptance/**`，`conftest.py` + 4 test modules，60 tests）针对真实 PostgreSQL 16.14 + Redis 7.4.10 + 真实 production FastAPI app（17 routers，经 `build_services` 组装）+ 隔离 Alembic `head` schema（`t037_<uuid>`）全部通过。22 项联合行为验证矩阵全部通过：production app/17 routers/`build_services`（非 stub）；`ApiResponse`/`PaginatedResponse` 信封 + `x-trace-id`==`traceId`；固定 401/403/404/422 错误码；secure session cookie（HttpOnly/Secure/SameSite=lax/Path/expires）+ `mustChangePassword` + session 回读；`DataScope=NONE` 空页；四角色×端点授权矩阵（24 例）+ 五 `DataScopeType` 精确项目集合 + 归档排除 + 范围外 404 非 leaky 403；CSRF origin / SSRF guard（private·loopback·metadata·IPv6·IPv4-mapped·DNS-rebinding·approved-internal）/ Excel 上传安全 / closed Agent Provider fail-closed 无 secret 泄漏；审计哈希链 `verify_integrity().status=="VALID"` + append-only 真实 DB trigger（UPDATE/DELETE/TRUNCATE 各抛 `DBAPIError`）；可靠性幂等 enqueue / crashed-worker `RETRY_WAIT` 恢复 / orphan gen-0 redispatch / outbox→Celery `publishedAt` 置位；回滚原子性 risk+todo+audit 全部不留痕；仅 3 处已批准 test double，无核心不变式被 mock 掉。**无 integration fix**——T037 write-set（`tests/acceptance/**` only）未暴露 integration failure。唯一 full-suite 失败为 pre-existing flaky-timing mailbox 测试（T025 frozen write-set，超出 T037 write-set 不可编辑）：full-suite load 下返回 `PARSER_TIMEOUT`（严格断言 `OUTPUT_TRUNCATED`）；隔离重跑 `10 passed` × 3 轮稳定；production fail-closed 安全——`parsing.py:313` `PARSER_TIMEOUT` 返回 `text=""` metadata-only，同胞测试已容忍；分类为 timing-only flaky + production fail-closed 安全，非 integration blocker，归属 T025 follow-up，不修改 T025 frozen write-set，与 Wave 23 precedent 一致。Validation：focused acceptance `60 passed`、full pytest `363 passed, 1 skipped, 1 failed`（123.36s）、mailbox flaky 隔离 `10 passed` × 3 轮、Ruff `All checks passed!`、mypy `Success: no issues found in 202 source files`（scope = src+tests，含 acceptance tests，未变）、`uv lock --check` `Resolved 65 packages`、`git diff --check` clean、`git status --porcelain` 为空、frozen write-sets（T035 `docker-compose.yml`/T040 `composition.py`·`celery_app.py`·`worker.py`·`main.py`/T046 `scheduler.py`/T036 `infra/backup/**`）自各自 checkpoint diff 0、frozen OpenAPI authority 自 `ac351cb` diff 0 且 fresh `risk-platform-openapi` 导出 byte-identical（93 paths/243 schemas/104 operations）、`src/risk_platform/**` 自 `ac351cb` diff 0。无 production 代码/测试基线/migration/contract artifact/frozen write-set/OpenAPI authority 修改。详见 `docs/implementation/reports/WAVE-24.md`。Final checkpoint 记录于本条目之后的提交。
- Wave 25 / T038 readiness 同步（仅 metadata，未执行 T038、未启动 Wave 25）：T038（deps T037 和 DG-05）的全部阻断已解除——T037 `REVIEW_PASSED` + Wave 24 Integration `PASS`，且 **DG-05 已由 ADR 0032 解决**（本 design/metadata checkpoint，未实施 production code、未实现 benchmark/load-test、未修改任何 frozen write-set、未执行 T038、未启动 Wave 25）。ADR 0032 定义 T038 所需的全部数值阈值与发布门语义：(1) API latency——fast read/mutation/admin-overview/async-dispatch/auth 五类 endpoint，p95 为硬 gate（≤500/800/2000/1000/1000ms），p50 目标、p99 报告，≥60s 窗口 + ≥30s warmup + 每类 ≥500 样本，50 并发认证 VU，锚定单 uvicorn + Celery concurrency=2 + ADR 0030 scheduler + PostgreSQL 16 + Redis 7 单机 Compose；(2) error-rate——HTTP 5xx ≤0.5%、任务非基础设施 FAILED ≤1%、retry 比例 ≤5%、scheduler tick 失败率 ≤1%、SSE 非预期断连 ≤1次/5min、SSE resume 100% 正确；(3) worker/queue——durable task queue age p95 ≤15s、outbox 未发布年龄 ≤30s、retry backlog ≤50 且非单调增长、worker availability `inspect ping` ≥1 node 100%、无 lease 超期；(4) scheduler——tick 新鲜度 liveness healthy ≥99% 且连续 unhealthy ≤30s、advisory-lock single-active 100%（第二实例 5s 内 fail-fast）、cadence 容差任一 action 不停滞超 2× interval、per-tick/per-function 失败隔离；(5) DB——峰值 active 连接 ≤70% max_connections 且 pool 取连接超时 0、无锁等待 >1s、>2s 查询 0 且 slow-query(>500ms) ≤1%、磁盘 <80% 且 size 增长不超 fixture 规模；(6) SSE/Agent——初始事件 p95 ≤2s、reconnect resume 无丢失/重复 100%、heartbeat ≤15s、Provider 失败在 task timeout(900s) 内必发 retryable error event；(7) 备份/恢复——T038 不显式消费备份门，不重开 ADR 0031，仅 reaffirm ADR 0009 RPO 24h/RTO 4h 不被负载违反；(8) release gate——PASS=全部硬 gate 连续 2 次满足且证据完整、WARN=target 超标但 gate 满足、FAIL=任一硬 gate 违反或证据缺失或不可重复；flaky 样本最多再跑 2 次（3 中 2 过为瞬态 WARN）；环境性失败标 UNVERIFIED 不得记 PASS；禁止 best-effort PASS——替身 AI/IMAP 路径证据显式标注，真实外部吞吐推迟 T039；(9) 方法论——可复现 generator（300/5000/1000 + 真实规模 risks/todos/audit/timeline/conversations/mailbox）、50 并发、本地单机 T035 Compose、可重复 ±20% p95/0.3pp error-rate、仅 3 类已批准 test double；(10) 归属——T038 直接拥有 `tests/load/**`+`artifacts/load/**`（generator/scenarios/raw/PASS-FAIL report），无需新增 remediation Task；任何达标所需 production code/index/migration 变更为 T038 FAIL finding 返回 owning Task（T020/T027/T008/T046/schema-owner），T038 不实施修复。T038 状态由 `BLOCKED_DESIGN_GAP (DG-05) / TODO` 恢复为 `READY (DG-05 resolved by ADR 0032) / TODO`；Wave 25 保持 `NOT_STARTED`，待显式授权。详见 `docs/adr/0032-define-capacity-and-resilience-acceptance-thresholds.md`。
- Wave 24 final checkpoint: `764ab79a4cd87500dffb19b95d70f2f1f843ab1e`（Wave 24 Integration report + state update；metadata recorded in this commit）
- DG-05 resolution / ADR 0032 design checkpoint: `4a654d1e328e63d316910acc7938b1ea2e6dbd0c`（ADR 0032 批准 T038 所需数值性能/可靠性阈值与发布门语义——API latency p95 硬 gate、error-rate、worker/queue/scheduler/DB/SSE 可靠性、PASS/WARN/FAIL 语义、flaky 重试、禁止 best-effort PASS、测量方法学与 T038 直接 ownership；锚定单机 Compose 部署与 ADR 0009 容量基线，不重开 ADR 0009 基线或 ADR 0031 语义；T038 由 `BLOCKED_DESIGN_GAP (DG-05)` 恢复 `READY`；无 production code、无 benchmark 实现、未修改 frozen write-set、未执行 T038、未启动 Wave 25；metadata recorded in this commit）
- Wave 25 / T038 readiness：`READY`（已授权执行）。T038 blocking 前置 T037（`REVIEW_PASSED` + Wave 24 Integration `PASS`）与 DG-05（ADR 0032 解决，design checkpoint `4a654d1`，SHA-record `23b112a`）均已满足。Wave 25 标记 `IN_PROGRESS`，仅授权 T038（Lean Execution Mode，T038 only）。
- T038 execution：`REVIEW_FAILED`。在批准 write-set（`apps/api-python/tests/load/**` + `artifacts/load/**` only；"any required code or index change is a FAIL finding returned to the owning task and is not implemented here"）内交付可复现 capacity/resilience load-acceptance harness（`config.py`/`generator.py`/`scenarios.py`/`collectors.py`/`gates.py`/`report.py`/`stats.py`/`run_load.py`），针对 T035 单机 Compose reference environment（single uvicorn + Celery concurrency=2 + ADR 0030 single-active scheduler + PostgreSQL 16 + Redis 7 broker-only）驱动 50 并发认证 VU、≥30s warmup、150s measurement 的两轮测量。**两轮均 FAIL（结构性、确定性，非 flaky）**，release_verdict = `FAIL`（`result.json`："2 run(s) with hard-gate FAIL; 2-of-3 not reached"，无需 Run 3）。Run 1 = 11 hard-gate FAIL（latency 全 5 类 fast_read 9485ms/mutation 10507ms/admin_overview 9167ms/async_dispatch 11101ms/auth 7191ms；http_5xx 1.86%；task_retry 100%；db_connection_saturation pool_acquire_timeouts=1；db_slow_query_2s=2623；db_slow_query_500ms=93.8%；sse_initial_event 6.02s）；Run 2 = 12（同 11 + db_lock_wait 1.079s）。**根因经源码逐行核实为真实 production 容量缺陷，非 harness/env artifact**：(1) 主因——`/api/todos` 无分页，全量 scoped 结果集（11667 todos × 6 表 JOIN）双重物化（`todos/service.py:62-63` 双 `_rows()`；`_statement:191-198` 无 `LIMIT`/`OFFSET`；`schemas.py:81-85` `ListTodosQuery` `extra="forbid"` 仅 `owner/status`，`?page=1&pageSize=20` 为 dead params），返回约 7.4MB，孤立 ~2s / 50 VU p95 6.4–9.5s → 级联连接池耗尽 → 跨类 latency + 5xx + 100% retry + DB slow-query/pool/lock gates（对照 `/api/risks` 有正确 `.offset/.limit` 分页，孤立 36ms）；(2) SSE 初始事件——会话创建写 0 个同步 `AgentEvent`，首帧受 ADR 0030 outbox-drain cadence（`scheduler.py:64` `DEFAULT_OUTBOX_DRAIN_SECONDS=5.0`）下界约束，即便无 provider ~5s > 2s gate（跨 ADR 0030 §3 vs ADR 0032 §6 张力）；(3) SSE heartbeat UNVERIFIED→FAIL——ADR 0032 §9 fake Provider 无 production 注入点（`composition.py:168` `build_provider` 无条件），fast-fail 流 ~5s 因 `ERROR` terminal 关闭（`events.py:38`），无长流可测 keepalive，`_stream` 无 transport keepalive。PASS gates（两轮稳定）：§3 worker/queue（queue_age p95 4.58–5.04s、outbox 0s、retry_backlog 0 非单调、worker 100%、无 expired lease）、§4 scheduler（freshness 100%、single-active、cadence ok、tick fail 0%）、§2 task_failed_ratio 0%、§5 db_disk <80%、§6 sse_resume correct——ADR 0030 调度器/可靠性拓扑与 outbox/durable-task 体系在负载下稳健，FAIL 集中 API latency/DB query/SSE 三处。reference env 非资源受限（16 CPU/15GB/无容器限制，驱动期 CPU 0.05–0.20% = I/O/序列化瓶颈）。external AI/IMAP throughput 未伪造——本轮仅验证本地可测维度，SSE gate 标 `substituted=True`，真实外部 E2E 推迟 T039，无 best-effort PASS。**production code 未修改**（`src/`、schema、index、migration、frozen write-set、frozen OpenAPI authority 全部 0 diff；`git status` 仅 `?? apps/api-python/tests/load/`）。capacity/resilience 缺陷作为 FAIL finding 返回 owning Task（`/api/todos` 分页 → todos list endpoint owning task；connection pool → owning concern；SSE 初始事件/keepalive + §9 fake-provider 接线 gap → SSE/outbox 架构 owning concern），不在 T038 内实施修复。Independent Review `CONFIRMED-FAIL`：FAIL honest、justified、production-untouched，无 harness-only 修复可使 gate PASS（4 项 honest caveats：`task_retry` 100% 归属歧义但不影响 verdict——11 项独立 gate FAIL 不依赖它；单请求 timing 未入 artifacts；`db_lock_wait` marginal 1.079s vs 1.0s；3 类 sample-count 不足为池饱和结果）。Validation：Ruff（`tests/load/**`）`All checks passed!`、mypy（10 files）`Success`、focused load-test tests `16 passed`、`uv lock --check` `Resolved 65 packages`、`git diff --check` clean、write-set 合规（仅 `tests/load/**` 新文件）。无 production code checkpoint（FAIL path）。详见 `docs/implementation/reports/T038.md` 与 `docs/implementation/reports/WAVE-25-PARTIAL.md`。
- Wave 25 状态：`IN_PROGRESS`（partial — T038 `REVIEW_FAILED`；harness + raw evidence 已交付；Wave 25 Integration 未启动；capacity/resilience production 缺陷返回 owning Task；T038 主因 `/api/todos` 分页已由 T047 `REVIEW_PASSED` remediate，首版低并发可用，其余 T038 findings DEFERRED 至 post-MVP hardening）。**Per ADR 0033**：Wave 25 为 capacity-track **deferred / closed-for-MVP**，**不是** Integration `PASS`（T038 本身未 PASS，不满足「全部必要 Review 通过」前提）；首版以 INTERNAL_MVP 发布配置继续，full 50-VU 认证 + T039 外部 E2E 推迟至 PRODUCTION_CAPACITY_READY milestone。**未启动** Wave 25 Integration；**未执行** T039；**未启动** 下一 Wave；**未修改** ADR 0032 / frozen write-set / frozen OpenAPI authority（T047 re-freeze 为 ManagerTodoListResponse additive 字段，93 paths/243 schemas/104 ops surface 不变）。
- T038 harness + FAIL report checkpoint: `9adc487`（T038 capacity/resilience load-acceptance harness `apps/api-python/tests/load/**` + raw evidence `artifacts/load/**`（gitignored，reproducible via documented command）+ `docs/implementation/reports/T038.md` + `WAVE-25-PARTIAL.md` + state update；T038 = `REVIEW_FAILED`，release_verdict `FAIL`，两轮均多 hard-gate FAIL；root cause = `/api/todos` 无分页双重物化 + SSE drain-bounded 初始事件 + heartbeat UNVERIFIED；production code 0 diff，findings 返回 owning Task；Independent Review CONFIRMED-FAIL；no production code checkpoint (FAIL path)；metadata recorded in this commit）
- T047 remediation: `REVIEW_PASSED`。在批准 write-set 内 remediate T038 REVIEW_FAILED 主因——`GET /api/todos` 无分页、全量 scoped 结果集（11667 todos × 6 表 JOIN，~7.4MB）双重物化。`todos/service.py` `list()` 重写为 SQL-layer 分页：items 查询 `.offset((page-1)*pageSize).limit(pageSize)` + 独立 `select(func.count(ActionItem.id))` 产生 `total`，消除原双 `_rows()` 全量物化与 Python 切片；`_scoped_summary`/`_scoped_owners` 经 SQL aggregate over full scoped set 保留 summary/owners/updatedAt 跨过滤稳定行为；`_schedule_items` bounded top-5 active `LIMIT 5`；order 增 `ActionItem.id.asc()` 确定性 tiebreaker。`todos/schemas.py` `ListTodosQuery`/`ManagerTodoListResponse` 增 `page`/`pageSize`/`total`（与 `RiskQuery` canonical 契约一致，`page=Field(default=1,ge=1)`/`pageSize=Field(default=20,ge=1,le=100)`，additive 保留 summary/owners/schedule/updatedAt/dataScope）。`agent/schemas.py`+`agent/tools.py` 透传 page/pageSize。`packages/contracts/src/index.ts` + 重新生成 `openapi.json`/`openapi.ts`（+38/+8，93 paths/243 schemas/104 ops surface 不变，3 轮 export+gen zero diff）。`apps/web` DashboardView 透传 page/pageSize + filter 重置 page 1 + 分页 footer。真实 PostgreSQL 16 分页验证 10 tests PASS（first/middle/last/empty page、跨页无重复/缺失、status+owner filter + 分页、DataScope 排除 out-of-scope+archived、稳定排序、500-fixture `before_cursor_execute` SQL LIMIT/OFFSET 捕获证明、scoped performance smoke <2.0s 明确标注非 ADR 0032 gate）。Independent Review `APPROVE-WITH-NITS`（3 项 non-blocking nit：`_scoped_owners` 空串理论边界、`list()` 5 round-trips 非 N+1 trade-off、`agent/tools.py` 与既有 `_risk_list` 同 pattern；无 blocking finding；无 disguised-PASS）。Validation：Ruff `All checks passed!`、mypy `Success: no issues found in 204 source files`、focused pytest（todos unit + pagination PG + dashboard + contract compat + auth matrix）`66 passed`、`uv lock --check`（65）、`git diff --check` clean、`@risk-platform/contracts`/`@risk-platform/web` typecheck PASS、web build PASS、post-commit `pnpm contracts:check` clean-tree exit 0。write-set 合规：仅 todos pagination + 必要 contract/frontend/generated artifact（10 modified + 1 new test）；无 schema/index/migration/SSE/scheduler/pool/infra/ADR 改动。**T038 状态不变 = `REVIEW_FAILED`**——首版 release 目标 = 低并发内部使用；full 50-VU ADR 0032 capacity 认证 DEFERRED 至 post-MVP hardening；SSE initial-event/heartbeat、Provider test seam（§9）、connection-pool 调优（先观察分页修复后是否仍独立）、剩余 API/DB load gates 仍 DEFERRED；T038 须在正式 capacity-ready milestone 重跑。原 T038 load evidence 未删除；ADR 0032 threshold 未修改；未声称首版 capacity-certified。本轮未执行 T038 50-VU 重跑认证 / Wave 25 Integration / T039 / SSE remediation / connection-pool 调优 / ADR 0032 修改。Code checkpoint `425c0fde081343f3c74b0c45733deb282a6cdcc6`；metadata recorded in the following report commit。详见 `docs/implementation/reports/T047.md`。
- T047: `425c0fde081343f3c74b0c45733deb282a6cdcc6`（`/api/todos` SQL-layer pagination remediation — LIMIT/OFFSET + 独立 count，消除双物化；canonical page/pageSize/total 契约；OpenAPI re-freeze + openapi.ts 生成；前端 consumer；+10 PostgreSQL 16 分页测试；Independent Review APPROVE-WITH-NITS；contracts:check clean-tree zero diff；T038 仍 REVIEW_FAILED，capacity 认证 DEFERRED；metadata recorded in this commit）
- INTERNAL_MVP / PRODUCTION_CAPACITY_READY release-policy disposition（ADR 0033，design/metadata checkpoint，未实施 production code、未修改 ADR 0032 阈值、未将 T038 改为 PASS、未删除 load evidence、未处理 SSE/pool/perf、未启动任何下一 Task）：正式区分两个发布配置。(1) **INTERNAL_MVP**（首版内部发布）允许继续发布流程，必需 gate 不得降低——功能验收（Wave 1–24 PASS + T001–T047 REVIEW_PASSED）、安全/授权（T037 CSRF/SSRF/Excel/Provider fail-closed/四角色×五数据范围）、审计（哈希链 + append-only DB trigger）、可靠性（幂等 enqueue/crashed-worker/orphan redispatch/outbox→Celery/回滚原子性）、备份恢复（T036 + 真实 PG16 drill，ADR 0009 RPO 24h/RTO 4h 不被低并发违反）、部署（T035 单机内网 Compose 仅 Python API 无 NestJS）；T047 `/api/todos` 分页 remediation 必须保持 `REVIEW_PASSED`（`425c0fd`）；T038 capacity 结果保持真实记录为 `REVIEW_FAILED`/`FAIL`，**不得声称 capacity-certified**；面向低并发内部使用；已知性能限制须进入 release notes/operational limitations。(2) **Deferred findings**（不作为 INTERNAL_MVP blocker，仍作为 PRODUCTION_CAPACITY_READY milestone 正式 blocker，不删除/不 waive/不改 PASS）：full ADR 0032 50-VU 认证（T038 重跑）、SSE 初始事件 p95 ≤2s、SSE heartbeat/transport keepalive、deterministic Provider test seam（ADR 0032 §9）、connection-pool 调优（分页修复后重新测量）、剩余 API/DB capacity gates。(3) **PRODUCTION_CAPACITY_READY**（全量/外部发布）必须：重跑 T038（ADR 0032 reference env/methodology，连续 2 次 hard gate PASS）、全部 required evidence 完整、deferred findings 完成或经正式 ADR 重新决策、随后执行 T039（真实 IMAP/Provider E2E + 恢复演练 + 前端 E2E + Python-only cutover）。(4) **Status/orchestration semantics**：T038 历史结果仍 `REVIEW_FAILED`/`FAIL`，**不覆写**；新增 `DEFERRED_FOR_INTERNAL_MVP` 作为**发布处置 metadata**（非伪造 Task PASS）；T047 记录为已完成 remediation；Wave 25 **不执行 Integration**（T038 未 PASS），记录为 capacity-track **deferred / closed-for-MVP**（非 Integration PASS）；INTERNAL_MVP 范围内**无必需后续 Task**（全部功能 Task 已 REVIEW_PASSED）；**T039 readiness 判断（依据 T039 Task contract，非猜测）**——T039 contract Objective=真实 mailbox/Agent E2E + 恢复 + 前端 E2E + Python-only production cutover，Dependencies=T038 PASS + 全部 design gap 解决 + 外部材料，Stop conditions=缺真实输入或任一前置 FAIL，Acceptance=设计 §11 全部 completion items + approved thresholds PASS——故 **T039 属 capacity/外部认证 track，非 INTERNAL_MVP 必需项**，保持 `DEFERRED`（`BLOCKED_EXTERNAL_INPUTS`，blocked on T038 PASS + external materials），不在 INTERNAL_MVP 启动，待 PRODUCTION_CAPACITY_READY milestone（T038 重跑 PASS 后）执行。**下一可执行 Task**：INTERNAL_MVP 范围内无；capacity/external track 受 PRODUCTION_CAPACITY_READY 显式授权门控，本次不启动。约束：不修改 ADR 0032 数值阈值、不把 T038 改 PASS、不删除 load evidence、不修改 production code、不处理 SSE/pool/perf。详见 `docs/adr/0033-define-internal-mvp-and-capacity-ready-release-profiles.md`。
- ADR 0033 release-policy design checkpoint: `5bb724a6cf54208fd9305d241130beebb8c611a1`（定义 INTERNAL_MVP 与 PRODUCTION_CAPACITY_READY 发布配置及 capacity-track deferred 处置语义；T038 保持 REVIEW_FAILED、增 DEFERRED_FOR_INTERNAL_MVP 发布处置 metadata；Wave 25 closed-for-MVP 非 Integration PASS；T039 判定为 capacity/external track 保持 DEFERRED；无 production code、未修改 ADR 0032 阈值、未删除 load evidence、未处理 SSE/pool/perf、未启动下一 Task；metadata recorded in this commit）
- INTERNAL_MVP release closeout checkpoint: `305706f8f42a6b92223f353e387f0c92dbba643b`（release documentation checkpoint，**仅文档 + release-state metadata**，未修改 production code/tests/ADR 0032/T038 evidence/frozen artifacts）。依据 ORCHESTRATOR.md + ADR 0033 完成 INTERNAL_MVP 发布交接与最终 readiness 核验：(1) working tree clean（HEAD `60f59fb`）；(2) required gates 与 ADR 0033 §1 一致——功能/安全/授权/审计/可靠性/备份恢复/部署 + T047 分页 `REVIEW_PASSED` 全通过；(3) 部署（docker-compose/env.example/init-secrets/TLS/nginx/startup docs）、migration + seed/admin bootstrap、backup/restore one-shot runbook、health checks（PG/Redis/API/worker/scheduler ✓；proxy/web 无独立 healthcheck = 非阻断观察）、rollback/restore/operator handoff 步骤完整；(4) `/api/todos` 源码核实使用 T047 SQL-layer 分页契约；(5) `pnpm contracts:check` clean-tree zero diff（93 paths/243 schemas）；(6) release notes 如实披露 limitations（INTERNAL_MVP=低并发内部使用、NOT capacity-certified、T038 remains FAIL/deferred、T039 remains deferred、T047≠T038 PASS、SSE initial-event/heartbeat 未认证、Provider seam 未完成、connection-pool/50-VU capacity gates deferred）；(7) secret-leak 核验——git-tracked 树无 `.env`/private key/TLS material/session key/backup KEK/secret literals/plaintext data（certs+secrets gitignored，仅占位符）。生成三份 release artifact：`docs/implementation/release/INTERNAL-MVP-RELEASE-NOTES.md`、`docs/implementation/release/INTERNAL-MVP-OPERATOR-HANDOFF.md`、`docs/implementation/reports/RELEASE-INTERNAL-MVP-CLOSEOUT.md`。**release readiness = READY，无 release blocker**；T038 保持 `REVIEW_FAILED`/`DEFERRED_FOR_INTERNAL_MVP`（未改 PASS、未声称 certified）；T039 保持 `DEFERRED`/`BLOCKED_EXTERNAL_INPUTS`；Wave 25 = capacity-track deferred/closed-for-MVP（非 Integration PASS）。未执行 T038/T039/新 Wave/SSE-pool hardening；未修改 ADR 0032/T038 evidence/frozen artifacts/production code。Release recommendation：可按 INTERNAL_MVP（低并发内部使用）发布，不得作为公网/高并发/外部 Provider 生产发布。metadata recorded in the following SHA-record commit）
