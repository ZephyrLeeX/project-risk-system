# T009 实施报告

- **Task:** T009 — Migrate authentication and sessions
- **Status:** IMPLEMENTED
- **Review:** REVIEW_PASSED
- **Integration:** Wave 5 PASS

## Changed

- 新增 `/api/auth/login`、`/session`、`/change-password`、`/logout`，保持 legacy envelope、
  Cookie、Origin/CSRF 和 UTC 固定毫秒 `expiresAt` contract。
- 实现 Argon2id、密码策略、首次登录强制改密、session hash/revocation、锁定与 PostgreSQL
  `FOR UPDATE` 并发计数；成功/失败路径使用 typed metadata-only audit。
- `SESSION_SECRET_FILE` 指向只读密钥文件；production 缺失时 fail closed，密钥至少 32 bytes。
  Token 与 client IP 使用 server-owned key 的 domain-separated HMAC-SHA-256。
- 修改 auth module、`config.py`、`.env.example`、auth/HTTP tests；无 migration/frontend 写入。

## Validation

- `uv lock --check`：PASS（55 packages）
- Ruff：PASS；mypy：PASS（62 source files）
- PostgreSQL 16 focused：PASS（25 passed）
- PostgreSQL 16 full：PASS（111 passed）
- `git diff --check`：PASS

## Review / Contract / Risk

- 初审 `DESIGN_DEVIATION`：缺少 server-owned session key，以及 `expiresAt` 非固定毫秒。
  两项均按批准设计修复，独立复审关闭。
- API contract、positive/negative、lock expiry/concurrency、forced change/revocation、secure Cookie、
  secret negative cases 与 failed-action audit：PASS。
- Database impact：无 schema/migration；复用 `users`、`sessions`、`audit_logs`。
- T010/T040 后续需显式组合 auth router/service；真实 reverse-proxy TLS E2E 留部署 Task。
- `DESIGN_GAP`：无；未批准的 `DESIGN_DEVIATION`：无；Blocker：无。
