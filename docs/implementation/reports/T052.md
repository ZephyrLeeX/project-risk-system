# T052 Implementation Report

- **Status:** REVIEW_FAILED
- **Mapped V2 Task:** Task 5 — 前端/Admin 收口 + 旧架构清理 + 全量 E2E

## T052 remediation run — Scope policy production regression (2026-08-18)

本轮继续处理 T052 的 production functional regression；未创建 Task 6，未修改
TASK_GRAPH、EXECUTION_STATE 或 Project status。

- `ScopePolicy` 现在只判断请求是否属于系统/项目数据领域，不再因 mutation intent
  直接 `OUT_OF_SCOPE`。proposal catalogue、RBAC/data scope、strict schema、
  `MutationDraft`、`WRITE_CONFIRMATION` 与 server-only commit boundary 保持不变。
- 回归覆盖五个 SYSTEM_DATA_ONLY mutation case、三个 general-chat negative case，
  以及真实用户示例到达 Provider/tool loop 且不返回 `OUT_OF_SCOPE_MESSAGE`。
- 新增 PostgreSQL acceptance：fake model 返回 `risk_create_proposal`，经真实
  `AgentToolRegistry` 生成 `MutationDraft` 和 `WRITE_CONFIRMATION`；确认前断言
  `Risk` 表行数不变。当前环境没有 `TEST_DATABASE_URL`/`DATA_ENCRYPTION_KEY`，该项诚实
  skip，未伪造通过。

本轮验证：Agent focused `21 passed, 1 skipped`；Agent 集合 `28 passed, 5 skipped`；
mypy（Agent + 新增 acceptance）PASS；Ruff PASS；FastAPI full pytest `346 passed,
165 skipped`。browser E2E、真实 PostgreSQL/Redis suite、Compose worker/SSE journey
和 DeepSeek live gate 尚未执行：当前环境缺少 `node`/`pnpm`、数据库/Redis 注入和凭据，且
仓库现有 README 引用的 `e2e/infra/run.sh` 不存在。T052 仍保持 `REVIEW_FAILED`，待具备
外部输入后继续原定 browser mutation/cancel/batch/reconnect/replay/scope-negative 与
独立 Review，不宣称最终通过。

## T052 REVIEW_FAILED remediation run (2026-08-17)

本轮仍只处理 T052 blocker，未创建 Task 6，未开始新阶段，未修改 TASK_GRAPH/EXECUTION_STATE 完成状态。

- production composition 已删除 `AgentProviderAdapter`、`build_provider()`、旧 `agent.execution` provider 类型及 `merge_worker_handlers(... provider ...)` 参数；worker 只组装 `ProviderV2Runtime` + `NativeAgentExecutionWorker`。
- `apps/api-python/src/risk_platform/agent/execution.py` 的 V1/fixed PLAN-RESPOND JSON execution implementation 已删除；confirmation compatibility service 的 canonical/scope/category 校验改为本模块本地实现。现有 V2 core/provider/runtime/interaction/mutation 测试继续覆盖 durable/security 约束。
- `/api/agent/confirmations/{token}` 未删除：ADR 0019 明确冻结该 public compatibility contract，当前没有批准的 retirement authority。endpoint 已标记 `deprecated`，并在 OpenAPI 描述中注明：Vue active path、V2 Core/Interaction/Mutation、worker 均为 zero consumer；新写操作使用 `AgentInteraction.respond`。Vue `rg` 审计无该 endpoint 调用。
- `agent_executions.updatedAt` drift 结论为 **A：旧长期 Compose DB 的 stale local environment**。fresh 隔离 `t052-fresh` Compose PostgreSQL volume 已从 0001→0013 完整升级，`alembic check` = `No new upgrade operations detected`；未修改业务 migration，临时 Compose 文件和 volume 已清理，现有用户数据卷未删除。

本轮验证：FastAPI `335 passed, 164 skipped`；Ruff、mypy（235 source files）、`uv lock --check`、compileall、`git diff --check` PASS；contracts/web typecheck、web tests `51 passed`、production build PASS；fresh Alembic PASS。现有浏览器 artifact 的 real Compose edge smoke 为 `2 passed`，Admin browser path 仍 `BLOCKED_EXTERNAL_INPUTS`（未提供凭据）。真实 DeepSeek live smoke 仍 `BLOCKED_EXTERNAL_INPUTS`；fake DeepSeek full browser journey、完整 Admin seeded browser E2E、当前 patched image 的全量 Compose E2E 尚未重新执行，因此本 remediation run 尚不能改为 `REVIEW_PASSED`。

## Independent Review

结论：`REVIEW_FAILED`。本轮只执行 Task 5，未创建 Task 6；未创建 T052 final checkpoint，也未将 T052、Task 5 或项目状态标记为完成。

启动核对：T051=`REVIEW_PASSED`；code checkpoint=`2696e1cca23d2ba7dc331f8dd3373e1886d4d34a`；metadata commit=`1082cc3f8ea9d1ef777592d7937c88d030902af0`；T052=`READY / NOT_STARTED`。

## 实施摘要

- Admin 已切换到 generated OpenAPI 驱动的 Provider Account + Model Config 页面，仅展示/创建 `DEEPSEEK_OFFICIAL`；官方 endpoint 由后端固定，未提供 Company API 或自定义 endpoint 输入；支持 health、default、priority、timeout、启停和 key rotation。
- Agent 前端已消费 `PROJECT_SELECTION`、手工输入、durable interaction resume/WAITING、`WRITE_CONFIRMATION` editable fields、CandidateRisk basis 展示、批量选择、取消和 reconnect；旧 preview/confirmation UI/runtime 调用已从 Vue active path 移除。
- 增加 Playwright browser harness，目标为真实 Compose edge；无 credential 时只报告 `BLOCKED_EXTERNAL_INPUTS`。真实合成环境 public/login Agent smoke `2 passed`；Admin credential 未提供，`1 skipped`，未伪造 PASS。
- T051 报告中的 worker/SSE 失败已定位为 integration test double 与 V2 worker invocation signature 不兼容；通过 signature-aware fixture compatibility 修复，完整 PostgreSQL suite 随后 `520 passed, 1 skipped`。

## Validation evidence

- FastAPI full suite（真实 PostgreSQL/Redis 环境）：`520 passed, 1 skipped`。
- Ruff、mypy、`uv lock --check`：PASS；mypy `237 source files`。
- fresh PostgreSQL Alembic：upgrade 到 `20260817_0013 (head)`，`alembic check` PASS。
- OpenAPI export/generate 连续 3 轮：zero diff；contracts/web typecheck、frontend 51 tests、production build：PASS。
- Compose API health、worker task discovery、proxy SSE buffering 配置 smoke：PASS；worker 注册 `risk_platform.reliability.execute`。
- Browser E2E：真实 Compose edge public/login/Agent smoke `2 passed`；Admin 及真实 DeepSeek credential/live mutation flow 为 `BLOCKED_EXTERNAL_INPUTS`，未宣称全量 E2E PASS。
- 当前运行 Compose 数据库的 `alembic check` 另发现 `agent_executions.updatedAt` server-default drift；fresh DB gate 不受影响，但该运行态 drift 仍需后续 integration remediation。
- 一次复跑使用了错误的本地 PostgreSQL 端口，产生连接错误；不作为业务回归结论，已保留并以已完成的真实 DB suite 结果为准。

## Legacy audit / blocker

V1 Agent provider wiring、固定 PLAN/RESPOND execution implementation 和 legacy provider snapshot 已从 production path 删除；mail/weekly 仍需要的 `AiProviderClient`、legacy provider service/table 保留。confirmation endpoint 按 ADR 0019 保留为 deprecated compatibility surface，不是 V2 runtime dependency。当前唯一未关闭的 Review blocker 是重新执行 patched Compose image 的 Admin seeded browser E2E、fake DeepSeek full journey 及其余完整 Compose gates；缺真实 DeepSeek credential 继续记录 `BLOCKED_EXTERNAL_INPUTS`，不伪造 vendor PASS。

项目 status 继续保持现有 DESIGN_GAP fail-closed；未新增 DELIVERY/COMPLETED/ARCHIVED transition。
