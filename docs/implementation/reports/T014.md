# T014 实施报告

- Task：T014 — Migrate AI Provider administration
- 结果：`IMPLEMENTED`；Independent Review：`REVIEW_PASSED`
- 范围：新增 Python `ai_providers` schemas、真实连接 client、service、API routes 与安全契约测试；未修改 migration、共享 bootstrap、T013 或其他 Task write-set。
- API：实现 `/api/admin/ai-services` summary、strategy、usage、calls、call detail、provider CRUD、key rotation、status/default、test/test-draft/test-all。
- 安全：复用 T007 `SecretCipher` 和 `OutboundEndpointGuard`；数据库仅保存加密 envelope 与 key suffix，响应只返回掩码；连接请求 revalidate DNS、禁止重定向，并报告真实上游失败，不生成 mock 成功。
- 事务与审计：provider mutation、测试状态、metadata-only call log 和成功审计在同一 PostgreSQL transaction；调用日志不保存 key、prompt、模型原文或业务正文。
- Database / migration impact：复用既有 `ai_provider_configs`、`ai_call_logs` 表，未新增 Alembic revision。
- Tests：新增 3 个契约/加密单元测试。
- Validation：定向 Ruff `PASS`；定向 mypy `PASS`；定向 pytest `3 passed`；`git diff --check` `PASS`。完整 pytest 为 `101 passed, 1 failed, 32 skipped`，唯一失败是已完成 T013 测试 `test_publish_contract_accepts_exact_three_levels` 的既有 T013 schema/test 不一致，未修改 T013。PostgreSQL validation 因 `TEST_DATABASE_URL` 未配置而 `skipped`，未使用 SQLite。
- Independent Review：按 T014、ADR 0005/0008/0011/0014/0015、legacy controller/service/client、contracts 与核心模型复核；确认权限、密钥不回显、出站限制、真实失败、选择策略、审计和 write-set，无 unresolved `DESIGN_DEVIATION`。
- Checkpoint：待提交后记录 SHA。
