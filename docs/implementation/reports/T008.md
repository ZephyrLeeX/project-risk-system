# T008 实施报告

- **Task:** T008 — Build durable Celery reliability core
- **Status:** IMPLEMENTED
- **Review:** REVIEW_PASSED
- **Integration:** Wave 6 partial PASS

## 实施摘要

- 新增可靠性模块的闭合 task registry、Celery/Redis 配置和 JSON-only worker entrypoint。
- 实现 PostgreSQL transactional outbox 的 enqueue/publish-after-commit 边界；broker 发布失败时不标记
  `publishedAt`，消息仅携带 `task_id` 与 `dispatch_generation`。
- 实现基于 expected status、dispatch generation 和 lease token 的 claim、heartbeat、完成/失败/重试
  fencing；重复投递只能被一次成功 claim 吸收。
- 实现有界尝试次数、指数退避和基于 PostgreSQL 的过期 lease/retry/outbox reconciliation。
- 新增 broker 故障、最小消息载荷、registry 闭合和 Celery result backend 禁用测试。

## 修改文件

- `apps/api-python/src/risk_platform/reliability/{__init__,celery_app,core,dispatcher,registry}.py`
- `apps/api-python/pyproject.toml`
- `apps/api-python/tests/reliability/test_dispatcher.py`
- `.env.example`

## Contract / database impact

- `/api` contract：无变化；无新增 migration。
- PostgreSQL：复用 T041 的 `durable_tasks` 与 `task_outbox`，不建立第二套任务事实表。
- Redis/Celery：仅作为 at-least-once 消息传输；任务状态、幂等、lease 和恢复事实均在 PostgreSQL。

## Validation

- `ruff check .`：PASS
- `mypy .`：PASS（67 source files）
- `pytest -ra`：PASS（89 passed, 25 skipped）
- PostgreSQL/Redis/Celery 外部集成测试：环境未提供 `TEST_DATABASE_URL`，相关 PostgreSQL 测试按协议跳过；
  未伪造外部服务成功。

## Acceptance / risk

- broker outage、duplicate-safe claim、lease expiry/reconciliation、restart recovery 的实现路径已审查；
  focused reliability tests PASS。
- `DESIGN_GAP` / `DESIGN_DEVIATION`：无。
- Wave 6 仍未完成：T010 继续保持 `READY`，T004 继续保持 `BLOCKED_DESIGN_GAP`。
