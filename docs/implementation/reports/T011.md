# T011 实施报告

- **Task:** T011 — Migrate user administration
- **Status:** REVIEW_PASSED
- **Review:** REVIEW_PASSED（初审发现敏感写失败路径未审计；最小修复后独立复审通过）

## 实施摘要

- 新增 T011 专属 `admin/users` 模块，实现 `/api/admin/users*` 的查询、汇总、详情、创建、更新、
  启停、解锁、密码重置和项目范围分配。
- 初始密码与重置密码仅在相应成功响应中返回一次；禁用与重置均撤销会话。
- 项目范围写入在单一业务事务中校验并持久化；成功审计与业务写入同一事务。
- 敏感写操作失败先回滚业务事务，再以独立事务写入 ADR 0017 metadata-only failure audit；审计不含
  密码、payload 或 snapshot。

## 修改文件

- `project-risk-system/apps/api-python/src/risk_platform/admin/users/__init__.py`
- `project-risk-system/apps/api-python/src/risk_platform/admin/users/api.py`
- `project-risk-system/apps/api-python/src/risk_platform/admin/users/policy.py`
- `project-risk-system/apps/api-python/src/risk_platform/admin/users/schemas.py`
- `project-risk-system/apps/api-python/src/risk_platform/admin/users/service.py`
- `project-risk-system/apps/api-python/tests/admin/users/test_admin_users.py`

## Validation

- `ruff check .`：PASS。
- `mypy .`：PASS（81 source files）。
- `uv lock --check`：PASS。
- 临时隔离 PostgreSQL 16 的 `pytest -ra`：PASS（124 passed）。
- `git diff --check`：PASS。

## Contract / Review

- `/api` contract：保持 legacy/admin frontend 的 user administration path、method、permission 与响应
  边界；未修改 TypeScript contracts。
- PostgreSQL：未新增 migration；T011 专属 PostgreSQL transaction/audit regression tests 覆盖成功与
  self-disable、无效项目范围、用户名冲突失败路径及回滚。
- `DESIGN_GAP` / `DESIGN_DEVIATION`：无。
- 初审 `REVIEW_FAILED`：六类敏感写操作失败未审计。修复后独立复审确认 failure audit 的事务、
  metadata-only 边界与密码不泄露均符合 ADR 0008，结果 `REVIEW_PASSED`。
