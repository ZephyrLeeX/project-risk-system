# T018 实施报告

- **Task：** T018 — Migrate import confirmation, history and download
- **结果：** `IMPLEMENTED`
- **Independent Review：** `REVIEW_PASSED`
- **Checkpoint：** `b8a5a5f`（`backend: complete T018 import commit history and download`）

## 实施摘要

在现有 Python `imports` bounded module 内补齐项目导入确认、批次历史/详情、源文件下载、项目选项和补充回款匹配/解除匹配。确认流程对批次加 PostgreSQL 行锁，在一个事务内写入项目、补充/法务行，并通过 T022 的 `RisksService.create_in_session` 复用风险、待办、时间线和审计写入，避免导入模块复制领域逻辑。重复确认对已 `IMPORTED` 批次幂等返回，不重复创建业务事实；错误行和未确认警告阻止提交；下载路径继续经过受限私有存储校验。

## 修改文件

- `project-risk-system/apps/api-python/src/risk_platform/imports/api.py`
- `project-risk-system/apps/api-python/src/risk_platform/imports/commit_service.py`
- `project-risk-system/apps/api-python/src/risk_platform/imports/schemas.py`
- `project-risk-system/apps/api-python/tests/imports/test_commit_contract.py`

## API / database impact

- 新增兼容路由：`GET /api/imports/project-list/batches`、详情、source download、project options、supplemental match/unmatch、`POST .../confirm`。
- 保持 `admin.import.manage` 权限、响应 envelope、camelCase 字段、状态/分页/下载 Content-Disposition 语义。
- 不新增 migration；复用既有 `import_batches`、三类 import row、projects、risks、action_items、risk timeline 和 audit 表。
- 确认事务按 batch `FOR UPDATE` 串行化；风险写入复用 T022 服务并共享事务。

## Independent Review

Reviewer 根据 T018、ADR 0004/0008/0011/0012/0015、T017/T022 边界、legacy controller/service、前端 contract 和本次 diff 复核：write-set 仅限 imports；T024/DG-10 未被读取或修改；没有复制风险/待办/时间线 mutation；确认、匹配和下载保留权限与资源状态检查；没有发现 API contract、数据库架构或架构偏离。结果：`REVIEW_PASSED`。

## Validation

| 检查项 | 结果 |
|---|---|
| Ruff | `PASS` — `/home/lijx/.local/bin/uv run --frozen ruff check .` |
| mypy | `PASS` — `/home/lijx/.local/bin/uv run --frozen mypy .` |
| focused pytest | `PASS` — `8 passed`（imports + risks regression） |
| `uv lock --check` | `PASS` |
| `git diff --check` | `PASS` |
| PostgreSQL-specific validation | `PASS` — 临时隔离 PostgreSQL 16，既有 Alembic/schema 专项 `7 passed`；临时容器已停止；未使用 SQLite |

由于执行环境未安装 `mise`，验证使用 repository 已安装的绝对路径 `/home/lijx/.local/bin/uv`；未修改依赖或 lockfile。

## 风险与状态

- 无 `DESIGN_GAP`、`DESIGN_DEVIATION` 或未解决的 T018 blocker。
- T018 明确不包含 rollback/file expiry；T024 及 DG-10 未执行、未解决。
