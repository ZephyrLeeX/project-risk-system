# T017 实施报告

- **Task：** T017 — Migrate Excel upload and preview worker
- **结果：** 实现完成；Independent Review：`REVIEW_PASSED`。
- **实施摘要：** 新增标准库 `.xlsx` ZIP/XML 解析器，覆盖主表、补充回款表和法务表；新增 20MB、扩展名、ZIP、路径和私有文件存储校验；新增 `IMPORT_PREVIEW` durable task 创建边界、Celery handler 适配器、Worker 行结果持久化、失败批次标记和临时文件清理路径。
- **修改文件：** `project-risk-system/apps/api-python/src/risk_platform/imports/{api.py,parser.py,service.py,storage.py,tasks.py,worker.py}`、`project-risk-system/apps/api-python/tests/imports/test_parser.py`。
- **API / database impact：** 提供 `/api/imports/project-list/preview` 上传路由；复用既有 `import_batches`、三类 row 表、`durable_tasks`/`task_outbox`，未新增 migration，payload 仅保存 `batch_id`、`storage_key` 等小型标识符。
- **安全与兼容：** 不把 workbook 内容写入 task payload；响应字段使用现有前端兼容的 camelCase；行级校验错误保持 `PREVIEWED`，解析/Worker 异常才标记 `FAILED`，正式项目版本不被修改。
- **Validation：** `uv lock --check` `PASS`；T017 定向 Ruff `PASS`；T017 定向 mypy `PASS`；T017 focused pytest `3 passed`；full pytest `107 passed, 33 skipped, 1 failed`。唯一 failure 是已知 T013 `system_config` regression，保留为 Wave 7 Integration 待处理项；`git diff --check` `PASS`。
- **PostgreSQL：** T017 当前没有独立 PostgreSQL-specific test，记为 `N/A`；full suite 中其他 PostgreSQL tests 因未配置 `TEST_DATABASE_URL` `skipped`，未使用 SQLite。
- **Review：** 按 T017、ADR 0006/0008/0011/0014/0015/0018、既有前端 import contract 与 legacy parser 复核；确认 write-set、20MB 限制、三表解析、task payload 边界、失败状态和 duplicate-safe enqueue，无 unresolved `DESIGN_DEVIATION`。
- **风险 / blocker：** 已知 T013 full-suite regression 仍待 Wave 7 Integration；本次未修复。无新的 T017 validation failure。

## Checkpoint

- **Checkpoint commit：** `7c4b2e60f1b54ad11c18caeae6a65a440026e9db`（`backend: complete T017 Excel preview worker`）。
