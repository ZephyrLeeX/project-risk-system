# T049 实施报告

状态：`IN_PROGRESS`

## 已完成

- 用户已明确启动 T049，并完成最小 ADR reconciliation：ADR 0035 仅冻结 Scope Policy、只读 native Tool Loop、limits、grounding、Provider/Core 边界、error taxonomy 和 durable/SSE 边界。
- 尚未完成 production Agent Core、Tool Registry、migration 或 API 变更；未开始 T050–T052。

## 当前验证

- `/home/lijx/.local/bin/mise exec uv@0.12.3 python@3.12.13 -- uv run --frozen ruff check src/risk_platform/agent`：`PASS`。
- 已从指定 HEAD `bc65e6ce5f7190d5e32d9b47ce47ac73efd64a4b` 运行 Agent/V2/PostgreSQL focused suite。启用仓库已有的 localhost-only PostgreSQL/Redis 测试 override 后，结果为 `58 passed, 8 failed`。
- 8 个失败逐项分类：
  - `test_real_module_local_worker_success_preview_invalid_timeout_and_cancellation`：`B`，仍使用 legacy `AgentExecutionWorker`、旧 Provider snapshot 和 preview/mutation 断言；不得恢复，需迁移到 Native worker/V2 durable success/retry/cancel 断言。
  - `test_grounding_execution_fails_without_a_tool_and_uses_fixed_empty_result_answer`：`B`，断言旧 PLAN/RESPOND grounding orchestration；改为 `ReadOnlyAgentCore` native tool-call/no-fact fail-closed 行为。
  - `test_tool_implementation_error_is_not_misclassified_as_config_invalid`：`A`，错误层次仍有效；需通过 Native worker + V2 Tool execution 验证 `AGENT_INTERNAL_ERROR`。
  - `test_real_worker_heartbeat_and_backpressure_are_persisted_and_fail_closed`：`A`，heartbeat/backpressure durable 不变量仍有效；需迁移 Native worker。
  - `test_slow_tool_obeys_attempt_deadline_heartbeat_and_cancellation`：`A`，deadline/heartbeat/cancellation 不变量仍有效；需迁移 Native worker。
  - `test_postgresql_sse_resume_cursor_and_asgi_framing`：`A`，SSE resume/keepalive 不变量仍有效；其旧 fixture 还直接制造违反 `retry_schedule_state` 的 RUNNING 事实，已修正 fixture，后续仍需以 V2 execution 验证。
  - `test_postgresql_category_stale_uses_durable_retry_budget_and_rebuilds_options`：`C`，属于 T030 preview/mutation execution；T049 不恢复，保留在 legacy confirmation compatibility 范围之外的后续 Task 验证。
  - `test_enum_values_and_single_alembic_head`：`A`，是当前 migration fact source 回归；已将旧 `0010` 期望同步为 repository 当前 head `20260817_0011`。
- 当前尚未完成 legacy test baseline 到 V2 的迁移；因此不能声称 focused suite、全量验证或 Independent Review 通过，也未创建 checkpoint、未更新 T049 完成状态、未开始 T050。
- 修正 SSE durable fixture 与 Alembic head 断言后，旧 `test_agent_execution.py` 当前为 `13 passed, 6 failed`；剩余 5 个 legacy worker/V1 orchestration 失败和 1 个 T030 preview/mutation 失败，正是本轮需要迁移/移出 T049 的基线。
- V2 迁移目标子集 `test_v2_core.py + test_capability_schema.py + test_provider_v2_runtime.py + test_provider_v2_postgresql.py` 当前为 `36 passed`。
