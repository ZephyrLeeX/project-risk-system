# T049 实施报告

状态：`REVIEW_PASSED`

## 实施摘要

- 5 个 legacy worker/V1 失败测试已迁移到 `NativeAgentExecutionWorker` 与 PostgreSQL durable lifecycle：success、tool error→`AGENT_INTERNAL_ERROR`、Provider transient→`RETRY_WAIT` 且不写 terminal `ERROR`、retry 后成功、heartbeat、backpressure、attempt deadline、cancellation、lease fencing、terminal monotonic。
- 保留并验证 V2 `ReadOnlyAgentCore`、`ProviderV2Runtime`、native tool loop、SSE resume/reconnect/keepalive；V2 catalogue 不暴露 `preview`、`project_list` 或 mutation tool。
- T030 的 preview/category-stale mutation orchestration 测试未恢复，改为 V2 read-only boundary 测试；category revalidation/mutation `deferred to T051`。
- `structured` Agent message schema/generated contract 基线已同步；测试 key 尾部空白与 parser metadata-only timing 的测试基线已收口，不改 production mailbox 语义。

## 6 个旧失败测试处理

| 原测试 | 最终处理 |
|---|---|
| `test_real_module_local_worker_success_preview_invalid_timeout_and_cancellation` | legacy runtime 测试不再收集；success/retry/cancel/terminal facts 由 Native worker durable 测试覆盖，preview/mutation 断言移出。 |
| `test_grounding_execution_fails_without_a_tool_and_uses_fixed_empty_result_answer` | 不恢复 PLAN/RESPOND；由 V2 Core native tool/no-fact fail-closed 测试覆盖。 |
| `test_tool_implementation_error_is_not_misclassified_as_config_invalid` | 迁移为 Native worker tool exception，断言 `AGENT_INTERNAL_ERROR` 与安全 error event。 |
| `test_real_worker_heartbeat_and_backpressure_are_persisted_and_fail_closed` | 迁移为 Native worker durable heartbeat/backpressure；backpressure 写 terminal error fact。 |
| `test_slow_tool_obeys_attempt_deadline_heartbeat_and_cancellation` | 迁移为 Native worker attempt deadline + heartbeat；Provider transient 进入 `RETRY_WAIT`，不写 terminal error。 |
| `test_postgresql_category_stale_uses_durable_retry_budget_and_rebuilds_options` | T030 旧 mutation/category-stale orchestration 不恢复；改为 V2 read-only catalogue boundary，真正 category revalidation deferred to T051。 |

## Quality gates

- Agent/V2 + PostgreSQL durable/SSE focused：`24 passed`。
- Full backend pytest（PostgreSQL 16 + Redis 7）：`513 passed, 1 skipped`；skip 为 audit query export 的既有 DB 专项分工。
- Ruff、mypy（230 source files）、`uv lock --check`（65 packages）、`git diff --check`：`PASS`。
- Fresh PostgreSQL schema：Alembic `upgrade head` + `alembic check`：`PASS`，head `20260817_0011`。
- OpenAPI export/generation、contracts typecheck、frontend typecheck：`PASS`。
- Production wiring static review：`composition.py` 仅注册 `native_agent_execution_handlers` + `ReadOnlyAgentCore(ProviderV2Runtime, ...)`；旧 PLAN/RESPOND parser 不在 production V2 handler path，未恢复旧 worker、action JSON、legacy snapshot、`project_list` 或 preview/mutation orchestration。

## Independent Review

`REVIEW_PASSED`。Review 覆盖 Provider/Core 解耦、native loop、scope-before-tool、read-only catalogue、durable task/event/SSE、error taxonomy、retry/lease fencing、backpressure、terminal monotonic 与 T030 deferred boundary；无 blocking finding。

## Checkpoint / handoff

- Code checkpoint：`3813163fca3d076c08a8ec9f5b5a69c9016c9678`。
- T049：`REVIEW_PASSED` / `COMPLETED`。
- T050：`READY`，未开始；本轮到此停止。
