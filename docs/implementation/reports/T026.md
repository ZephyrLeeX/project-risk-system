# T026 实施报告

- **Task：** T026 — Extract, review and publish mail risk candidates
- **结果：** `DESIGN_GAP`
- **Independent Review：** `NOT_STARTED`
- **Checkpoint：** 无（未产生代码变更）

## 阻塞原因

`DESIGN_GAP`：ADR 0022 要求 T026 在每次执行/重试时按
`(mailbox_config_id, uid_validity, imap_uid)` 重新抓取 source，同时 T026 的输入不能携带
raw source；ADR 0024 只规定 T025 的附件解析边界。目前批准来源没有定义 T026 可向 Provider
提交的、从该 source 派生的最小内容契约（正文与附件衍生文本是否可合并、上限、以及该内容是否
必须在 T026 中重新执行 T025 parser），也没有定义该 AI 输入的版本/可审计标识。现有 NestJS
实现会将正文与附件衍生文本直接放入 prompt，但它是历史参考，不能作为新的生产安全契约。

该决策影响邮件内容最小留存、附件安全边界和 Provider 调用的可重复性；不能由实现自行选择。
因此未开始代码、Independent Review、PostgreSQL validation 或 checkpoint。

## 未执行项

- 未修改 production code、migration、API contract 或依赖锁。
- 未处理 DG-04，未执行 T042，未启动 Wave 11 Integration 或下一 Wave。
