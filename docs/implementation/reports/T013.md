# T013 实施报告

- Task：T013 — Migrate versioned system configuration
- 结果：`IMPLEMENTED`；Independent Review：`REVIEW_PASSED`
- 范围：新增 Python `system_config` schemas、service、API routes 与契约测试；未修改 migration、共享 bootstrap、历史 NestJS 实现或其他 Task write-set。
- API：实现 `/api/admin/system-config` overview、`project-options`、`publish`、`releases`、`releases/{id}`，保留 `ApiResponse`、权限 `admin.config.manage`、字段与状态语义。
- 事务与审计：发布在同一 PostgreSQL 事务内更新风险类别、等级规则、项目别名、不可变版本快照和成功审计；失败回滚后使用独立事务记录失败审计。别名按 NFKC、大小写折叠、去空白规范化并保证唯一。
- Database / migration impact：复用 T003 核心表和既有 `system_config_releases`，未新增 Alembic revision。
- Tests：新增 3 个契约/规范化单元测试，覆盖三级风险等级、非法等级集合和编码/别名规范化。Wave 7 Integration 中确认原 fixture 使用单字显示名与当前 `min_length=2` contract 不一致，已最小更新为“高风险/中风险/低风险”；该测试单独 `PASS`，未修改 T013 production code。
- Validation：Wave 7 Integration 中 T013 regression focused pytest `PASS`（1 passed）；隔离 PostgreSQL full suite、full Ruff/mypy 的结果与 blocker 详见 `WAVE-07.md`。未使用 SQLite 替代。
- Independent Review：按 T013、ADR 0004/0008/0011/0015、legacy controller/service、contracts 与核心模型复核；确认 write-set、权限、事务原子性、快照不可变性和失败审计路径，无 unresolved `DESIGN_DEVIATION`。
- Checkpoint：`13463f3`。
- Wave 7 Integration：T013 regression 已解除；带隔离 PostgreSQL 的全量 validation 被既有 audit migration 的 schema/search_path 问题阻断，详见 `WAVE-07.md`。本次无代码修复，T013 原 Independent Review 继续有效。
