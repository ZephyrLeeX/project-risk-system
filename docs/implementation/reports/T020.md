# T020 实施报告

- **Task：** T020 — Migrate dashboard project, collection and risk queries
- **结果：** `REVIEW_PASSED`
- **代码 checkpoint：** `8a0297ba1eaa5d72432a442cbda746d0ff480075`

## remediation 结果

完成 dashboard summary/focus、风险列表/详情/options，以及以下既有只读 `/api` 契约：

- `GET /dashboard/departments/collections`
- `GET /dashboard/departments/{departmentKey}/collections`
- `GET /dashboard/collections`
- `GET /dashboard/collections/{projectId}`

所有项目、聚合、部门/回款详情均使用统一项目范围过滤。空金额保持 `null`；项目清单金额不完整时，已确认关联的 supplemental 回款按既有规则作为后备来源。collection `level` 参数恢复为 `ProjectRiskLevel` enum 校验，无效值返回 `422`。

## Independent Review

初审发现 collection `level` 被实现成无约束字符串，未保留 legacy enum validation。已修复为 `ProjectRiskLevel | None` 并增加无效值 `422` 回归测试；独立复审结果：`REVIEW_PASSED`。

## validation

| 命令 / 检查 | 结果 |
| --- | --- |
| PostgreSQL 16 focused `pytest -ra tests/dashboard/test_dashboard_queries.py` | `PASS` — 7 passed |
| `ruff check .` | `PASS` |
| `mypy .` | `PASS` |
| `uv lock --check` | `PASS` |
| `git diff --check` | `PASS` |

PostgreSQL 使用临时、无持久数据的 PostgreSQL 16 实例；测试完成后已停止并删除。
