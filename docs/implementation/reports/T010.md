# T010 实施报告

- **Task:** T010 — Migrate RBAC and project data scopes
- **Status:** IMPLEMENTED
- **Review:** REVIEW_PASSED
- **Integration:** Wave 6 partial PASS

## 实施摘要

- 新增可复用 `require_permissions` / `has_all_permissions` FastAPI permission guard；权限校验与数据范围校验保持独立。
- 新增统一 PostgreSQL project-scope predicate，支持且仅支持 `ALL`、`OWNED`、`ASSIGNED`、`OWNED_OR_ASSIGNED`、`NONE`；不读取部门关系，所有范围排除 `ARCHIVED` 项目。
- 新增 scoped list/detail/existence helpers；越权或归档项目返回无记录，避免详情存在性泄漏。
- 新增权限负向测试、PostgreSQL predicate 编译测试和五种 scope matrix 集成测试。

## 修改文件

- `apps/api-python/src/risk_platform/rbac/{__init__,guards,scopes}.py`
- `apps/api-python/tests/rbac/test_rbac.py`

## Contract / database impact

- `/api` contract：无变化；无新增 migration。
- PostgreSQL：复用现有 `projects`、`user_project_scopes` 和 `DataScopeType`；查询统一使用可组合 scope predicate，无重复 join/count 扩大结果集。
- 未修改 frontend、NestJS、Prisma、TypeScript contracts、ADR 或冻结设计。

## Validation

- `ruff check .`：PASS
- `mypy .`：PASS（70 source files）
- `pytest -ra`：PASS（92 passed, 26 skipped）
- PostgreSQL scope matrix：当前未配置 `TEST_DATABASE_URL`，1 项按协议跳过；未伪造数据库验证成功。

## Acceptance / review

- 权限依赖的 all-of 语义、五种范围 predicate、归档排除及越权详情无存在性泄漏：PASS。
- `DESIGN_GAP` / `DESIGN_DEVIATION`：无。
- T008 不重做、不重新 Review；T004 blocker 原样保留。
