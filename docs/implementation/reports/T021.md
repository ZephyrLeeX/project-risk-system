# T021 实施报告

- **Task：** T021 — Migrate manager todo lifecycle
- **结果：** 实现完成；Independent Review：`REVIEW_PASSED`。
- **实施摘要：** 新增 Python `todos` 模块的请求/响应 schemas、todo policy、事务服务和兼容 API；覆盖 scoped list/detail、summary、schedule、负责人/状态/截止日期/处理说明更新，以及可复用的每个 risk 一个自动 todo 规则。
- **修改文件：** `project-risk-system/apps/api-python/src/risk_platform/todos/{api.py,policy.py,schemas.py,service.py}`、`project-risk-system/apps/api-python/tests/todos/test_todos.py`。
- **API / database impact：** 提供 `/api/todos`、`/api/todos/{todo_id}` 和 `PATCH /api/todos/{todo_id}`；复用既有 `action_items`、`risks`、`projects`、`risk_timeline_events`、`audit_logs` 表，未新增 migration。
- **兼容与安全：** 保留 `dashboard.view` 查询权限与 `risk.resolve` 更新权限；列表、详情和 mutation 均使用五种项目 scope；越权资源按 `404 NOT_FOUND` 处理；更新中的 todo、timeline 和 metadata-only audit 在同一事务内提交；risk 仍只有 `ACTIVE`/`RESOLVED`，todo 状态不改变 risk lifecycle。
- **Validation：** `uv lock --check` `PASS`；T021 Ruff `PASS`；T021 mypy `PASS`；T021 focused pytest `3 passed`；`git diff --check` `PASS`。Full pytest：`110 passed, 33 skipped, 1 failed`；唯一 failure 是已知 T013 `system_config` regression，按指令保留为 Wave 7 Integration 待处理项。Full Ruff/mypy 的既有 T013 findings 未在本 Task 修复。
- **PostgreSQL：** T021 PostgreSQL API validation 因 `TEST_DATABASE_URL` 未配置而 `skipped`；未使用 SQLite 替代。
- **Review：** 独立按 T021、Design §§3/5/6、ADR 0004/0008/0011/0015、legacy todo/timeline 行为、contracts/frontend dashboard 复核；确认 API contract、scope/permission negative path、三态规则、每 risk 唯一 auto todo、事务边界、timeline/audit hooks 和 write-set，无 unresolved `DESIGN_DEVIATION`。
- **风险 / blocker：** 已知 T013 full-suite regression 仍待 Wave 7 Integration；本次未修复。未启动 Wave 7 Integration 或下一 Wave。

## Checkpoint

- **Checkpoint commit：** `e6638f4`（`backend: complete T021 manager todos`）。
