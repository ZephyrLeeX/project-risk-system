# T005 实施报告

## 执行结果

- Task：T005 — Implement repeatable Python Seed
- 实施结果：`IMPLEMENTED`
- 独立 Review 结果：`REVIEW_PASSED`
- Integration 结果：`NOT_STARTED`（T006 为 `BLOCKED`，Wave 4 不满足集成门槛）
- Implementation commit：`60f17c0f9774d8a8429480912ab17b721bbe2f28`
- 分支：`wave4-t005`

## 实施摘要

- 新增环境驱动的 Python Seed CLI：`python -m risk_platform.seed`。
- 在单个 PostgreSQL 事务中可重复初始化 15 个权限、4 个系统角色及批准权限映射、
  8 个风险类别、3 个风险等级规则、5 个部门和 initial administrator。
- initial administrator 密码必须由环境变量输入并满足密码策略；密码不进入输出或错误。
- 首次创建使用 Argon2id 并设置 `mustChangePassword=true`；重复 Seed 不覆盖现有密码哈希或
  `mustChangePassword` 状态。
- 未导入历史或 demo rows，未创建 migration，未修改 ORM model、shared fixture、application
  bootstrap 或 API contract。

## 修改文件

实现位于隔离分支 `wave4-t005`，尚未集成到 `main`：

- `project-risk-system/apps/api-python/src/risk_platform/seed.py`
- `project-risk-system/apps/api-python/tests/test_seed.py`
- `project-risk-system/apps/api-python/README.md`
- `project-risk-system/apps/api-python/pyproject.toml`
- `project-risk-system/apps/api-python/uv.lock`

## API contract impact

无。

## Database / migration impact

- T005 未产生 Alembic revision。
- T005 独立分支 migration head：`20260810_0001`。
- Empty-schema upgrade、Seed twice、`alembic check`：`PASS`。

## Validation

- `uv sync --frozen`：`PASS`
- `uv lock --check`：`PASS`
- Ruff：`PASS`
- mypy：`PASS`，49 files
- T005 pytest：`PASS`，6 tests
- 真实 PostgreSQL 17.10 全量 pytest：`PASS`，88 tests
- Seed 事务故障注入 / atomic rollback：`PASS`
- stdout / stderr secret scan：`PASS`
- `git diff --check`：`PASS`

## Acceptance criteria

- Twice-run Seed yields identical reference data：`PASS`
- Missing/weak secrets fail safely：`PASS`
- Four role codes / five scopes：`PASS`
- Initial password never logged and must change：`PASS`
- Rerun does not overwrite administrator password：`PASS`
- CLI、tests、usage documentation：`PASS`

## Blocker 与 Design

- T005 Blocker：无。
- 新增 `DESIGN_GAP`：无。
- `DESIGN_DEVIATION`：无。
- Wave 集成阻断来自 T006，与 T005 实现无关。

## 风险

- T010/T011 后续仍需复用相同 permission parity 与首次改密语义。
- 本 Task 代码保持在隔离分支，Wave 4 解除阻断前不得单独集成。
