# T041 实施报告

- **Task:** T041 — Add durable background-task schema
- **Status:** IMPLEMENTED
- **Review:** REVIEW_PASSED
- **Integration:** Wave 5 PASS

## Changed

- 新增可靠性模块拥有的 `durable_tasks`、`task_outbox` ORM models。
- 新增唯一 migration `20260811_0003`，基于 T006 head `20260810_0002`。
- `import_batches`、`mail_sync_batches` 以 `taskId NOT NULL UNIQUE`、`ON DELETE RESTRICT`
  单向引用 task；基础设施无领域多态引用。
- 固化 ADR 0018 kind/status、idempotency、attempt、lease/heartbeat/retry/completion、dispatch
  generation 与 outbox 结构约束；状态迁移说明见 `notes/T041-state-transitions.md`。

## Validation

- `alembic heads`：PASS（`20260811_0003 (head)`）
- Ruff：PASS；mypy：PASS
- PostgreSQL 16 focused schema/constraint tests：PASS（10 passed）
- PostgreSQL 16 Wave full tests：PASS（111 passed）

## Contract / Risk

- `/api` contract：无变化。
- PostgreSQL：新增 2 张表、2 个 enum、1 个 revision；empty/latest upgrade 保持单 head。
- duplicate idempotency/outbox generation、非法状态字段组合、unknown/restricted/duplicate domain
  references 的负向约束：PASS。
- T008 必须用 expected-old-status + lease-token 原子条件更新；kind/domain 类型匹配留在 service。
- `DESIGN_GAP` / `DESIGN_DEVIATION` / Blocker：无。
