# T050 实施报告

- **Status:** `REVIEW_PASSED`
- **Mapped V2 Task:** Task 3 — AgentInteraction + 项目消歧 + WAITING_FOR_USER
- **Started:** 2026-08-17
- **Completed:** 2026-08-17
- **Code checkpoint:** `85dc4bc29f2de19f7a874d2a6c934e68cff8d5f0`

## 实施摘要

- 新增 ADR 0036，冻结 AgentExecution 业务状态、统一 AgentInteraction、PROJECT_SELECTION、一次有效 respond、scope revalidation、durable outbox resume 与 SSE required/resolved 语义。
- 新增 `agent_executions`、`agent_interactions` 与 migration `20260817_0012`；`WAITING_FOR_USER` 独立于 DurableTask `RETRY_WAIT`。
- native worker 检测授权 `project_search` 的 0/1/多候选：唯一匹配自动继续，多候选持久化候选并原子写 `interaction.required` 后释放 Worker；响应后以原始 message/context resume，不调用 Provider 的 respond 路径。
- 新增 `POST /api/agent/interactions/{interactionId}/respond`，严格支持 `SELECT`、`MANUAL_INPUT`、`CANCEL`；手输重新执行 scoped `project_search`，不接受客户端 projectId 作为事实。
- 未实现 WRITE_CONFIRMATION、MutationDraft、Risk/Todo/Project mutation、前端 UI 或 T051 内容。

## API / SSE / database

- response envelope 为现有 `{code,message,data,traceId}`；成功 data 返回 resolved interaction 与 resume `streamUrl`，取消返回 `streamUrl: null`。
- SSE 增加 `interaction.required` / `interaction.resolved`；required 使当前流正常关闭，transport failure 不修改 execution business state。
- interaction 创建、execution→WAITING、bounded resume context 与 required event 在同一 PostgreSQL transaction；respond 原子锁定/消费 interaction，并在同一事务写 resolved event、更新 execution、创建 durable task/outbox/config。
- current owner、conversation、expiry、replay、candidate membership、`agent.use` + `dashboard.view`、project scope 均 fail-closed；candidate rename/删除/不可见由重新 detail/search 处理。

## Tests / Quality Gates

- focused interaction/schema/PostgreSQL：`23 passed`；T050 regression focus：`13 passed`。
- full Python Quality Gates：`uv lock --check`、Ruff、mypy（233 files）、full pytest `464 passed, 52 skipped`；scheduler advisory-lock tests 在停用本地 scheduler 后通过。
- migration fresh-upgrade、single-head、Alembic autogenerate check、enum/constraint checks：PASS，head `20260817_0012`。
- OpenAPI export/generation、contracts sync、frontend typecheck、frontend production build：PASS；未修改前端 UI。
- `git diff --check`：PASS。

## Independent Review

`REVIEW_PASSED`。独立复核覆盖 persistence 原子边界、WAITING 无 Worker/Provider/timeout、resume 关联、one-use/replay/concurrent response、owner/scope/RBAC revalidation、真实 project_search grounding、SSE cursor/close/reconnect 与 T051 零 mutation；无 blocking finding。

## Handoff / readiness

- T050 已完成并通过 Review；T051 保持 `READY`，只允许在后续明确启动后扩展同一 AgentInteraction 承载 WRITE_CONFIRMATION。
- 本轮到此停止，不开始 T051。
