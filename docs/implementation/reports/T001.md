# T001 实施报告

## 结果

- 任务：T001 — 搭建 Python 后端工作区
- 实施结果：已完成
- 验证结果：PASS
- 写入范围：仅 `project-risk-system/apps/api-python/**` 和本必需报告

## 变更

- 新增 Python 3.12 软件包工作区，将
  `requires-python = ">=3.12,<3.13"` 写入项目配置，并将
  `.python-version` 设置为 `3.12`。
- 声明已批准的 FastAPI、Pydantic v2、SQLAlchemy 2、Alembic、
  psycopg 3、Celery 和 Redis 运行时技术栈。
- 声明 pytest、Ruff 和 mypy 开发质量门，并在 `pyproject.toml` 中配置各工具。
- 生成 `uv.lock`；锁文件记录 `requires-python = "==3.12.*"`，并解析出
  45 个软件包，其中包括本地项目。
- 新增 `risk_platform` 源码包，并为设计文档第 4 节列出的所有模块建立明确的空模块边界。
  这些占位模块不暴露 API、持久化或领域行为。
- 新增软件包类型元数据、工作区忽略规则和命令说明，并增加两个冒烟测试，
  用于验证软件包导入及已批准运行时技术栈是否存在。
- 未修改根 pnpm 脚本和根工具配置；uv 的使用范围继续明确归属于
  `apps/api-python`。

## 验收标准

| 验收项 | 证据 | 结果 |
| --- | --- | --- |
| 全新执行 `uv sync --frozen` 成功 | 使用 CPython 3.12.13 创建新的 `.venv` 并安装 44 个锁定依赖；后续冻结检查输出 `Checked 44 packages` | PASS |
| 导入与测试骨架通过 | pytest 收集并通过两个工作区冒烟测试 | PASS |
| 代码检查骨架通过 | Ruff 输出 `All checks passed!` | PASS |
| 类型检查骨架通过 | mypy 在 16 个源文件中未发现问题 | PASS |
| 强制使用 Python 版本 | `.python-version` 为 `3.12`；项目约束为 `>=3.12,<3.13`；锁文件约束为 `==3.12.*`；pytest 在 3.12.13 上运行 | PASS |
| 仅使用已批准工具链 | 锁定依赖包含 ADR 0010 的全部组成部分；源码中不存在 SQLModel 或 SQLite 引用 | PASS |

## 验证

命令均从 `project-risk-system/apps/api-python` 运行。为兼容沙箱，
`UV_CACHE_DIR` 指向可写的临时存储；这不会改变锁文件或项目行为。

| 命令 | 结果 |
| --- | --- |
| `uv sync --frozen` | PASS — 根据锁文件创建 CPython 3.12.13 环境并安装 44 个依赖 |
| `uv run ruff check .` | PASS — `All checks passed!` |
| `uv run mypy .` | PASS — 16 个源文件中未发现问题 |
| `uv run pytest` | PASS — 2 个测试通过 |

## 阻塞项与风险

- 阻塞项：无。
- 已知集成风险：根 pnpm 命令不会调用 uv。这是 T001 的有意安排，
  因为该任务要求明确 uv 工作目录的归属，且其验收标准不需要接入根命令。
- 外部验证：不适用；T001 不包含外部邮箱、Provider、TLS、数据库、Redis 或 Docker 行为。

## 设计状态

- DESIGN_GAP：未发现新增项。
- DESIGN_DEVIATION：无。
- API 兼容性：不适用；未引入运行时端点。
- 迁移影响：无；未引入模型或迁移。
