# T029 实施报告

## 结果

- Task：T029
- Readiness：`READY`
- 实施结果：`DESIGN_GAP`
- Independent Review：未启动（未产生候选实现）
- 设计状态：`DESIGN_GAP`

## Readiness

T004、T007、T008、T010、T014、T028 均为已完成依赖；ADR 0019/0020 已批准公开 SSE event、持久化 event、确认边界、断线/恢复和 Worker 必须使用 durable task 的规则。DG-05/DG-08 不在本 Task 范围内。

## DESIGN_GAP

无法在已批准边界内创建 ADR 0020 所要求的 Agent Worker execution：

1. `DurableTaskKind` 和 PostgreSQL enum/registry 没有 Agent execution kind。ADR 0018 明确规定新增 kind 必须经显式设计与 migration 加入统一 registry/数据库约束，禁止业务模块使用未登记的自由文本 kind；ADR 0020 未指定应新增的 Agent kind、其稳定 idempotency key 或 retry/timeout definition。
2. ADR 0020 的 task payload 只列出 `conversation_id`、`user_message_id`、`requested_by_user_id` 和 execution configuration identifier，但未定义 execution configuration identifier 的持久化权威来源、字段或有效性规则。
3. Provider 对不可信模型输出的受限协议未获批准：未定义 intent/tool-call/arguments/text-delta/preview proposal 的结构、允许的 mutation-preview 工具集合，或 malformed output 到 ADR 0019 `error` event 的确定映射。自行选择 JSON shape 或把任意模型文本解释为领域操作，会扩张 Agent 安全与公开行为契约。

这些缺失直接阻止 T029 的 Provider 编排、intent/arguments validation、fake Provider 边界与 malformed-model acceptance tests。实现任何一个候选结构都会构成未经批准的架构/契约决定，因此未修改生产代码、migration 或测试。

## Independent Review / validation

- Independent Review：未启动；没有实现 diff 可审查。
- Ruff / mypy / focused pytest / PostgreSQL 16：未执行；Task 在实现前触发 stop condition，不能以虚构 Provider 或未登记 task kind 验证。
- `uv lock --check`：未执行（无依赖改动）。
- `git diff --check`：将在 metadata 更新后执行。

## Checkpoint

未创建 code checkpoint；`DESIGN_GAP` 未解决前不满足 checkpoint 条件。

## Wave 14

Wave 14 保持 `IN_PROGRESS`。未执行 T030，未启动 Wave 14 Integration 或下一 Wave，DG-05/DG-08 未处理。
