# T029 实施报告

## 结果

- Task：T029
- 实施结果：`REVIEW_PASSED`
- Independent Review：`REVIEW_PASSED`
- Code checkpoint：`b57e831e68e47cdfba43f78285d10481c15000e1`
- Wave 14：`IN_PROGRESS`

## 实施摘要

- 新增 ADR 0028 `AGENT_EXECUTION` durable kind、固定 3 attempts / 90s timeout / 30s exponential backoff，以及 PostgreSQL `agent_execution_configs` immutable snapshot migration。
- Agent module 暴露 dependency-injected `agent_execution_handlers(...)`，mapping 仅含 `AGENT_EXECUTION`；未 import、修改或注册 shared production `celery_app`，production dependency construction/handler merge/shared registration 保持 T040 独占。
- 完成 `AGENT_PROVIDER_EXECUTION_V1` raw status/body transport、128 KiB/UTF-8/strict JSON object/duplicate-key、phase/action/count/text/request/tool-result size 与 closed T028 tool arguments validation。
- 完成 REPORT/PROCESS/RESOLVE canonical preview 本地权限、project scope、risk/todo/assignee 关系验证与一次性 confirmation token issue；Agent execution 不执行 risk/todo/timeline/audit business mutation。
- 完成 fenced durable success/failure/retry/cancellation、attempt-wide 90s deadline、绝对 15s heartbeat cadence、completed-event crash recovery、config-invalid explicit event，以及 terminal-slot backpressure semantics。
- SSE 仅从 PostgreSQL `agent_events` 读取，支持严格 sequence、cursor resume/unrecoverable、disconnect cancellation、persistent heartbeat、bounded/idempotent idle error、256 events/1 MiB backpressure 和 terminal close。
- execution secret snapshot 随 conversation retention 删除；直接 references 保持 `RESTRICT`，configuration 除 `cancellationRequestedAt NULL -> timestamp` 外不可更新。

## Independent Review

初审与首次复审分别发现 tool deadline/heartbeat/cancellation、raw Provider transport、idle/backpressure、negative matrix、config-invalid event、terminal capacity 和跨 await heartbeat cadence 缺陷。上述问题均在 T029 ownership 内完成 remediation。

最终 Independent Review：`REVIEW_PASSED`，无剩余 actionable finding。Reviewer 确认 module-local mapping 仅含 `AGENT_EXECUTION`，未越过 T040 composition write-set，且未产生 risk/todo/timeline/audit business mutation。

## Module-local worker / SSE acceptance

- PostgreSQL 16 + Redis 7 + isolated real Celery `solo` worker 使用 T008 `register_executor` 注册 T029 module-local mapping，覆盖 discovery、success、closed tool、preview、invalid output、timeout/retry、cancellation、heartbeat、backpressure、config mismatch、completed recovery 和 no-write assertions。
- slow-tool real-worker acceptance 验证 attempt-wide deadline、tool 期间 fenced heartbeat、取消后不进入 RESPOND；8 个连续短 tool calls 验证 heartbeat 不在 await 边界重置。
- PostgreSQL-backed SSE/ASGI acceptance 覆盖 ordered persistence、cursor resume、unknown cursor、stream framing/header、idle close 幂等、disconnect cancellation、capacity terminal slot。

## Validation

- Ruff：`PASS` — `mise exec uv@0.12.3 python@3.12.13 -- uv run --frozen ruff check .`
- mypy：`PASS` — `Success: no issues found in 182 source files`
- focused PostgreSQL 16 + Redis/Celery/SSE：`PASS` — `27 passed`
- Alembic：`PASS` — empty-schema `upgrade head` / autogenerate `check` 由 PostgreSQL tests 覆盖；`alembic heads` = `20260812_0008 (head)`
- `uv lock --check`：`PASS` — `Resolved 57 packages`
- `git diff --check`：`PASS`

## Scope

T030 未执行；Wave 14 Integration、下一 Wave、DG-05、DG-08 均未启动或处理。Wave 14 保持 `IN_PROGRESS`。

## Grounded RESPOND internal MVP limitation（2026-08-16）

- 结果：`ACCEPTED_INTERNAL_MVP_LIMITATION`
- Independent Review：`DESIGN_GAP reviewed and accepted for INTERNAL_MVP`
- grounded RESPOND 已改为持久化 Provider 根据本轮 `toolResults` 生成的自然语言 `TextAction`，不再向用户输出原始工具 JSON；业务数据存在时缺少 `TextAction` 会以 `AGENT_PROVIDER_INVALID_OUTPUT` fail-closed。
- 已保持 PLAN 必须 `grounded=true` 且至少调用一个当前用户授权工具、空结果固定“当前系统数据中未找到”、history/模型知识不得作为当前业务事实、RBAC/data scope/snapshot/SSRF 与 prompt-injection 边界。
- 已知限制：现有 Provider protocol 没有逐 claim 的结构化 source reference。因此自然语言 RESPOND 的事实一致性依赖 system instruction，尚未实现逐 claim 的机器可验证 source reference；不得声称完全杜绝 hallucination 或所有自然语言事实均经过机器验证。
- 更高 assurance 阶段如需该保证，单独设计 `structured claims`、`fact references`、`sourceTool` 与 `sourceItemId`；本 checkpoint 不扩大 Provider protocol。
