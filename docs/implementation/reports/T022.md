# T022 实施报告

- **Task：** T022 — Migrate risk lifecycle and timeline
- **结果：** `STABLE`；Independent Review：`REVIEW_PASSED`（含本次 Quality Fix 复核）。
- **实施摘要：** 新增 Python risks/timeline DTO、策略、事务服务和兼容 API；覆盖风险幂等创建、自动待办、创建/解除/重开时间线、ACTIVE↔RESOLVED 两态生命周期、关联待办联动、风险/已解除风险/时间线范围查询与详情。
- **修改文件：** 实施阶段文件保持不变；本次 Quality Fix 仅修改 T022 的 `risks/api.py`、`risks/schemas.py`、`risks/service.py`、`timeline/__init__.py`、`timeline/policy.py` 和 `tests/timeline/test_timeline_policy.py`。
- **API / database impact：** 提供 `/api/risks`、`/api/risks/{id}`、`/api/risks/{id}/resolve`、`/api/risks/{id}/reopen`、`/api/risks/resolved`、`/api/dashboard/timeline` 及详情路由；复用既有 `risks`、`action_items`、`risk_timeline_events`、`audit_logs` 表，未新增 migration。
- **安全与一致性：** 查询和详情均使用五种 project scope；越权按 `404` 处理；生命周期写入要求 `risk.resolve` 和 origin 校验；risk/todo/timeline/metadata-only audit 在同一事务；唯一指纹竞争使用 PostgreSQL savepoint 收敛为幂等回读。
- **Quality Fix：** 仅修复 T022 Ruff 格式/import/命名 findings 与 `risks/service.py` 的 6 项 mypy 类型错误；使用明确 `Risk` 返回类型、`Sequence` 条件集合、非空 owner 收窄和保留中文文案的精确 lint 标注，无新增 `Any`、`type: ignore` 或检查禁用。
- **Validation：** `ruff check .` `PASS`；`mypy .` `PASS`；`pytest tests/risks/test_risks.py tests/timeline/test_timeline_policy.py` `PASS`（4 passed）；`uv lock --check` `PASS`；`git diff --check` `PASS`。Full pytest 已尝试，但当前 pytest 全量收集/捕获环境异常，未作为本次 focused quality fix 阻塞项。
- **PostgreSQL：** T022 无专属 PostgreSQL tests，`N/A`；未使用 SQLite 替代。
- **Review：** Independent Reviewer 仅检查本次 T022 Quality Fix diff，确认 Ruff/mypy 修复、导入初始化顺序、类型收窄与 API/domain behavior 无语义漂移，结果 `REVIEW_PASSED`。
- **风险 / blocker：** T023 保持 READY，未启动；Wave 8 integration 未执行。

## Checkpoint

- **Implementation checkpoint：** `205e8fc69686d00f2d20b4f75dbf405a8ace0310`（`backend: complete T022 risk lifecycle and timeline`）。
- **Quality Fix checkpoint：** `ba339789409dc8138f763c6325262e9cff1be319`（`fix: close T022 quality validation`）。
