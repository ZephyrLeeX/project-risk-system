# T022 实施报告

- **Task：** T022 — Migrate risk lifecycle and timeline
- **结果：** `IMPLEMENTED`；Independent Review：`REVIEW_PASSED`。
- **实施摘要：** 新增 Python risks/timeline DTO、策略、事务服务和兼容 API；覆盖风险幂等创建、自动待办、创建/解除/重开时间线、ACTIVE↔RESOLVED 两态生命周期、关联待办联动、风险/已解除风险/时间线范围查询与详情。
- **修改文件：** `project-risk-system/apps/api-python/src/risk_platform/risks/{__init__.py,api.py,schemas.py,service.py}`、`project-risk-system/apps/api-python/src/risk_platform/timeline/{__init__.py,policy.py}`、`project-risk-system/apps/api-python/tests/{risks/test_risks.py,timeline/test_timeline_policy.py}`。
- **API / database impact：** 提供 `/api/risks`、`/api/risks/{id}`、`/api/risks/{id}/resolve`、`/api/risks/{id}/reopen`、`/api/risks/resolved`、`/api/dashboard/timeline` 及详情路由；复用既有 `risks`、`action_items`、`risk_timeline_events`、`audit_logs` 表，未新增 migration。
- **安全与一致性：** 查询和详情均使用五种 project scope；越权按 `404` 处理；生命周期写入要求 `risk.resolve` 和 origin 校验；risk/todo/timeline/metadata-only audit 在同一事务；唯一指纹竞争使用 PostgreSQL savepoint 收敛为幂等回读。
- **Validation：** `python3 -m compileall -q src tests` `PASS`；`git diff --check` `PASS`。Ruff、mypy、pytest 无法执行：环境缺少 `mise`、`uv` 及 Python 依赖（`pytest`、`ruff`、`mypy`、`pydantic` 等）。
- **PostgreSQL：** 因 `TEST_DATABASE_URL` 未配置而 `skipped`；未使用 SQLite 替代。
- **Review：** 按 T022、Design §§5/6、ADR 0004/0008/0011/0015、legacy risk lifecycle/timeline、contracts 和 T021 todo service 独立复核；确认两态规则、scope/permission、事务联动、并发幂等和 write-set，无 unresolved `DESIGN_GAP` / `DESIGN_DEVIATION`。
- **风险 / blocker：** Python quality gates 和 PostgreSQL API validation 需在具备仓库工具链及 `TEST_DATABASE_URL` 的环境补跑；T023 保持 READY，未启动下一 Task/Wave。

## Checkpoint

- **Checkpoint commit：** `205e8fc69686d00f2d20b4f75dbf405a8ace0310`（`backend: complete T022 risk lifecycle and timeline`）。
