# T051 Implementation Report

- **Status:** REVIEW_PASSED
- **Mapped V2 Task:** Task 4 — 写操作 + 人工确认 + Mutation

## 实施结果

- **启动/完成：** 2026-08-17 / 2026-08-17
- **Code checkpoint：** `2696e1cca23d2ba7dc331f8dd3373e1886d4d34a`
- **Design checkpoint：** ADR 0037 `approved`，冻结六个 proposal tools、MutationDraft、WRITE_CONFIRMATION、CONFIRM/CANCEL、editable final fields、server-only commit、Risk 1:N Todo、batch partial success 与 fail-closed Project status policy。
- **实现：** model catalogue 仅增加六个 proposal tools；proposal 只持久化 MutationDraft + WRITE_CONFIRMATION；确认前不写业务表；确认时重新执行 permission、scope、resource/category/status/business validation；单项与 batch commit 使用事务/savepoint 和幂等指纹。
- **Risk/Todo：** `RiskSourceType.AGENT`、确认用户 reporter、Risk create/update/resolve、Risk update allowlist；Risk 由 1:1 改为 1:N Todo；Risk create 保留一个 default Todo；DB partial unique index 与 domain/concurrency guard 双重保护；Agent todo 只能绑定已有 Risk。
- **Project status：** 现有 authority 仅能确认 `DELIVERY/COMPLETED/ARCHIVED` enum，无法确认合法 transition；已建立集中 `ProjectStatusPolicy` 并返回 `DESIGN_GAP`，没有猜测实现 status mutation。
- **数据库/API：** Alembic `20260817_0013`，fresh PostgreSQL chain 单 head；无 backfill/dual-write/online migration；OpenAPI 与 generated types 已同步。

## Validation

- T051 focused PostgreSQL/interaction/contract/schema：`22 passed`。
- Ruff：PASS；mypy：PASS（237 source files）；Alembic head：`20260817_0013`；`git diff --check`：PASS。
- `pnpm contracts:sync`：PASS；`packages/contracts` typecheck：PASS。
- 全量后端 PostgreSQL：`463 passed, 52 skipped, 6 failed`。失败为既有 worker/SSE/scheduler 集成环境基线（原生 worker fixture 与全局 advisory lock），并非 T051 write-set；T051 专项与新增 catalogue regression 已单独通过。NestJS/Prisma reference `pnpm typecheck` 仍有既有类型生成错误，未修改 legacy。

## Independent Review

`REVIEW_PASSED`。攻击者视角复核确认：无确认无业务 mutation；commit handler 不在 catalogue；editable allowlist 拒绝 mass assignment；owner/scope/RBAC/stale resource/replay/double-click/concurrency fail closed；batch A/C 保留、B 回滚；audit 只记录 metadata，不记录正文或 Tool Result。CandidateRisk 的 basis/provenance contract 保持 AI_ANALYSIS 与 SYSTEM_FACT/MIXED 的边界。

## 风险与交接

- `DESIGN_GAP`：Project status 合法 transition authority 尚缺，project_status_update_proposal 保持阻断。
- 全量历史 integration 失败已如实记录，未以 T051 名义修复 legacy 或扩大 scope。
- T052 readiness：T051 code、migration、contracts 和 report 已完成；T052 仍未开始，需由 Orchestrator 后续单独调度。
