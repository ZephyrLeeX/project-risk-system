# T006 实施与 Review 报告

## 执行结果

- Task：T006 — Enforce append-only audit chain
- 实施结果：`IMPLEMENTED`
- 最终 Review 结果：`REVIEW_FAILED`
- Task 状态：`BLOCKED`
- Integration 结果：`NOT_STARTED`
- 初始 Implementation commit：`12f5c4deb1baae1ad2a273d1d1ae5706985979c5`
- 第 1 轮修复 commit：`8b9fa2a6b901c80510170bbfc58e9225faf4706a`
- 第 2 轮修复 commit：`ae4a910cdcaa41ce267ccb1b08e2c9d32218aaed`
- 分支：`wave4-t006`

## 实施摘要

隔离分支实现了：

- `20260810_0002` Alembic revision，`down_revision=20260810_0001`；
- `pgcrypto`、legacy-field canonical hash function、transaction-scoped advisory lock append
  trigger、hash/link unique indexes；
- PostgreSQL 级 `UPDATE`、`DELETE`、`TRUNCATE` 拒绝；
- caller-owned `AsyncSession` success/failure `AuditService`；
- 只读 deterministic verifier，发现断链返回 `INVALID` 且不修复；
- bounded deep snapshot redaction。

## 修改文件

实现位于隔离分支 `wave4-t006`，因 Review 未通过而未集成到 `main`。主要 write set：

- `project-risk-system/apps/api-python/alembic/versions/20260810_0002_audit_chain.py`
- `project-risk-system/apps/api-python/alembic/versions/20260810_0002_audit_chain.sql`
- `project-risk-system/apps/api-python/src/risk_platform/audit/**`
- `project-risk-system/apps/api-python/tests/audit/test_audit_chain.py`
- `project-risk-system/apps/api-python/tests/test_postgresql_schema.py`

`tests/test_postgresql_schema.py` 的修改仅更新 T003 留下的 `head=20260810_0001` 和
“尚无 audit trigger”阶段性断言。该最小扩展不与 T005 write set 冲突。

## Review 历史

### 初审：`REVIEW_FAILED`

- `TRUNCATE` 可绕过 UPDATE/DELETE row trigger，清空 audit evidence。
- 自然嵌套的 `mail.body`、`attachment.text`、`model.response` 会绕过脱敏。

### 第 1/2 轮修复复审：`REVIEW_FAILED`

- PostgreSQL `TRUNCATE` 拒绝已修复并通过。
- 脱敏仍遗漏复数、复合键和相邻路径，例如 `attachments[].text`、`mail.htmlBody`、
  `messages[].content`、`model.completion`。

### 第 2/2 轮修复最终复审：`REVIEW_FAILED`

最终阻断证据：

- `model.result` 和根级 `completion` 仍可能原样保存模型输出；
- `mailMessage.bodySize`、`mailMessage.contentType`、`email_messages[].textLength`、
  `attachment-files[].content-type`、`providerModel.outputTokenCount`、
  `providerModel.responseTimeMs` 等 metadata 被错误脱敏；
- structured `LLMResponse` 可能整体被替换，导致 `requestId`、model name、latency 等
  metadata 丢失。

该结果违反“禁止保存模型原始响应”与“保留非敏感 metadata”的安全/可用性边界。两轮修复额度
已用尽，按执行规则将 T006 标记为 `BLOCKED`，不再继续修复。

## PostgreSQL / Migration validation

以下候选分支行为均为 `PASS`：

- Migration graph：`20260810_0001 -> 20260810_0002`，single head；
- empty-schema upgrade；
- `alembic check` 无意外 diff；
- `pgcrypto`、3 个 functions、4 个 triggers、2 个 T006 unique indexes；
- 20 路 concurrent inserts 形成单一有效 hash chain；
- `UPDATE` / `DELETE` / `TRUNCATE` 全部由 PostgreSQL 拒绝；
- transaction commit / rollback；
- deterministic verifier detects tampering and never repairs；
- downgrade 清理 T006 objects、保留 audit rows。

这些结果属于未集成候选分支，不代表 Wave 4 migration 已进入 `main`。`main` 仍保持
`20260810_0001`。

## Quality validation

- `uv sync --frozen`：`PASS`
- `uv lock --check`：`PASS`
- Ruff：`PASS`
- mypy：`PASS`，52 files
- 真实 PostgreSQL 17.10 全量 pytest：`PASS`，88 tests
- API compatibility：未受影响

## Blocker 与 Design

- Blocker：audit snapshot redaction 同时存在漏脱敏与 metadata 过度脱敏。
- 新增 `DESIGN_GAP`：无。
- `DESIGN_DEVIATION`：无。

## 风险

- 固定 advisory transaction lock 会串行化 audit append，后续容量验收需观测。
- 部署数据库角色需要安装或复用 `pgcrypto` 的权限。
- 在重新批准修复工作前，不得集成 T006 候选 commits。
