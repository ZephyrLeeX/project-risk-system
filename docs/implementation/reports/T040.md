# T040 实施与独立审查报告

## 结果

- Task：T040
- Implementation：`IMPLEMENTED`
- Independent Review：`REVIEW_PASSED`
- 当前状态：`REVIEW_PASSED`
- Wave 16：`IN_PROGRESS`（Integration 未启动）

## 实施摘要

- 新增 `src/risk_platform/composition.py` 作为 T002 之后 shared FastAPI/Celery composition root 的唯一
  owner：`load_cipher`、`import_storage_root`、生产 `AgentProviderAdapter`（受限 Provider）、
  `build_services`、`build_tool_registry` 与 `merge_worker_handlers`。
- `main.py` 重组为完整 production composition：装配 17 个模块 router（含 mailbox 的 `candidate_router`），
  在 lifespan 内构造全部 19 个 domain service 并绑定到 `app.state`；startup 不建表，shutdown 释放 engine。
- 新增 `src/risk_platform/worker.py`：构造 production session factory、Provider adapter、T028 tool
  registry，合并 8 个 module-local handler mapping，并对 shared `celery_app` 恰好一次调用 T008
  `register_executor`（模块级 `_registered` 守卫）。
- `AgentProviderAdapter` 从 immutable `AgentExecutionConfig` snapshot 解密 API key，经
  `OutboundEndpointGuard` 校验后调用 OpenAI-compatible `chat/completions`，仅返回 raw
  `ProviderTransportResponse`；超时/连接失败映射为 retryable，SSRF/DNS 违规与解密失败作为永久配置错误
  传播。不持久化 prompt、Provider request/response 或 secret。
- 新增 `tests/test_composition.py`：service graph 完整性、worker handler 8 类覆盖、全 app 路由唯一性、
  lifespan boot/shutdown、`register_production_worker` 幂等。

## Composition ownership（ADR 0028 addendum）

T029 的 `agent_execution_handlers` 仅被消费，未被修改或注册；production session factory / Provider
adapter / T028 registry 的构造、handler merge 与 shared `celery_app` registration 全部归 T040。`main.py`
与 `worker.py` 之间无 import 依赖，HTTP 进程不注册 Celery executor。

## 契约 / 依赖变更

- `pyproject.toml` 新增 `python-multipart>=0.0.9,<1`：compose imports upload router（`UploadFile`）的
  FastAPI 必需依赖；`uv.lock` 同步至 58 packages。无新 endpoint、schema、migration、业务逻辑或
  production Compose。

## Validation

- Ruff：`All checks passed`
- mypy：`Success: no issues found in 186 source files`
- focused pytest（`tests/test_composition.py`）：`5 passed`
- full pytest：`167 passed, 86 skipped`
- `uv lock --check`：通过（Resolved 58 packages）
- `git diff --check`：通过
- worker task discovery：`merge_worker_handlers` 覆盖全部 8 个 `DurableTaskKind`；隔离 Celery app 上
  `risk_platform.reliability.execute` 注册成功

## PostgreSQL 16 + Redis 7 + real Celery worker integration

本执行环境缺少 PostgreSQL 16 / Redis 7（`TEST_DATABASE_URL` 未配置，无 `postgres`/`redis-server`），
该 integration validation 无法执行，已在报告中明确记录为 unavailable/skipped。86 个依赖
`TEST_DATABASE_URL` 的 PostgreSQL/Redis/Celery 测试相应 `skipped`，无失败。

## 范围

未修改 T029 orchestration 语义、任何 migration、NestJS reference、前端或 production Compose。
Integration、下一 Wave、DG-05 和 DG-08 均未启动或处理。

## Checkpoint

- 实现代码 checkpoint：`2a99da0`
- Review 元数据 checkpoint：见 `docs/implementation/reports/WAVE-16-PARTIAL.md` 与 `EXECUTION_STATE.md`
