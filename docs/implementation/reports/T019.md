# T019 实施报告

- **Task：** T019 — Migrate import rollback
- **结果：** `IMPLEMENTED`
- **Independent Review：** `REVIEW_PASSED`
- **Checkpoint：** 待 checkpoint commit 后回填

## 实施摘要

在 Python imports bounded module 内实现已导入批次的安全回滚：锁定批次并校验状态，拒绝存在后续导入覆盖的项目；在单一 PostgreSQL transaction 中删除本批次时间线，按项目与风险快照恢复或删除项目/风险，标记项目、补充回款、法务行和批次为 `ROLLED_BACK`，清理无引用的导入临时部门，并写入 metadata-only rollback audit。新增 rollback API route；确认导入时补全风险前后快照，保证风险与关联待办可安全恢复或级联删除。源文件不会被删除。

## 修改文件

- `project-risk-system/apps/api-python/src/risk_platform/imports/api.py`
- `project-risk-system/apps/api-python/src/risk_platform/imports/commit_service.py`
- `project-risk-system/apps/api-python/tests/imports/test_commit_contract.py`

## API / database impact

- 新增兼容既有 contract 的 `POST /api/imports/project-list/batches/{batch_id}/rollback`，权限为 `admin.import.manage`，响应 envelope 与既有 imports API 一致。
- 无 migration、表结构、枚举或 retention policy 变更；复用既有项目/风险/待办/时间线/导入快照字段。

## Independent Review

Reviewer 根据 T019、Design §6、ADR 0008/0012/0015、legacy rollback service、当前 imports/risk/todo/timeline models、diff 和 focused tests 复核：批次锁定、后续写冲突保护、单事务恢复、项目新增/更新分支、风险快照、时间线清理、附属行状态、临时部门清理、权限路由和 metadata-only audit 均符合当前 contract。未发现 `DESIGN_GAP`、`DESIGN_DEVIATION` 或 unresolved `REVIEW_FAILED`。结果：`REVIEW_PASSED`。

## Validation

| 检查项 | 结果 |
|---|---|
| Ruff | `PASS` — `.venv/bin/ruff check .` |
| mypy | `PASS` — `.venv/bin/mypy .`（135 source files） |
| focused pytest | `PASS` — `5 passed`（imports rollback contract/snapshot tests） |
| full pytest | `PASS` — `125 passed, 34 skipped`；skip 项为未配置 PostgreSQL 的既有集成测试 |
| `uv lock --check` | `PASS` — uv 0.12.3 |
| `git diff --check` | `PASS` |
| PostgreSQL-specific validation | `PASS` — 临时隔离 PostgreSQL 16 执行 schema/durable-task integration，`11 passed`；未使用 SQLite，容器已停止 |
| `ruff format --check .` | `UNAVAILABLE` — 基线存在 19 个既有未格式化文件；本次修改文件已通过 format check |
| mise | `UNAVAILABLE` — 执行环境未安装 `mise`；未修改工具链或依赖 |

## 风险与状态

- 无 `DESIGN_GAP`、`DESIGN_DEVIATION` 或未解决 blocker。
- 本次未启动 Wave 10 Integration；T016、T020、T025 保持 READY。
