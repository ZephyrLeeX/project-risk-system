# T012 实施报告

- **Task:** T012 — Migrate role and permission administration
- **Status:** REVIEW_PASSED
- **Review:** REVIEW_PASSED（独立 Review 检查范围、契约、权限边界、事务、审计和测试）

## 实施摘要

- 新增 `admin/roles` 模块，实现 `/api/admin/roles` 的列表、创建、更新、删除和
  `/api/admin/permissions` 查询。
- 新增 `admin/options` 模块，实现启用部门选项查询 `/api/admin/departments`。
- 保留四个默认角色、权限编码、五种数据范围边界；系统角色不可停用/删除，使用中角色不可删除。
- 角色敏感写操作使用 PostgreSQL 事务，并记录成功与失败的 metadata-only audit；失败审计不含请求内容。

## 修改文件

- `project-risk-system/apps/api-python/src/risk_platform/admin/roles/**`
- `project-risk-system/apps/api-python/src/risk_platform/admin/options/**`
- `project-risk-system/apps/api-python/tests/admin/roles/test_admin_roles.py`

## Validation

- `.venv/bin/ruff check .`：PASS。
- `.venv/bin/mypy .`：PASS（91 source files）。
- `.venv/bin/pytest -q -ra`：PASS（96 passed，32 skipped）。
- T012 专属测试：2 passed，2 skipped；PostgreSQL 集成测试因 `TEST_DATABASE_URL` 未配置而 skipped。
- `git diff --check`：PASS。

## Wave 7 Integration Fix

- Root cause：T012 update regression 的测试复用了 create fixture；其中 `code` 不属于当前
  `UpdateRoleRequest`，因此 API 按契约返回 `422`。
- Fix：仅在 `project-risk-system/apps/api-python/tests/admin/roles/test_admin_roles.py` 中拆分
  update fixture，移除 `code`；未修改 production code、角色/权限/部门管理语义或 API contract。
- Fix validation：T012 focused tests `4 passed`；full isolated PostgreSQL pytest `144 passed, 1 skipped`。
- Independent Review：无需新增；本次仅修正过期测试 fixture，原 T012 `REVIEW_PASSED` 继续有效。

## Contract / Review

- `/api` contract：保持 legacy admin role/permission/department 的 path、method、响应 envelope 和权限要求；未修改前端或 TypeScript contracts。
- Database：未新增 migration；使用既有 `roles`、`permissions`、`role_permissions`、`user_roles`、`departments` 表和现有审计服务。
- `DESIGN_GAP` / `DESIGN_DEVIATION`：无。
- Independent Review：`REVIEW_PASSED`。

## Checkpoint

- Checkpoint commit：`7361d26`（`backend: complete T012 role administration`）。
