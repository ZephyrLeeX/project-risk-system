# T028 实施报告

## 结果

- Task：T028
- Readiness：`READY`
- 实施结果：`IMPLEMENTED`
- Independent Review：`REVIEW_PASSED`
- 设计状态：无 `DESIGN_GAP`，无 `DESIGN_DEVIATION`

## 实施摘要

实现了 Agent 会话创建、继续、owner-scoped history/message page 与帮助目录；创建会话时按 ADR 0027 已冻结的当前 retention release 写入 `expiresAt` 和 `retentionConfigVersion`。新增的 `AgentToolRegistry` 只登记只读业务工具，并在每次调用重新检查 `dashboard.view`，委托既有 Dashboard/Risk/Todo/WeeklyReport service，不生成或执行 SQL，不包含业务写操作。

工具结果携带 `tool`、`data`、`dataAsOf`、`traceId`；未知工具和权限不足 fail closed。Agent message content 按不可信用户文本持久化，不解析为指令，也不进入 SQL/Provider 执行路径。

## 修改文件

- `project-risk-system/apps/api-python/src/risk_platform/agent/api.py`
- `project-risk-system/apps/api-python/src/risk_platform/agent/repository.py`
- `project-risk-system/apps/api-python/src/risk_platform/agent/schemas.py`
- `project-risk-system/apps/api-python/src/risk_platform/agent/service.py`
- `project-risk-system/apps/api-python/src/risk_platform/agent/tools.py`
- `project-risk-system/apps/api-python/src/risk_platform/main.py`
- `project-risk-system/apps/api-python/tests/agent/test_agent.py`

## Contract / database impact

- 使用 T004 已存在的 `agent_conversations`、`agent_messages` 表及其 PostgreSQL contiguous-sequence trigger；无新增 migration。
- API 遵循 ADR 0019 的 `/api/agent/conversations`、`messages`、`help` 路径、统一 envelope、`201/202` 状态、owner 404 和 `agent.use` 前置权限。
- 继续消息响应仅返回 `userMessage` 与 `streamUrl`；创建响应返回 `conversation`、`userMessage` 与 `streamUrl`。
- SSE、Provider orchestration、durable Agent execution、preview 和业务写入均留给 T029/T030。

## Independent Review

独立检查确认：write-set 限于 Agent 模块、其 composition wiring 与 T028 tests；owner 条件在 repository 查询中执行；工具 registry 为封闭名称集合；每次 invoke 重新检查权限；weekly/risk/todo 查询复用既有 scoped domain service；没有任意 SQL、跨表旁路、业务写入或 DG-05/DG-08 处理。

## Validation

- `ruff check .`：PASS
- `mypy .`：PASS（178 source files）
- focused pytest：PASS（T028 PostgreSQL 16 isolated schema，`2 passed`）
- PostgreSQL 16：PASS，隔离 schema 执行 Alembic `upgrade head`，验证会话序列、owner 隔离和 90 天 frozen retention fact
- `uv lock --check`：PASS
- `git diff --check`：PASS
- 全量 pytest 额外尝试：在 `61 passed, 1 skipped` 后因既有 Redis connection 路径等待被中断；该环境级等待不属于 T028 focused validation，未将其计为 PASS

## Checkpoint

- Code checkpoint：待本报告及执行状态提交后回填
- Metadata checkpoint：如需回填 checkpoint SHA，将在 code checkpoint 后单独提交

## Wave 13

Wave 13 保持 `IN_PROGRESS`；未执行 T029，未启动 Wave 13 Integration，未启动下一 Wave，DG-05/DG-08 未处理。
