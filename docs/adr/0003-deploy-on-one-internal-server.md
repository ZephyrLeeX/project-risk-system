# 部署到单台内网服务器

完整 MVP 的目标运行环境是一台内网服务器，使用 Docker Compose 管理 FastAPI、PostgreSQL 和必要的后台任务组件。该方案满足当前组织内部使用和运维成本要求，同时通过无状态 API 与独立任务组件保留未来扩展能力。

## Consequences

- 首期不引入 Kubernetes 或多节点高可用架构。
- 数据库、上传文件、邮件同步状态和日志必须使用持久化存储，并纳入备份恢复方案。
- 后台任务不能依赖某个 Web 请求持续存活；进程重启后应能识别并恢复未完成任务。
